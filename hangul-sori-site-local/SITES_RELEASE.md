# Hangul Sori site release

This repository root is the deployable Hangul Sori Sites source package.

- Production build: `npm run build`
- Rendered route tests: `node --test tests/rendered-html.test.mjs`
- Hosting project: `.openai/hosting.json`
