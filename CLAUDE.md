# CLAUDE.md → AGENTS.md

이 프로젝트의 상시 지침·파일 맵·규칙·지속 메모리는 저장소 루트의 **`AGENTS.md`** 에 있다.
과거 **세션 히스토리는 `docs/SESSION_LOG.md`** 로 분리되어 있다 (매 세션 자동으로 읽지 말고, 필요할 때만 grep/Read).

**세션 시작 시 `AGENTS.md`(간결) 를 읽고 시작한다.** 새 변경 기록은 `docs/SESSION_LOG.md` 최상단에 남긴다.
