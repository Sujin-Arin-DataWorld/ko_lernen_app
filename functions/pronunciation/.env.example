# Deployment environment for the pronunciation codebase (firebase-functions dotenv).
# Copy to functions/pronunciation/.env (git-ignored) ONLY after the runbook's Azure F0
# resource + AZURE_SPEECH_KEY secret checks pass, then redeploy so the secret binding
# matches. Any other value keeps the callable disabled and binds no secret.
PRONUNCIATION_ASSESSMENT_MODE=azure_f0
