import 'package:flutter/widgets.dart';

// XCTest host only: no application services, authentication, or network setup.
// The unsigned release compile continues to use the normal application target.
void main() {
  runApp(const SizedBox.shrink());
}
