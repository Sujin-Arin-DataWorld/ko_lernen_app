<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:label="Hangul Sori"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Impeller deaktiviert → Skia-Renderer. GRUND (2026-08-06 실기기
             M2101K6G/Android 12): Impeller kann auf Android < 33 die Video-
             Textur-Fence nicht abwarten ("ImageReaderSurfaceProducer:
             ImageTextureEntry can't wait on the fence on Android < 33") →
             veraltete/überlagerte Frames bleiben stehen, Onboarding/Home zeigen
             Video+Bild+Seiten übereinander, dazu "Handler on a dead thread"-
             Absturz. Skia rendert Video-Texturen korrekt und behält alle Clips.
             Rückgängig: dieses meta-data entfernen (Impeller ist Flutter-Default). -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- DSGVO/TTDSG §25: Analytics + Crashlytics sind OPT-IN. Erhebung ist
             hier ab Werk deaktiviert und wird erst nach expliziter
             Einwilligung zur Laufzeit aktiviert
             (PrivacyConsentService.setAnalytics/setCrash). -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <!-- M3: flutter_local_notifications — geplante Erinnerungen +
             Wiederherstellung nach Reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Phase 5 — image_cropper (UCrop). PFLICHT-Registrierung: ohne diese
             Activity hängt/crasht der Zuschneide-Schritt von „책 한 컷" nach
             dem Foto (Endlos-Spinner). Theme kommt aus androidx.appcompat
             (transitiv via image_cropper → ucrop). -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- AdMob ist in v1.0.0 nicht aktiv (Plugin in pubspec.yaml deaktiviert,
             ad_service.dart ist ein Stub). Bei Reaktivierung in v1.1+:
             entweder echte App-ID hier eintragen und google_mobile_ads in
             pubspec wieder enablen, oder Test-ID:
             ca-app-pub-3940256099942544~3347511713 verwenden.
             Aktuelle Begründung: "no ads" Data-Safety-Aussage soll konsistent
             sein — keine Ad-SDK-Spuren im Release-Build. -->
    </application>

    <!-- Internet wird für Firebase (Cloud-Sync, Crashlytics, Analytics,
         Remote Config) benötigt. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- M3: lokale Benachrichtigungen (tägliche Lern-Erinnerung). Kein FCM. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Ads are disabled in this release. Remove AD_ID even if a transitive
         dependency contributes it, so the packaged permission matches the
         no-ads Play Data Safety declaration. -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <!-- Privacy Sandbox(ACCESS_ADSERVICES_AD_ID)는 해당 SDK 도입 시 활성화 —
         현재는 전이 매니페스트 유입 차단 유지. -->
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />

    <!-- Phase 5 (stately-rising-jongga) — "책 한 컷" Snap-and-Learn.
         Kamera zum Fotografieren von Lehrbuchseiten; die Galerie verwendet
         den System-Picker ohne Medienberechtigung. OCR (ML Kit) läuft
         on-device, kein Cloud-Upload
         des Bildes; nur der extrahierte Text wird zum Analysieren versendet.
         iOS Pendant siehe ios/Runner/Info.plist. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
