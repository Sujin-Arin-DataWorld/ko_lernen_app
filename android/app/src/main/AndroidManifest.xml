<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Hangul Sori"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- DSGVO/TTDSG §25: Analytics + Crashlytics sind OPT-IN. Erhebung ist
             hier ab Werk deaktiviert und wird erst nach expliziter
             Einwilligung zur Laufzeit aktiviert
             (PrivacyConsentService.setAnalytics/setCrash). -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <!-- M3: flutter_local_notifications — geplante Erinnerungen +
             Wiederherstellung nach Reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Phase 5 — image_cropper (UCrop). PFLICHT-Registrierung: ohne diese
             Activity hängt/crasht der Zuschneide-Schritt von „책 한 컷" nach
             dem Foto (Endlos-Spinner). Theme kommt aus androidx.appcompat
             (transitiv via image_cropper → ucrop). -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- AdMob ist in v1.0.0 nicht aktiv (Plugin in pubspec.yaml deaktiviert,
             ad_service.dart ist ein Stub). Bei Reaktivierung in v1.1+:
             entweder echte App-ID hier eintragen und google_mobile_ads in
             pubspec wieder enablen, oder Test-ID:
             ca-app-pub-3940256099942544~3347511713 verwenden.
             Aktuelle Begründung: "no ads" Data-Safety-Aussage soll konsistent
             sein — keine Ad-SDK-Spuren im Release-Build. -->
    </application>

    <!-- Internet wird für Firebase (Cloud-Sync, Crashlytics, Analytics,
         Remote Config) benötigt. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- M3: lokale Benachrichtigungen (tägliche Lern-Erinnerung). Kein FCM. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Android 13+ requires this permission when the app uses Advertising ID
         via Firebase Analytics. No AdMob ads are included in v1.0.0. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- Phase 5 (stately-rising-jongga) — "책 한 컷" Snap-and-Learn.
         Kamera zum Fotografieren von Lehrbuchseiten + Galeriezugriff für
         vorhandene Fotos. OCR (ML Kit) läuft on-device, kein Cloud-Upload
         des Bildes; nur der extrahierte Text wird zum Analysieren versendet.
         iOS Pendant siehe ios/Runner/Info.plist. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Android 13+ (SDK 33+): granulare Medienberechtigung statt READ_EXTERNAL_STORAGE -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Fallback für ältere Android (≤ 12), max-sdk verhindert Doppel-Prompt -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
