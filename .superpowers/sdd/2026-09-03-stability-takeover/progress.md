# Stability release-integrity takeover — progress ledger

- Packages: P1 R1 (unknown scope, not this worktree) / P2 R2 (unknown scope, not this worktree) / P3 R3+R4 (tool integrity SHA/checksum verifier + Android symbol/AAB evidence) — this session covers P3 R3+R4 only.
- Branch: `codex/stability-release-integrity-20260903` in worktree `C:\dev\hangulsori\ko_lernen_app_worktrees\stability-release-integrity-20260903`, based on `08b80c6e` (origin/main).
- R3+R4 commit: `118b1933` — "tool(release): SHA-pinned toolchain verifier + fail-closed Android symbol/AAB evidence receipt (Codex R3+R4 takeover)" — adds `tool/release_integrity.py`, `tool/test_release_integrity.py`, `tool/release_toolchain.json`, `tool/android_release_evidence.py`, `tool/test_android_release_evidence.py`.
- CI wiring deferred until PR #256 merges — `.github/workflows/ci.yml` and `play_closed.yml` intentionally NOT touched this session.
- Graphify noise (`graphify-out/**`) discarded per worktree convention (`git checkout -- graphify-out`; `git clean -f -- graphify-out`) before committing; not part of the R3+R4 commit.
- Full test/CLI/smoke evidence, environment gaps (missing PyYAML/numpy/PIL in `.venv`), and open questions are recorded in `r3r4-report.md` next to this file.
R2 (web/WebKit): committed d725e65f + 9b874bdd; Fable direct read APPROVE (index.html deletion is automation-gated only; spec blocks all non-local requests, 30s boot budget, rAF/visibility preservation, pixel-contrast paint check; playwright.yml pins match release_toolchain.json; Flutter 3.44.0 == ci.yml). Pushed; draft PR #257 — GitHub Playwright workflow = final WebKit evidence.
R3+R4 (release tools): committed 118b1933 + 57146f10; Fable direct read APPROVE (R3: fail-closed manifest/binary/workflow checks, alias+merge-key rejection, token/depth caps, TOCTOU double-hash; R4: ELF offsets verified by hand, in-memory zip read (no extraction), argv SHA verified before+after run, shell=False, receipt lock + atomic write). Ruling: CI job "Asset pipeline gates" must install pyyaml==6.0.2 (19/27 R3 tests fail without it) — 1-line ci.yml change in this branch, no overlap with PR #256 hunks. Python reviewer (ecc:python-reviewer) pending; fix round after.
R1 (migration): committed d5cc12dd + 1a8e04e6; full suite on branch 5757/0/15, analyze 0. Fable direct read of the 853-line lib diff: FIX-REQUIRED — Important: backup snapshot + journal are written before `pending` is computed, so a no-step version bump serialises all `kl_` data and can lock writes forever on an odd value type (old fast path "도장만 옮긴다" lost) → restore stamp-only fast path under the new commit semantics + 2 tests. Minor: web numeric runtimeType in `_Snapshot.matches`; `resetCachesAfterExternalWrite` on every startup. Approved otherwise (reentrancy guard, lock at admission, native-reload commit reconciliation, all-or-nothing restore with verification, non-rollback cleanup). Design note for the follow-up before registering real steps: invalidBackup/invalidMetadata lock writes permanently with no user-facing recovery.
R2 evidence: PR #257 GitHub "Playwright Tests" run 33772332430 = success, 5m03s job, steps all green (Flutter 3.44.0 setup, node --test, browsers, release web build via webServer), log "Running 6 tests using 2 workers" … "[5/6] [webkit-phone]" "[6/6] [webkit-wide]" … "6 passed (1.6m)". CI workflow on the same head also green. Codex's local 6×2 pass independently reproduced on Linux. Remaining: real Safari device check (Jin), root cause of the earlier intermittent blank screen still unconfirmed.
R3+R4 fix round 1: committed ed422dba (pyyaml==6.0.2 added to ci.yml "Asset pipeline gates" pip-install; full type hints on release_integrity.py; android_release_evidence.py libapp.so read now bounded via bundle.open()+read(256MiB+1) instead of trusting getinfo().file_size, new regression test test_lying_zip_size_metadata_cannot_bypass_the_read_cap; archive_release FileExistsError branch now raises ... from None). R3 27/27 (throwaway venv), R4 44/44 (canonical venv), discover 181/0f/25e (25 pre-existing numpy/PIL import gaps, unrelated). git diff --check clean.
