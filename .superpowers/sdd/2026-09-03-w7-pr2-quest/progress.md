# SDD ledger — plan: docs/superpowers/plans/2026-09-03-w7-pr2-quest-engines.md
Worktree: C:\dev\hangulsori\ko_lernen_app_worktrees\w7-pr2-quest-20260903 (branch claude/w7-pr2-quest-20260903)
BASE: origin/main d120af87
T2.1 dispatched 2026-09-03
T2.1 Fix round 1 (Fable FIX-REQUIRED on be0a7062): entry autoplay removed from luecken/particlePop, post-reveal readback added to particlePop only (canonical-corpus gated, STEP 0) — 2026-09-04
T2.1 APPROVED by Fable direct read (be0a7062 + fix 48dc3247): dialog autoplay, quiz entry silent, particlePop post-reveal readback, luecken/uebersetzen readback blocked on canonical corpus (W9-C).
T2.2 dispatched 2026-09-04 — 정답 효과(burst+sound+haptic) 5엔진 통일 + batchim_drop TtsService→SoriSpeech 이관 (지시서 4.7). hoerverstehen/luecken/uebersetzen/particle_pop/diktat correct 분기에 widget.correctFeedback.play(context) 배선, 기존 HapticFeedback.*Impact() 제거; batchim_drop_quest.dart:161/474 TtsService→SoriSpeech; quest_engines_uiux_test.dart의 burst/sound/haptic 단언을 2엔진→7엔진으로 확장(RED→GREEN 확인) — analyze 0, git grep TtsService -- lib/screens/quest_engines/ 빈 결과.
T2.2 APPROVED by Fable direct read (99afc0bd): 5 engines use correctFeedback.play, batchim_drop unified to SoriSpeech, 7-engine triad test.
T2.3 dispatched 2026-09-04 — SoriGaps 8토큰 배선 (지시서 4.8/4.10). 신규 `test/sori_gaps_usage_guard_test.dart`(tokens.dart의 SoriGaps 클래스 본문을 정규식으로 파싱해 8토큰 이름을 뽑고, lib/ 밖 사용처 ≥1 확인 — RED→GREEN). optionGap(12): hoerverstehen/luecken/uebersetzen/particle_pop 4개 선택형 엔진의 옵션 타일 사이 SizedBox(전부 SoriAnswerTile 루프의 Spacing.sm, 8→12 시각 변경). questionToOptions(24): 같은 4엔진의 질문→옵션 간격(hoerverstehen/luecken/uebersetzen는 Spacing.lg, particle_pop은 원시 리터럴 24 — 값 일치라 무변경). 잔여 6토큰은 quest_flow.dart에 배선: chromeToContent(SoriPromptCard 아이콘→텍스트, 8→16), headingToBody(SoriPromptCard 문장→재생행, 4→8), cardGap(SoriAnswerTile 배지→라벨, 8→16), labelToField(ScenarioQuestAction 힌트→제출버튼, 8→8 무변경), paragraphGap(SoriAnswerTray 카드 패딩, 12→12 무변경), sectionGap(ScenarioQuestAction 피드백→계속버튼, 8→24). diktat_quest.dart의 브리프 인용 Spacing.sm 7곳(228/268/287/509/552/561/583류)은 실측 결과 전부 워드뱅크 칩 UI(Wrap spacing:Spacing.xs가 실제 타일 간격)로 "옵션 타일 사이" 간격이 아니어서 미변경 — 브리프 사실 오류로 판단, 아래 보고서에 근거 기재. satz_bauen_quest.dart 로컬 `sectionGap` 변수는 개명하되 브리프 지정값 `_compactGap` 대신 `compactGap`으로 변경(선행 언더스코어는 `no_leading_underscores_for_local_identifiers` 린트에 걸려 analyze 0 게이트와 충돌 — 최소 이탈, 값 불변). 검증: analyze 0(전체), 5개 지정 테스트 파일 개별 전부 GREEN(합 71개; flutter test에 5개를 한 번에 넘기면 셰이딩 버그로 2개 파일만 실행되는 러너 결함 관찰 — `--concurrency=1`로 71/71 재확인), git diff --check 클린. 커밋 4741408a.
T2.3 Fix round 1 (Fable FIX-REQUIRED on 4741408a): quest_flow.dart 잔여 6토큰 배선 6곳 전부 원복(4382a8e8과 바이트 동일) — 지시서의 "quest_flow.dart 어딘가에" 지시 자체가 결함이었다고 판정; optionGap/questionToOptions(4개 선택형 엔진)·satz_bauen compactGap·12dp 지오메트리 단언은 유지. sori_gaps_usage_guard_test.dart 재설계: optionGap/questionToOptions 필수 2토큰 + 나머지 6토큰은 knownUnadoptedGaps/knownUnadoptedCap down-only 래칫(auto_speech_test_stub_guard_test.dart 패턴 미러링). 5개 파일 개별 GREEN 76/76, analyze 0, optionGap 전량 제거 뮤테이션 FAIL→복원 재확인. 커밋 0f474698.
T2.3 APPROVED by Fable (4741408a + fix 0f474698): optionGap/questionToOptions wired in 4 engines, quest_flow untouched, guard = 2 required + 6 unadopted ratchet.
T2.4 dispatched 2026-09-04 — Diktat 한국어 뜻(promptKo, 지시서 4.13 · §9-3 룰링). 호스트: diktat_quest_test.dart(기존 diktat 하네스 보유 — quest_engines_uiux_test.dart는 7엔진 매트릭스 위주라 부적합). 사실 재검증: _promptDe/_promptEn L319-320→실측 L321-322, _meaning() L377-381→L379-384, 토글 L588-603→L587-606(구현 전) — T2.1-T2.3로 2줄 밀림, 브리프 결함 아님. `_promptKo` getter(`?.trim() ?? ''`) 추가, 의미 보기 토글을 `if (_completed)`로 게이팅(기존엔 항상 노출 — 계약 변경, §9-3 명시 지시), 열림 상태를 버튼 라벨 치환 대신 별도 Column(DE/EN 줄 + `_promptKo.isNotEmpty`일 때만 `t.diktatMeaningKo` 라벨+KO 줄)으로 재구성. arb `diktatMeaningKo` de "Auf Koreanisch"/en "In Korean" + `@` description 추가 후 `flutter gen-l10n`(생성 diff 20줄만). `validate_content.py`에 `_validate_diktat_prompt_ko` 신설(선택 필드; 있으면 비어있지 않아야 하고 targetKo와 strip 비교 동일 금지). TDD: diktat_quest_test.dart 신규 그룹 5건(RED 4건 실패→구현→테스트 버그 1건 수정→32/32 GREEN), test_validate_content.py 신규 케이스(RED 1건→구현→모듈 15/15 GREEN, test_current_repository_content_passes 포함 회귀 없음). 검증 개별 실행: diktat_quest_test/quest_engines_uiux_test/content_audio_policy_guard_test/arb_l10n_guard_test/arb_orphan_key_guard_test/auto_speech_test_stub_guard_test 전부 GREEN, analyze 0(20.2s), git diff --check 클린, 예상 밖 가드 실패 없음. 커밋 6c9cb310. 리포트 .superpowers/sdd/2026-09-03-w7-pr2-quest/task-4-report.md.
T2.4 APPROVED by Fable (6c9cb310): promptKo optional, completed-gated meaning, arb diktatMeaningKo, validator rule.
T2.5 dispatched 2026-09-04 — Silben SoriSpeech 이관(2.9 파생). targetScreens에 `lib/screens/silben_kreuz_screen.dart` 먼저 추가해 content_audio_policy_guard_test.dart RED 확인, 이어서 auto_speech_test_stub_guard_test.dart도 신규 offender(test/silben_grid_clue_sync_test.dart — SilbenKreuzScreen을 pumpWidget하면서 미스텁)로 RED임을 확인 — 지시대로 allowlist 상향 대신 그 파일 setUp에 `stubSoriSpeech()`를 추가해 GREEN 전환(R3 준수, 다른 4개 silben 펌핑 테스트 c0_level_selection/screen_smoke/standalone_games_uiux/study_activity_responsive는 이미 knownUnstubbedTestFiles 동결선에 있어 무관). 이후 silben_kreuz_screen.dart 소스 이관: import를 `../services/tts_service.dart`→`../widgets/sori/speakable.dart`(알파벳 순서 정렬 위치 준수), 완성 단어 발화 `TtsService.speak(`→`SoriSpeech.speak(`, dispose()에 `unawaited(SoriSpeech.stop());` 추가(dart:async는 이미 L1 import). git grep TtsService -- lib/screens/silben_kreuz_screen.dart 빈 결과 확인. 검증 개별 실행: content_audio_policy_guard_test(RED→GREEN)·auto_speech_test_stub_guard_test(RED→GREEN)·silben_grid_clue_sync_test·silben_puzzle_spoken_test·silben_puzzle_test 전부 GREEN, git grep TtsService -- lib/screens/quest_engines/ 빈 결과(T2.1/T2.2 완결 재확인), analyze 0, git diff --check 클린. 커밋 9ad48c9c.
T2.6 dispatched 2026-09-04 — 대사 카드 책갈피(1.24 파생, §9-2: 책갈피만·하트 제외). `_buildDialog`의 SoriCard 분기(내레이터 제외 — 내레이터 줄은 SoriSpeakable 단순 텍스트라 "카드"가 아님)에서 Column children 끝에 `Align(alignment: Alignment.centerRight, child: AddToWordbookButton(compact: true, korean: line.ko, translationDe: line.de, translationEn: line.en))` 추가. TDD: scenario_player_ui_test.dart에 stubSoriSpeech() 기반 신규 2건 — (a) '대사 카드 책갈피 탭은 quickAdd 1회'(find.byIcon(Icons.bookmark_add_outlined).first 탭 → CustomPackService.getById(quickPackId).words 중 line.ko 일치 정확히 1개, stub.spoken은 진입 자동재생분 그대로 불변) (b) '대사 카드 본문 탭은 여전히 발화를 1회 추가한다'(semantics 라벨로 카드 본문 탭 → stub.spoken에 항목 1개 추가) — 두 테스트 모두 소스 변경 전 RED(버튼 부재로 StateError/No element) 확인 후 구현, GREEN. AddToWordbookButton은 자체 IconButton(InkResponse)이 제스처 아레나를 먼저 점유해 SoriCard.onTap으로 전파되지 않음을 실측으로 확인(이론이 아니라 테스트 (a)의 stub.spoken 불변 단언이 직접 증거). 검증 개별 실행: scenario_player_ui_test.dart 전체 12/12 GREEN(회귀 없음), content_audio_policy_guard_test·auto_speech_test_stub_guard_test GREEN(scenario_player_screen.dart는 이미 두 가드의 대상/스텁 목록에 있어 변화 없음), accessibility_guideline_test.dart 55/55 GREEN — 단 그 매트릭스의 'scenario player vocabulary' 항목은 vocab 스테이지만 펌핑해 새 북마크 버튼이 사는 dialog 스테이지는 그 화면 매트릭스에 없음(기존 범위, 이 태스크로 넓히지 않음 — 아래 보고서에 기재). analyze 0, git diff --check 클린. 커밋 1db223ce.
