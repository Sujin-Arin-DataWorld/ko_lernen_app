# Hangul Sori: five tabs, first activity completion

Status: user-approved execution plan; rendered mockup review precedes production route wiring.
Baseline: origin/main 3b6d1ca1327d7429b6bf6f244aea8f61f09bc97d, verified 2026-09-14.
Authority: the user's final "한글소리 5탭 UIUX — 첫 학습 완료 중심 실행 계획" in this task.

## Global constraints

- Help a newcomer complete one existing real learning activity. Keep Heute, Lernen, Spiele, Hanok, Gye. Heute and Lernen must share the same goal and next activity.
- Main CTA directly opens the existing next activity with exact arguments; goal and estimated time appear before starting. Course/step overview is secondary.
- Preserve all 20 activity IDs, original illustration bytes and routes, mascot choice/hiding, Paperlogy body and Maru Buri title tokens, jade palette, current curriculum, game rules, rewards, privacy and age/account rules.
- Do not change mandatory onboarding, restore maps, invent building or community interactions, add API/Firestore schema, commit, push, merge or deploy.
- Superseded: default Learn hero=Wortpakete and promotion of recent Learn activity to hero. Course goal owns the Learn top.
- User steering during execution: Heute's Taego/Joy must use an active video, not a standing still image. Use the explicitly chosen CharacterClips.tigerSitting2 for Taego (supersedes tigerThinking); Joy keeps its existing walking clip. Preserve CharacterClipPlayer/video-lease lifecycle in a compact but readable companion area; retain selection/hide/reduced-motion/failure behavior. Provide an actual playable motion preview; PNG is only a still frame.
- Example progress, results and rewards in mockups must be visibly identified. Static design fixtures are not real account state, operational navigation, or proof of deployment.
- Complete the first-experience and entire five-tab mockup set and review them before wiring production app routes. Show the concrete boards to the user during work; the explicit implementation request authorizes subsequent implementation without a new permission gate.

## Task 1: Real-asset mockup set and reviewable preview

Work only at C:/dev/hangulsori/.worktrees/uiux-five-tabs-20260914. Read AGENTS.md's relevant active rules. Read Flutter widget-test and responsive-layout skills. Do not spawn helpers. Do not commit/push/merge/deploy. Other worktrees and source art must remain untouched.

Create a reproducible Flutter-rendered mockup set using original bundle art, the real bundled Paperlogy/Maru Buri fonts and existing theme tokens. Prefer isolated preview widgets under test/support/five_tabs_preview/ and a CAPTURE-gated renderer test; no production route wiring until the mockup set has been rendered and reviewed. Do not produce screenshots using AI-recreated art. New preview copy may live in a localized DE/EN fixture map outside product lib; production copy later must be ARB.

Own these files only: test/support/five_tabs_preview/**; test/five_tabs_design_preview_test.dart; the existing untracked lib/widgets/sori/activity_feature_card.dart only to remove its compile errors or move its draft to the preview support directory; lib/widgets/sori/collapsing_header.dart only if its old unused draft must be reverted; docs/UIUX_BIBLE_APPLICATION_EXECUTION_LOCK.md and its inventory test only if a new production Sori widget remains; new preview-specific tools as necessary. Do not touch production screen or service routes. Output images/fixture manifest/render evidence to C:/dev/hangulsori/_codex_artifacts/uiux-five-tabs-20260914/mockups/.

Deliver all:
1. First-experience board: first-visit Heute -> exact next actual activity -> activity completed -> return Heute. Select a real course unit, representative next link and existing content from the repository; derive actual counts, words and time rules, and report the source IDs/files. Clearly separate first activity completion from whole course competence, Hanok growth and reward. No invented XP. Preserve SoriStudyFrame's real question/answer/audio/exit hierarchy in the preview.
2. Five-tab comparison board with five first screens side by side. Each individually 390x844 logical pixels exported at 3x (1170x2532 PNG), DE default and EN support. Render boards with Flutter canvas/widgets; preserve whole 4:3 original images.
3. Full Learn and Games browsing boards showing all 20 correctly mapped activities: Learn course in the top goal region, other 11 in sections (words/sentences: vocab_packs,grammar,word_web; listen/speak: pronunciation,listening,scenarios,smalltalk; Hangul/write: hangul,calligraphy; review/my words: srs,my_words). Games order daily_game,chosung,syllable_cross,cloze,speed_match,sentence_arcade,kkeunmari,custom_practice, featured daily or recent from this tab only, featured not repeated. custom_practice -> /my_words. Use authoritative catalog for names, minutes, IDs and route manifest.
4. State/internal screen boards: activity explanation (same 4:3 art, real rules/reward conditions), learning and game results, Hanok quests/stamps/bojagi pending-opening-result, Gye empty/member/group-detail/join error, loading/offline/progress unavailable/image failure. No raw implementation jargon inside app flows; example captions outside phone frames or a discreet non-product fixture label.
5. Responsive evidence/checks for 360x640,390x844,430x932,tablet portrait/landscape in DE/EN at 100/160/200% text. Avoid text clipping, inner-card scroll, overlapping buttons or obscured controls. At 390 default, main CTA and first two-card row must be visible in Learn and Games. Show 2-column normal cards and one column at large phone text, preserve rail-adaptive concept. Long pages may scroll normally.

Composition:
- Heute: small greeting + companion, shared goal and start first, pending reward only when fixture actually has pending, Hanok summary and nearest quest. First-use guide compact next action with expandable full checklist. Same goal text and activity in Lernen.
- Lernen: goal/current course location/direct start, secondary course overview, section jump links and free exploration cards; recent learn only a secondary label, never changes goal.
- Spiele: compact title, featured original art + purpose/time/start/details, other 7 image cards. Titles and controls below/beside art; no fake progress badges or XP decoration.
- Hanok: learning summary, full current ildu_v3_preview image, readable updating state below it, clear quests/stamps/bojagi. No map controls or invented completion.
- Gye: empty state short explanation/current actual courtyard preview/create or join/continue alone; member state name/count/weekly goal above art; existing learning/community actions. No long intro stepper pushing primary action down.
- Featured art ~128x96 at 390, 4:3 contain. Normal card image 4:3 contain with name/time/verified or neutral availability below. Visible details and long-press same explanation; avoid simultaneous start. Side20, gap12, section24, h1 26/body16/secondary15. No repetitive pulse or pill decoration.

The old draft lib/widgets/sori/activity_feature_card.dart has unresolved ARB keys and entry.description.resolve (not a real API). It is not wired anywhere. Preserve its useful idea by moving/refactoring into preview support rather than adding speculative product API strings just to compile. The old compact header patch is unused; keep it only if compile-safe and justified, or revert this task-owned draft. Do not erase unrelated changes.

Verification: source hashes remain exact, catalog manifest remains 12+8; real font load; no Flutter exceptions at target sizes; visible CTA+first row at default390. Test live preview layout, not a test mirroring constants. Inspect rendered PNGs with view_image. Return exact render command, test count, output paths, content provenance, production wiring status and limitations in report. Do not claim automated tests are beginner observation or physical device QA.

## Task 2: Shared learning focus and direct activity journey (after rendered mockup review)

Create a read-only presentation model consuming TodayLearningSnapshot and CourseMissionBrief, owned/refreshed by the shell and shared by Heute/Lernen. Carry goal, exact next destination/typed arguments, minutes, step brief and availability. No second recommender. Course uses directDestinationForCourseLink; vocabulary resolves containing pack and preserves CoursePracticeContext. Failed resolution gives reason/retry without unrelated fallback. Ancillary Gye/reward queries do not delay activity start. Retain existing allowed review path when recommendation unavailable. Propagate failed progress reads distinctly from loaded zero.

Preserve direct launch post-attempt companion behavior, actual learning event ownership, course evidence and completion conditions. Separate completed vs abandoned activity, return to origin tab/scroll, refresh all summaries. Deduplicate receipts already displayed by native result using actual receipts, never reward promises.

Test identical focus across tabs, precise routes/args, failures, multi-tap protection, post-attempt behavior, completion vs exit, origin return and reward deduplication.

### Task 2 implementation brief and verified seams

Work in the same isolated worktree only. No commits, pushes, deployments, extra agents or changed curriculum/game/award rules. Read `.superpowers/sdd/2026-09-14-five-tabs-first-completion/journey-preflight.md` before designing return/outcome integration. The report is source-audited guidance, not implemented code. New production Sori widgets require the inventory update; new UI strings require DE/EN ARB and generated l10n. Do not modify the completed test-only mockup files or their artifacts.

Scope: implement the shared focus, shell ownership, exact direct start, honest availability, journey lifecycle and native result/receipt connection. Task 3 handles the broader visual composition, catalog grouping/history and Hanok/Gye UI. Minimal shared-focus display on Heute and Lernen is required now so its real behavior can be exercised. Reuse the approved mockup layout/tokens when adding that widget; expose the focus/controller to Task 3 through documented, typed APIs in your task report. Existing screens and widgets not involved in those connections keep their current visual behavior until Task 3.

Latest steering priority: while composing the minimal Heute focus surface, also make the native companion header compact and actively play Taego CharacterClips.tigerSitting2 (user explicitly corrected the gaze/pose) and Joy walking through CharacterClipPlayer. Keep its lifecycle, choice/hide, actual failure handling and matte contract. Existing Xiaomi-specific eligibility intentionally does not equate MediaQuery.disableAnimations with unsupported video; preserve that policy. Do not introduce a separate decoder or alter original MP4 bytes. Task 3 should consume this compact header, not redo it.

1. Build a local read-only focus model/controller around TodayLearningSnapshot/CourseMissionBrief. The shell owns one latest result and refresh generation shared by Heute and Lernen. Start a fresh request only when needed; discard stale refresh/disposal/account-lifetime responses. Preserve healthy empty history versus loading/failed reads. Do not independently change recommendation priority. Do not wait for aggregate Gye/reward/quest reads before showing a ready focus. Existing standalone screen test seams may use injected focus loaders. Production must use shared shell ownership.
2. Resolve the exact next CourseMissionBrief.firstLink via directDestinationForCourseLink; use the original typed CoursePracticeContext and containing vocabulary pack. Root's runtime probe for first A1 unit is recorded in external `first-activity-source.json`: vocab_a1_0001 -> a1_greetings_1, 9 learning words/7 quiz/2 boss. Do not hardcode this as every learner's next activity. Use catalog activity minutes for an entire opened activity; existing course phase estimates are for the brief, not a promise that a whole vocabulary pack takes one minute. No `continue` based solely on CoursePick.started or last opened activity.
3. Keep course overview secondary and exact existing Pack/Review/Scenario routes and arguments. Resolve failure shows typed user-readable reason/retry; no arbitrary library fallback. Preserve existing permitted saved review behavior when Today is unavailable. Add tests proving same object/goal and exact arguments from both tabs and blocked incomplete recommendation.
4. Route all Today/catalog starts through a small root-Navigator journey owner tracking the captured origin Route object. Register an observer beside existing observers without changing media RouteAware behavior. Cover push, replacement, retry, child hubs/sheets, pop/remove, cancellation and disposal. Await actual return to the origin, not the first pushed route Future. Keep shell tab/scroll controllers alive. An intentional Home escape inside an owned journey returns to that origin after existing confirmation/cleanup; outside a journey preserve existing Home behavior. Guard duplicate launches before async resolution. Test with real Navigator operations and replacements, not mocked Future-only navigation.
5. Extract/reuse CourseMissionScreen's before/after evidence and OnboardingCompanionService invitation sequence after real return for direct course launch. Preserve the existing qualifying kinds and evidence predicates; no new success gate, duplicate companion invocation or parent lessonStarted event. Keep native actual lesson/quiz event ownership. Refresh focus and the other tab summaries on return even if abandoned or measurement failed.
6. Record existing activity terminal outcomes at their actual existing finish/persistence boundaries. Hubs, feedback allocation and XP deltas are not completion. Distinguish completed attempt, pass/fail where already meaningful, abandoned and save failure. Finishing one round then abandoning a retry must retain the earlier completed attempt. Existing unawaited reward writes may be awaited/tracked for truthful display, never granted twice. All 20 catalog families are mapped in preflight; library-only actions remain library-only. No new competence or reward conditions.
7. Native result presentation reports only actual persisted reward items that were visibly displayed, keyed by local attempt identity. Subtract only matching shown items/amounts from measured shell receipts, capped at measured increase. No global suppression by activity ID or screen name. Keep unshown Bojagi/quest/Gye/XP items. GameOutcome is a useful existing persisted outcome seam, but a recordGameResult call is not proof its result was shown. Handle delayed result arrival and duplicate builds. Scenario result does not show XP, so keep its unshown XP receipt. Vocab result must receive actual successful coordinator XP (rather than approximate count) before acknowledging it. No new server/Firestore receipt schema.
8. Propagate Hanok/course progress read failure to presentation/receipt layer rather than converting it to empty/zero. Preserve legacy read API where needed with an explicit new read result for honest surfaces; avoid silently breaking unrelated callers. Untracked activity progress is neutral available, not new/complete. Game best is not a completion rate. Verify unknown-before/after values never produce fake grants.

Tests: meaningful new focus/return/outcome/dedup tests plus impacted course mission/companion, Today availability, shell, catalog reward flow, home/study-frame navigation, vocab finish/result, GameOverCard/outcome and collection projection tests. Use existing fixtures/seams and actual native result paths where practical. Run static analysis for edited files and generated localization. Iterate targeted tests; run the broader relevant group once after stable changes. Report exact commands/results, changed files, public interfaces for Task 3 and remaining concerns. Do not claim a test-only mockup or green source tests are production deployment or physical-device/beginner QA.

## Task 3: Catalog, history and five-tab production composition (after Task 2)

Connect approved preview composition to production widgets. One source catalog, no global change to unrelated pack/listening cards. Add localized DE/EN copy, accessibility semantics and visible detail button with independent gesture. Split recent local keys by Learn/Games; migrate legacy only to matching catalog tab without overriding newer value, include reset/account deletion and lifetime-race protection. Learn recency is secondary only. Unmeasured progress is neutral available; failures are unknown, not Neu/0/completed. Game best is not completion rate.

Apply compact Hoje/Heute companion settings, guide expansion, real Hanok summaries/current-preview status below art, existing collections, Gye membership-specific priorities and current privacy/error states. Register any new Sori production widget in UIUX inventory.

### Task 3 implementation brief and verified seams

Read `composition-preflight.md` in this plan's SDD workspace and Task 2's final report/interfaces. Keep the latest sitting2 compact companion implemented by Task 2. Read the approved test-only preview and actual rendered boards as visual references; do not copy fixture account state or build a second activity catalog into production.

- Replace catalog-only cards/grid and catalog activity sheet with full 4:3 art, natural content height, visible independent details/start semantics, and two columns at normal phone scale / one at large text. Keep shared pack/listening card defaults and measurement cache unchanged. Use one catalog-adjacent grouping definition for the four Learn groups; Course remains represented in the top overview. Games hero uses only validated Games history and is excluded from the original-order grid. Remove hero pulse and false continue copy.
- Replace oversized root slogans with common compact tab headers using existing h1/body/secondary tokens, preserving profile/cultural help access, adaptive rail and primary scroll controller. Today and Learn use the Task 2 shared focus/direct start. Today guide shows one next existing action plus expandable checklist; pending rewards are conditional, then honest Hanok and next quest. Preserve all guide persistence, dismissal, routes and errors.
- Implement per-tab local history with precise catalog membership, safe legacy migration, newer-write wins, local-data lifetime and account-session guards. Include normal/strict reset and account deletion; prove a delayed submitted platform write cannot resurrect history after reset. No new cloud schema or broad storage redesign.
- Hanok root: honest confirmed learning summary; complete original current preview; updating message below image; clear Quests/Stamps/Bojagi. Avoid changing shared HanokV3Preview defaults in unrelated onboarding/path consumers. Keep old retired-map gate enabled. Collection screens retain actual quest destinations, earned/unearned stamps, pending/opening/claimed Bojagi states; align their readable headers/status/actions without changing reward persistence.
- Gye root: one scrolling surface, empty short intro/current courtyard/create+join+continue alone; member groups lead with name/count/weekly goal. Group detail orders existing weekly goal and contributing learning before anonymous progress/courtyard/support/members. Existing create/join forms use consistent validation/loading/success/error hierarchy and duplicate-submit protection while preserving age/account/privacy/report/block behavior. No discovery or chat additions.
- Native result presentation: preserve existing SoriStudyFrame and question/audio/feedback behavior. Align result content, actual persisted rewards and clear return-to-origin action using Task 2 journey interface. Do not create competence from practice completion, show a promised XP formula before persistence, or remove existing cleanup/exit confirmation.
- Intentional test updates replace superseded assumptions (old Learn vocab/recent hero, three old sections, pinned shrinking map, Gye stepper-first layout). Keep useful acceptance checks: all20IDs/art/routes, no crop, no inner scrolling or text clipping, accessible independent actions, default390 CTA+first grid row, all target locales/scales/rail sizes. Register every new production Sori widget and regenerate localization.

Run focused meaningful tests per subsystem, static analysis, then the relevant broader group once stable. Report exact files/interfaces/tests and remaining concerns; no commits/push/merge/deploy.

## Task 4: Integrated verification and evidence

Run relevant course navigation/catalog/responsiveness/storage/rewards/Hanok/Gye tests and static analysis. Render approved production roots with real fonts and root evidence command from AGENTS.md. Check DE/EN, all viewport/text sizes and error states from Task 1, asset hashes, gesture separation, exact direct start, origin return and no duplicate rewards. Use existing consented actual lesson/quiz events; exclude course brief opens from first activity start. Report physical device/beginner observations separately as pending unless actually performed. Four-tab alternative is out of scope. End with graphify update and safe canonical prune; no commit or deployment.
