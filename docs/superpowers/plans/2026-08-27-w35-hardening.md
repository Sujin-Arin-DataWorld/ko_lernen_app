# W3.5 하드닝 웨이브 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Jin의 "소리 안 나와"/"진행이 안 저장돼" 신고의 실제 원인인, W1-W3 이전부터 있던 침묵 실패(silent-failure) 9건 + 접근성 결함 3건을 최소 diff로 고치고 회귀 가드 테스트로 고정한다.

**Architecture:** 이미 존재하는 방어 패턴(그림자 존 있음: `PrivacyConsentController`, `sori_stage_reward_receipt_service.dart`의 `unawaited(f.then(...))`, `AppError`/`onRetry` 화면 2곳, `TtsPlaybackEngine`의 생성자 DI)을 재사용해 나머지 지점에 동일 관용구를 복제한다. 새 서브시스템은 만들지 않는다 — 이건 하드닝이지 리팩터가 아니다.

**Tech Stack:** Flutter/Dart (flutter_test), 기존 `@visibleForTesting` 훅 패턴

**Spec:** 지시서 audit 결과 (ecc:silent-failure-hunter 10건, ecc:a11y-architect 3건) — 이 문서 하단 "감사 결과 재검증" 참고. 마스터 플랜 문서 없음(하드닝 웨이브는 지시서 항목이 아니라 감사 파생).

## ⚠️ 감사 결과 재검증 — 원문과 다른 점 (반드시 읽을 것)

감사는 2026-08-27 이전 어느 시점의 `main` 스냅샷에 대한 것이었고, 지금 이 워크트리(`fix/w35-hardening`, `main`@`51c8fffb` 기준)의 **실제 코드를 다시 읽어 확인한 결과 두 항목이 원문과 다르다**:

- **finding 7 (전역 에러 훅)**: "NO global error hook"는 **틀렸다**. `lib/services/privacy_consent_service.dart`에 `FlutterError.onError`/`PlatformDispatcher.instance.onError`를 설치하는 완전히 동의 게이팅된 `PrivacyConsentController`가 이미 있고 (consent 체크 → Crashlytics 포워딩 → 실패 시 graceful fallback, 전부 구현됨), `installErrorHandlers()`가 `main.dart`에서 실제로 호출된다. **진짜 결함은 설치 시점**: `_initFirebase()` 안에서 `Firebase.initializeApp()` 성공 **이후에만**, 즉 `runApp()`으로 첫 프레임이 뜨고도 한참 뒤(비동기 대기열 맨 끝)에야 설치되고, Firebase 초기화 자체가 실패하면(오프라인, 설정 누락, `google-services.json` 없음 — `_initFirebase()`의 `catch (_)`가 조용히 삼킨다) **세션 전체에서 단 한 번도 설치되지 않는다.** Task 1은 "훅을 새로 만드는" 게 아니라 "이미 잘 만들어진 훅의 설치 시점을 앞당기고 무조건화"하는 것이다.
- **finding 8 (SoriPressable 키보드 활성화)**: "ZERO onKeyEvent" / "zero sendKeyEvent in test/"도 **틀렸다**. `lib/widgets/sori/pressable.dart:148-158`에 이미 `Focus(onKeyEvent: ...)`로 Enter/Space→`_onTap()` 배선이 있고, `test/sori_pressable_keyboard_test.dart`에 `sendKeyEvent(LogicalKeyboardKey.enter/space)` 회귀 테스트가 이미 있어 통과 중이다(git blame: `260dc9d6`). **진짜 남은 결함**은 다르다 — `onKeyEvent`가 `widget.onTap == null`이면 무조건 무시하는데, `enabled = onTap != null || onLongPress != null`이 focus 가능 여부를 결정하므로 **`onLongPress`만 있고 `onTap`이 없는 위젯은 Tab 으로 포커스는 잡히지만 Enter/Space 를 눌러도 아무 일도 안 일어나는 "포커스 트랩"이 된다.** Task 8은 이 좁은 잔여 결함만 고친다.

두 항목 다 "고쳐야 할 게 없어졌다"가 아니라 "고칠 지점이 원문과 다르다" — 코드는 재검증한 그대로 옮겼다.

## Global Constraints

- **워크트리/브랜치**: 작업은 오직 `C:\dev\hangulsori\ko_lernen_app_w35`에서만, 브랜치 `fix/w35-hardening`(이미 체크아웃됨, `main`에서 분기, base `51c8fffb`). 형제 워크트리(`ko_lernen_app`, `_w2`, `_w3`, `_w4`)는 **절대 건드리지 않는다** — 커밋도, 파일도, `cd`도 없다.
- **커밋**: 태스크당 1커밋. 커밋 메시지 푸터: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. 서브에이전트 금지 — 각 태스크는 이 세션(또는 이 플랜을 실행하는 세션)이 직접 수행한다.
- **각 태스크 종료 시 `flutter analyze` 신규 이슈 0.**
- **계약 고정 테스트 — 절대 수정 금지** (그린 유지만): `test/learn_session_queue_test.dart`, `test/course_mastery_test.dart` (+ `course_mastery_sync_test.dart`, `course_mastery_production_contract_test.dart`), `test/audio_policy_guard_test.dart` (+ `content_audio_policy_guard_test.dart`), `test/game_surface_contract_test.dart`. 이 플랜의 어떤 태스크도 이 파일들을 열지 않는다 — 실패하면 태스크 코드가 잘못된 것이지 계약을 고치는 게 아니다.
- **래칫은 오르면 안 된다** (실측 후 재확인 없이 숫자를 올리지 않는다):
  - `test/spacing_literal_guard_test.dart` — `lib/` 그리드 밖(0/4/8/12/16/24/32/48 제외) 간격 리터럴 상한 **181**.
  - `test/typography_guard_test.dart` — `lib/screens/` 원시 `TextStyle(` 안 `fontSize:` 리터럴 상한 **115**.
  - `test/chrome_stack_guard_test.dart` — `lib/` 원시 `InkWell(` 상한 **19**; `chipWrapAllowlist`(5개 파일·고정 개수) 신규 위반 화면 추가 금지.
  - `test/uiux_bible_closeout_inventory_test.dart` — `lib/widgets/sori/` Dart 파일 개수 **정확히 133**, `docs/UIUX_BIBLE_APPLICATION_EXECUTION_LOCK.md` §8 표와 1:1 대응. 이 플랜의 어떤 태스크도 `lib/widgets/sori/`에 파일을 추가·삭제하지 않는다(전부 기존 파일 편집) — 만약 추가가 불가피해지면 그 표도 같은 커밋에서 갱신해야 한다.
- **ARB**: 신규 사용자 문자열은 `lib/l10n/app_de.arb` + `lib/l10n/app_en.arb` **동시** 추가 후 `flutter gen-l10n` 실행.
- **"의도된 swallow" — 절대 "고치지" 않는다** (아래는 침묵 실패가 아니라 문서화된 fail-open/best-effort 설계다. 이 플랜의 어떤 태스크도 아래 catch 블록에 로깅·재throw·에러 표면화를 추가하지 않는다):
  - `lib/services/sori_stage_reward_receipt_service.dart` — `capture()`의 두 fail-open catch: `catch (_) { await openActivity(); return null; }` (~:47-50, "before" 스냅샷 실패) 와 `catch (_) { return null; }` (~:74-76, "after" 스냅샷 실패). 클래스 독스트링이 명시: "이 fail-open 경계가 진단/보상 표면을 자격 게이트로 만들지 않는다."
  - `lib/services/tts_service.dart` — `prefetch()`의 `catch (_) { _prefetchAttempted.remove(key); }` (~:618-623, 문서화된 "실패는 전부 삼킨다"); `clearCache()`의 `catch (_) { // best effort }` (~:956-960 — **`clearCacheStrict()`는 그대로 전파해야 하므로 건드리지 않는다**, 계정 삭제 경로가 그 전파에 의존); `_ensureSpeechAudioContext()`의 `catch (_) { // best-effort — 다음 발화에서 다시 시도 }` (~:1021-1023).
  - `lib/services/audio_policy.dart` — `applyPlatformAudioContext()`의 `catch (_) { // best-effort — 플랫폼 미지원 시 기존 기본값 }` (~:246-248).
- **`~:줄번호` 표기는 `main`@`51c8fffb` 기준 재실측치**다. 같은 파일을 건드리는 앞 태스크가 이미 반영된 뒤라면 몇 줄 밀려 있을 수 있다 — 반드시 함수/심볼명으로 재확인 후 편집한다(라인 넘버만 믿고 패치하지 않는다).
- **각 태스크는 독립적으로 리뷰 가능해야 한다.** 같은 파일을 두 태스크가 건드리는 경우(vocab_pack_screen.dart — Task 3/7, review_session_screen.dart — Task 4/5)는 서로 다른 함수/영역이라 diff가 겹치지 않지만, **적힌 순서대로** 실행한다(Task 5는 Task 4가 만드는 테스트 파일을 확장한다).

---

### Task 1: 전역 에러 훅 조기·무조건 설치 (finding 7)

**Files:**
- Modify: `lib/main.dart:133` (`WidgetsFlutterBinding.ensureInitialized()` 직후에 삽입)
- Modify: `lib/main.dart:437-457` (`_initFirebase()` — 이제 중복인 호출 제거)
- Test: `test/main_error_hook_test.dart` (create)

**Interfaces:**
- Consumes: `lib/services/privacy_consent_service.dart`의 `PrivacyConsentService.installErrorHandlers()` (기존 public 정적 메서드, 시그니처 불변).
- Produces: Firebase 성공 여부와 무관하게, 앱 세션 전체(첫 프레임 이전부터)에서 `FlutterError.onError`/`PlatformDispatcher.instance.onError`가 항상 설치돼 있다는 보장. Task 3/5가 다루는 미청취 비동기 에러가 (동의가 켜져 있다면) Crashlytics 로 실제 도달하게 만든다.

- [ ] **Step 1: 실패하는 테스트 작성** — `launchKoLernenApp`의 `startProduction` 주입 시임(main.dart:128-145, `@visibleForTesting`)은 `_startProductionApplication()`(그리고 그 안의 `_initFirebase()`)을 완전히 건너뛴다. 지금 코드에서는 `installErrorHandlers()`가 오직 `_initFirebase()` 안에만 있으므로, 이 주입 경로로는 훅이 설치되지 않는다 — 그게 바로 이 테스트가 잡아야 할 결함이다:

```dart
import 'package:flutter/foundation.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:ko_lernen_app/main.dart' as app;

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  testWidgets(
    '전역 에러 훅은 Firebase/스플래시 없이도 조기·무조건 설치된다 (finding 7)',
    (tester) async {
      final originalOnError = FlutterError.onError;
      final originalPlatformOnError = PlatformDispatcher.instance.onError;
      addTearDown(() {
        FlutterError.onError = originalOnError;
        PlatformDispatcher.instance.onError = originalPlatformOnError;
      });

      var started = false;
      // startProduction 을 주입하면 _startProductionApplication() 전체
      // (Storage.init, Firebase, _initFirebase, installErrorHandlers 포함)를
      // 건너뛴다 — 그런데도 훅이 설치돼 있어야 "조기·무조건"이 참이다.
      await app.launchKoLernenApp(
        runApplication: (_) {},
        startProduction: () async {
          started = true;
        },
      );

      expect(started, isTrue, reason: '테스트 시임 자체가 안 불렸다');
      expect(
        FlutterError.onError,
        isNot(same(originalOnError)),
        reason:
            'installErrorHandlers() 가 _initFirebase() 안에만 있으면 '
            'Firebase 를 건드리지 않는 이 경로에선 훅이 설치되지 않는다',
      );
      expect(
        PlatformDispatcher.instance.onError,
        isNot(same(originalPlatformOnError)),
      );
    },
  );
}
```

- [ ] **Step 2: 실행해 실패 확인** — `flutter test test/main_error_hook_test.dart`. 예상: 두 `isNot(same(...))` 단언 중 최소 하나(보통 둘 다) 실패 — 현재는 `installErrorHandlers()`가 이 경로에서 전혀 안 불린다.
- [ ] **Step 3: 구현 — 설치 지점 이동.** `lib/main.dart`에 `import 'services/privacy_consent_service.dart';`가 없으면 추가(이미 `_initFirebase()`에서 쓰고 있으므로 십중팔구 이미 있다). `launchKoLernenApp`(133행 `WidgetsFlutterBinding.ensureInitialized();` 직후, `featureGate.isEnabled` 분기보다 먼저)에 추가:

```dart
  WidgetsFlutterBinding.ensureInitialized();
  // finding 7: Firebase 초기화 성공 여부·스플래시 진행과 완전히 독립적으로
  // 가장 먼저 설치한다. PrivacyConsentController.handleFlutterError/
  // handlePlatformError 는 이미 crashClient 호출 실패를 자체 try/catch 로
  // 감싸므로(Firebase 가 아직 없어도) 여기서 거는 게 안전하다 — 최악의
  // 경우 리포팅만 스킵되고 조용히 삼켜지진 않는다(debugPrint 남음).
  PrivacyConsentService.installErrorHandlers();
  final runner = runApplication ?? runApp;
```

  이어서 `_initFirebase()`(~:437-457)에서 이제 중복인 호출을 제거:

```dart
Future<bool> _initFirebase() async {
  try {
    await Firebase.initializeApp(
      options: DefaultFirebaseOptions.currentPlatform,
    );

    // DSGVO/TTDSG: Analytics + Crashlytics sind opt-in. Die Erhebung ist im
    // Manifest/Info.plist deaktiviert; hier wird die gespeicherte
    // Einwilligung (Default: aus) auf die SDKs angewendet.
    await PrivacyConsentService.applyStored();
    // installErrorHandlers() 는 이제 launchKoLernenApp() 맨 앞에서 조기·
    // 무조건 설치된다(finding 7) — 여기 있던 호출은 Firebase 성공에
    // 종속된 중복이라 제거.
    // Non-PII Segmentierungs-Properties setzen (no-op ohne Einwilligung).
    unawaited(Analytics.syncUserProperties());
    return true;
  } catch (_) {
    // google-services.json fehlt → Cloud-Sync deaktiviert, lokale App funktioniert weiter
    // ignore: avoid_print
    debugPrint('Firebase init skipped.');
    return false;
  }
}
```

- [ ] **Step 4: 실행해 통과 확인** — `flutter test test/main_error_hook_test.dart`.
- [ ] **Step 5: 회귀 확인** — `flutter test test/privacy_consent_service_test.dart test/ux_preview_app_test.dart` (이 이동이 건드리는 인접 경로). `flutter analyze` 신규 이슈 0.
- [ ] **Step 6: 커밋** — `git commit -m "fix(startup): 전역 에러 훅을 Firebase 성공에 앞서 조기·무조건 설치 (finding 7 — 감사 원문 재검증: 훅 자체는 이미 있었음)"`

---

### Task 2: TTS 해석 실패가 폴백을 건너뛰고 조용히 사라짐 (finding 1)

**Files:**
- Modify: `lib/services/tts_service.dart:744-761` (로컬 캐시 1단 — 예외 폭 확대)
- Modify: `lib/services/tts_service.dart:315-326` (`TtsPlaybackEngine.speak`의 `resolution.then(onError:)`)
- Modify: `lib/services/tts_service.dart:501-506` (`_playbackEngine` 조립부 — `errorReporter` 배선)
- Test: `test/tts_premium_only_test.dart` (extend — 기존 `group('프리미엄이 없으면 — 무음이되 조용하지 않다', ...)`에 추가)

**Interfaces:**
- Consumes: `TtsPlaybackEngine` 생성자 DI(`resolveAudio`/`platform`/`errorReporter`) — 기존, 시그니처 불변.
- Produces: (a) 로컬 캐시 I/O가 `TimeoutException` 외의 예외(`FileSystemException` 등)로 실패해도 `_resolveAudio`가 throw 없이 Storage(2단)로 넘어간다. (b) `TtsPlaybackEngine.speak`는 `TtsSynthesisBlocked`가 아닌 해석 실패도 `errorReporter`로 보고한다. (c) 아직 아무 티어도 구체적 사유(`unavailable`)를 안 채운 새 예외 종류가 나타나면 `TtsService.unavailable`이 최소한 `offline`으로라도 채워져 배너가 뜬다.

- [ ] **Step 1: 실패하는 테스트 2개 작성** — `test/tts_premium_only_test.dart`에 추가(파일 맨 위 `import 'dart:io';`는 이미 있음):

  (a) 소스 패턴 테스트 — 이 파일은 이미 같은 스타일(19-48행, `File(...).readAsStringSync()` 기반 회귀 가드)을 쓰고 있다. `main()` 최상위에 추가:

```dart
  test('로컬 캐시 읽기 실패는 TimeoutException 외의 예외도 잡아 Storage 로 넘어간다 (finding 1a)', () {
    final source = File('lib/services/tts_service.dart').readAsStringSync();
    final tierOneStart = source.indexOf('// 1. 로컬 캐시');
    final tierTwoStart = source.indexOf('// 2. Firebase Storage', tierOneStart);
    expect(tierOneStart, greaterThanOrEqualTo(0));
    expect(tierTwoStart, greaterThan(tierOneStart));
    final tierOneBlock = source.substring(tierOneStart, tierTwoStart);

    expect(
      tierOneBlock,
      contains('on TimeoutException'),
      reason: '느린 디스크는 여전히 시한으로 잡아야 한다',
    );
    expect(
      RegExp(r'\}\s*catch\s*\(_\)\s*\{').hasMatch(tierOneBlock),
      isTrue,
      reason:
          'TimeoutException 전용 catch 뒤에 일반 catch(_) 가 없으면 '
          'FileSystemException 등이 _resolveAudio 전체를 throw 해 '
          'Storage/CF 폴백을 건너뛴다 (finding 1a)',
    );
  });
```

  (b) `TtsPlaybackEngine` 행위 테스트 — 기존 `group('프리미엄이 없으면 — 무음이되 조용하지 않다', () { ... })` 안, "차단된 합성의 사유가 그대로 전달된다" 테스트 바로 뒤에 추가(같은 그룹의 `_RecordingPlatform` 재사용):

```dart
    test('TtsSynthesisBlocked 가 아닌 해석 실패도 사유가 보고된다 (finding 1b)', () async {
      final errors = <String>[];
      final engine = TtsPlaybackEngine(
        resolveAudio: (text, voice) async =>
            throw const FormatException('해석 중 예기치 못한 오류'),
        platform: const _RecordingPlatform(),
        errorReporter: errors.add,
      );

      expect(
        await engine.speak(text: 'x', voice: 'female', baseRate: 0.42),
        isFalse,
      );
      expect(
        errors,
        isNotEmpty,
        reason:
            '지금은 error is TtsSynthesisBlocked 일 때만 errorReporter 가 '
            '불려서, 그 외 예외는 lastError/unavailable 을 갱신하지 않고 '
            '사라진다 (finding 1b) — 사용자는 이유 없는 무음만 본다',
      );
    });
```

- [ ] **Step 2: 실행해 실패 확인** — `flutter test test/tts_premium_only_test.dart`. (a)는 현재 tier-1 블록에 두 번째 `catch (_)`가 없어 FAIL. (b)는 `errors`가 빈 채로 남아 FAIL.
- [ ] **Step 3: 구현 (a) — 로컬 캐시 catch 확대.** `lib/services/tts_service.dart` ~:744-761:

```dart
    // 1. 로컬 캐시. 멈춘 파일시스템이 speak() 를 붙잡지 못하게 시한을 건다.
    if (file != null) {
      try {
        if (await file.exists().timeout(_diskTimeout)) {
          final localBytes = await file.readAsBytes().timeout(_diskTimeout);
          if (TtsCacheKey.isUsableAudio(localBytes)) {
            return TtsAudio.path(file.path);
          }
          try {
            await file.delete();
          } catch (_) {
            // Never return this file. The next lookup still rejects junk bytes.
          }
        }
      } on TimeoutException {
        // 디스크가 막혔다 — Storage 로 넘어간다.
      } catch (_) {
        // FileSystemException 등 그 외 I/O 실패(권한·손상 매체 등) —
        // 여기서 던지면 _resolveAudio 전체가 throw 해 Storage/CF 폴백을
        // 건너뛴다(finding 1a). Storage 로 넘어간다.
      }
    } else {
```

- [ ] **Step 4: 구현 (b) — `TtsPlaybackEngine.speak`의 onError 확대.** ~:315-326:

```dart
    final resolution =
        Future<TtsAudio?>.sync(
          () => resolveAudio(trimmed, normalizedVoice),
        ).then<_TtsResolution>(
          (audio) => _TtsResolution(audio: audio),
          onError: (Object error, _) {
            // TtsSynthesisBlocked 는 사유 문자열이 이미 사람이 읽을 말이다.
            // 그 외 예외(finding 1b — 예전엔 여기서 조용히 버려졌다)도
            // errorReporter 로 보내야 lastError/unavailable 이 갱신된다.
            errorReporter?.call(
              error is TtsSynthesisBlocked
                  ? error.message
                  : 'TTS resolution failed: $error',
            );
            return const _TtsResolution(audio: null);
          },
        );
```

- [ ] **Step 5: 구현 (c) — 조립부에서 `unavailable` 폴백.** ~:501-506, `_playbackEngine` 정적 필드:

```dart
  static final TtsPlaybackEngine _playbackEngine = TtsPlaybackEngine(
    resolveAudio: _resolveAudio,
    platform: const _ServicePlaybackPlatform(),
    completionTimeout: _playTimeout,
    errorReporter: (message) {
      lastError = message;
      // _resolveAudio 의 각 티어가 이미 구체적 사유(quota/offline/...)로
      // unavailable 을 채웠다면 여기서 일반 사유로 덮어쓰지 않는다 —
      // 아직 비어 있을 때만(finding 1b 가 다루는, 어떤 티어도 사유를
      // 남기지 않은 새 예외 종류) 최소한 배너가 뜨도록 채운다.
      if (unavailable.value == null) {
        _reportUnavailable(TtsUnavailableReason.offline);
      }
    },
  );
```

- [ ] **Step 6: 실행해 통과 확인** — `flutter test test/tts_premium_only_test.dart test/tts_unavailable_banner_test.dart test/tts_cache_key_test.dart`. `flutter analyze` 신규 이슈 0.
- [ ] **Step 7: 커밋** — `git commit -m "fix(tts): 로컬 캐시 예외 폭 확대 + 비차단 해석 실패 보고 (finding 1)"`

---

### Task 3: vocab_pack_screen `_finish()` 무방비 7단 체인 (finding 2, Critical)

**Files:**
- Modify: `lib/screens/vocab_pack_screen.dart:747-844` (`_finish()`)
- Test: `test/vocab_pack_finish_error_test.dart` (create)

**Interfaces:**
- Consumes: `lib/services/diagnostics_service.dart`의 `DiagnosticsService.logBreadcrumb(String)` (기존, 동의 게이팅 자체 내장).
- Produces: `_finish()`는 이제 `recordBossAttempt`~`addXp`~보상 체인 중 어디서 throw 해도 (a) 이미 성공한 단계는 되돌리지 않고 (b) breadcrumb 을 남기고 (c) **반드시** `/vocab/result` 로 네비게이션한다(단, `justCleared`/`nextUnlockedPackId`는 실패 지점 이전 값으로 안전하게 폴백).

- [ ] **Step 1: 실패하는 테스트 작성.** `Storage.addXp`(storage_service.dart:2754-2762)는 내부에서 `_i('kl_xp')`(→`prefs.getInt('kl_xp')`, try/catch 없음)를 쓴다 — `kl_xp`를 int 아닌 값으로 시딩하면 `recordBossAttempt`/analytics 가 **이미 끝난 뒤**, `_finish()` 한복판에서 진짜 `TypeError`가 던져진다. `VocabPackScreen`의 `packLoader`/`siblingPacksLoader`(이미 있는 테스트 DI, `test/vocab_pack_quiz_save_test.dart` 참고)로 최소 2단어(비보스 1 + 보스 1) 팩을 주입해 결정적으로 만든다:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';

import 'package:ko_lernen_app/l10n/generated/app_localizations.dart';
import 'package:ko_lernen_app/models/vocab.dart';
import 'package:ko_lernen_app/models/vocab_pack.dart';
import 'package:ko_lernen_app/screens/vocab_pack_screen.dart';
import 'package:ko_lernen_app/services/custom_pack_service.dart';
import 'package:ko_lernen_app/services/storage_service.dart';
import 'package:ko_lernen_app/theme.dart';
import 'package:ko_lernen_app/widgets/flip_card.dart';

import 'helpers/deck_actions.dart';

/// finding 2 — `_finish()` 는 recordBossAttempt→analytics×2→
/// CourseActivityReporter→addXp→stamps/box→Navigator 를 try/catch 없이
/// 잇는다. 중간에 하나가 throw 하면 결과 화면 전환이 통째로 사라진다
/// ("진행이 안 저장돼"). `kl_xp` 를 문자열로 오염시켜 `Storage.addXp`
/// (recordBossAttempt/analytics 를 이미 지난 지점)가 실제로 던지게 만들고,
/// 그래도 결과 화면으로 넘어가는지 확인한다.
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  Vocab word(int n, String korean, {bool boss = false}) => Vocab(
    id: 'fin_v$n',
    korean: korean,
    romanization: 'r$n',
    german: 'GER-$n',
    english: 'EN-$n',
    level: 'A1',
    posDe: 'Nomen',
    exampleKorean: '',
    exampleGerman: '',
    topic: 'test',
    packId: 'a1_fin_1',
    packOrder: n,
    isReviewBoss: boss,
  );

  VocabPack pack() => VocabPack(
    id: 'a1_fin_1',
    level: 'A1',
    words: [word(1, '단어일'), word(2, '보스단어', boss: true)],
  );

  setUp(() async {
    Storage.resetForTesting();
    // kl_xp 를 int 아닌 값으로 오염시켜 Storage.addXp 내부의
    // prefs.getInt('kl_xp') 가 던지게 만든다.
    SharedPreferences.setMockInitialValues({'kl_xp': 'corrupt'});
    await Storage.init();
    CustomPackService.revision.value = 0;
    await Storage.setTutVocabPackSeen();
    await Storage.setTutPackQuizSeen();
    await Storage.setTutPackBossSeen();
    await Storage.setTutWordbookSeen();
  });

  Widget buildScreen() => MaterialApp(
    debugShowCheckedModeBanner: false,
    theme: AppTheme.light,
    locale: const Locale('de'),
    supportedLocales: AppL10n.supportedLocales,
    localizationsDelegates: AppL10n.localizationsDelegates,
    onGenerateRoute: (settings) {
      if (settings.name == '/vocab/result') {
        return MaterialPageRoute(
          builder: (_) => const Scaffold(body: Text('finish-navigated')),
        );
      }
      return null;
    },
    home: VocabPackScreen(
      packId: 'a1_fin_1',
      packLoader: (_) async => pack(),
      siblingPacksLoader: (_) async => [pack()],
    ),
  );

  testWidgets('Storage.addXp 가 던져도 결과 화면으로 넘어간다 (finding 2)', (
    tester,
  ) async {
    await tester.pumpWidget(buildScreen());
    await tester.pump();
    await tester.pump(const Duration(milliseconds: 300));

    final t = await AppL10n.delegate.load(const Locale('de'));
    // Learn: 2장(단어일→보스단어) 넘기기.
    for (var i = 0; i < 2; i++) {
      tester.widget<FlipCard>(find.byType(FlipCard)).onTap!();
      await tester.pump();
      await tester.pump(const Duration(milliseconds: 400));
      tapDeckAction(tester, t.vocabPackGotIt);
      await tester.pump();
      await tester.pump(const Duration(milliseconds: 400));
    }

    // Quiz: 유일한 문제(단어일) 정답 선택 → 850ms 뒤 자동 Boss 진입.
    await tester.tap(find.text('GER-1'));
    await tester.pump(const Duration(milliseconds: 900));

    // Boss: 유일한 문제(보스단어) 정답 선택 → 850ms 뒤 _finish() 호출.
    await tester.tap(find.text('GER-2'));
    await tester.pump(const Duration(milliseconds: 900));
    await tester.pumpAndSettle();

    expect(
      find.text('finish-navigated'),
      findsOneWidget,
      reason:
          '_finish() 중간(Storage.addXp)이 throw 해도 Navigator.'
          'pushReplacementNamed 는 실행돼야 한다 — 지금은 예외가 전체 '
          '체인을 끊어 결과 화면 전환이 사라진다',
    );
    expect(tester.takeException(), isNull);
  });
}
```

- [ ] **Step 2: 실행해 실패 확인** — `flutter test test/vocab_pack_finish_error_test.dart`. 예상: `find.text('finish-navigated')` 못 찾음(RED) — 또는 `tester.takeException()`이 non-null(미청취 `TypeError`가 테스트 프레임워크까지 올라옴).
- [ ] **Step 3: 구현.** `lib/screens/vocab_pack_screen.dart`에 `import '../services/diagnostics_service.dart';`가 없으면 추가. `_finish()`(~:747-844) 전체를 아래로 교체 — `recordBossAttempt`의 반환값을 try 밖에서도 참조해야 하므로 nullable 로 미리 선언한다:

```dart
  Future<void> _finish({
    required double bossAccuracy,
    required int bossCorrect,
    required int bossTotal,
  }) async {
    final pack = _pack;
    if (pack == null) return;
    final lang = Localizations.localeOf(context).languageCode;
    final feedbackCompletion = FeedbackCompletion.vocabPack(
      packId: pack.id,
      contentLabel: VocabPackService.displayLabel(pack.id, lang: lang),
      level: pack.level,
      bossCorrect: bossCorrect,
      bossTotal: bossTotal,
      quizCorrect: _quizCorrect,
      quizTotal: _quizQuestions.length,
    );

    ({PackProgress progress, bool justCleared, VocabPack? nextUnlocked})?
    result;
    try {
      // SiblingPacks 같은 level (이미 _siblingPacks). 정렬된 pack list.
      result = await PackProgressService.recordBossAttempt(
        pack,
        _siblingPacks,
        bossAccuracy: bossAccuracy,
      );
      final bossPct = bossTotal == 0
          ? 0
          : (bossCorrect * 100 / bossTotal).round();
      await Analytics.packCompleted(
        packId: pack.id,
        accuracyPct: bossPct,
        firstClear: result.justCleared,
      );
      final bossPassed =
          bossAccuracy >= PackProgressService.bossClearThreshold;
      await Analytics.quizCompleted(
        quizType: 'vocab_boss',
        accuracyPct: bossPct,
        level: pack.level,
        pass: bossPassed,
      );
      _abandonTracker.markCompleted();
      if (!bossPassed) {
        Analytics.questFailed(
          questType: 'vocab_boss',
          failReason: 'accuracy_below_threshold',
        );
      }
      final missionContext = _missionStep == null ? null : widget.courseContext;
      if (missionContext != null) {
        final totalAnswers = _quizQuestions.length + bossTotal;
        final correctAnswers = _quizCorrect + bossCorrect;
        final courseScore = totalAnswers == 0
            ? 0.0
            : correctAnswers / totalAnswers;
        await CourseActivityReporter.recordContentAttempt(
          CurriculumContentKind.vocab,
          missionContext.initialContentId,
          courseScore >= .70,
          courseContext: missionContext,
          // Legacy enum spelling only; this score is from four-choice
          // recognition assessment, not an independent-recall gate.
          errorReason: courseScore >= .70
              ? null
              : MasteryErrorReason.vocabularyRecall,
          score: courseScore,
        );
      }
      // XP 보상 (Plan §4.4) — wordsTotal*5 + bossCorrect*10
      await Storage.addXp(pack.total * 5 + bossCorrect * 10);
      // 도장 획득 — 첫 클리어 시 토픽군 motif 도장을 도장첩에 추가.
      if (result.justCleared) {
        await Storage.addEarnedStamp(motifForPackId(pack.id).name);
        // 첫 클리어 = 보자기 하나. 팩 출처(`pack:<id>`)로 큐에 넣으면 홈·사랑방
        // 발견 배너가 바로 집어 든다(퀘스트 화면 불필요). justCleared + 큐 dedup
        // 으로 팩당 정확히 1개.
        await DecorationRewardService.ensurePendingBox(
          '${DecorationRewardService.kPackSourcePrefix}${pack.id}',
        );
      }
    } catch (error) {
      // finding 2: 이 체인 어디서든 throw 하면 예전엔 여기서 통째로
      // 끊겨 XP/클리어/도장이 조용히 사라지고 결과 화면 전환도 없었다
      // ("진행이 안 저장돼"). 이미 성공한 단계는 되돌리지 않는다 — 남은
      // 단계를 건너뛰고서라도 결과 화면으로 보내는 편이 사용자를 막다른
      // 화면(퀴즈가 그냥 멈춤)에 두는 것보다 낫다. breadcrumb 만 남긴다
      // (동의 게이팅은 DiagnosticsService 내장).
      await DiagnosticsService.logBreadcrumb(
        'vocab_pack.finish_failed pack=${pack.id} error=$error',
      );
    }

    if (!mounted) return;
    Navigator.of(context).pushReplacementNamed(
      '/vocab/result',
      arguments: vocabPackResultArguments(
        packId: pack.id,
        packLevel: pack.level,
        bossAccuracy: bossAccuracy,
        bossCorrect: bossCorrect,
        bossTotal: bossTotal,
        quizCorrect: _quizCorrect,
        quizTotal: _quizQuestions.length,
        justCleared: result?.justCleared ?? false,
        nextUnlockedPackId: result?.nextUnlocked?.id,
        feedbackCompletion: feedbackCompletion,
        courseContext: _missionStep == null ? null : widget.courseContext,
        showHardWordsCta: shouldOfferHardWordPractice(_sessionMissedWordIds),
        recallSession: _recallSession,
      ),
    );
  }
```

  (`PackProgress`/`VocabPack` 타입은 이미 이 파일에서 쓰이고 있어(`pack_progress_service.dart`/`vocab_pack_service.dart` 등 기존 import) 추가 import 불필요 — `flutter analyze`가 미해결 타입을 알리면 그 두 타입의 정의 파일을 import 에 추가할 것.)

- [ ] **Step 4: 실행해 통과 확인** — `flutter test test/vocab_pack_finish_error_test.dart`.
- [ ] **Step 5: 회귀 확인** — `flutter test test/vocab_pack_quiz_save_test.dart test/vocab_pack_flipgate_test.dart test/vocab_pack_srs_ledger_integration_test.dart` (정상 경로 `_finish()` 가 여전히 justCleared/nextUnlocked 를 정확히 채우는지). `flutter analyze` 신규 이슈 0.
- [ ] **Step 6: 커밋** — `git commit -m "fix(vocab-pack): _finish() 보상 체인 try/catch + 실패해도 결과 화면 진입 보장 (finding 2)"`

---

### Task 4: review_session_screen `_load()` 실패가 축하 빈 상태로 위장됨 (finding 3)

**Files:**
- Modify: `lib/services/review_deck_service.dart` (테스트 훅 추가)
- Modify: `lib/screens/review_session_screen.dart:75-76`(상태 필드), `:148-178`(`_load()`), `:317-346`(`build()`/`_buildEmpty`)
- Test: `test/review_session_load_error_test.dart` (create)

**Interfaces:**
- Consumes: `lib/widgets/app_error.dart`의 `AppError({required String message, required VoidCallback onRetry})` (기존, grammar_screen.dart:698-704·vocab_pack_screen.dart:863 에서 이미 쓰는 정확히 같은 시그니처).
- Produces: `ReviewDeckService.allReviewableOverride`(`@visibleForTesting` 정적 훅, `Future<List<Vocab>> Function()?`) — Task 5도 같은 파일의 다른 초기화 경로를 테스트할 때 참고할 수 있는 패턴.

- [ ] **Step 1: `ReviewDeckService`에 테스트 훅 추가.** `lib/services/review_deck_service.dart`는 지금 `allReviewable()`을 강제로 실패시킬 DI 지점이 없다(내부적으로 CSV/커스텀팩/책장을 각각 방어적으로 합치는 순수 로컬 로직이라 외부에서 자연스럽게 throw 시킬 수 없다). `SoriSpeech.speakImpl`(speakable.dart) 과 같은 패턴으로 최소 훅을 추가한다 — `class ReviewDeckService` 안, `allReviewable()`(~:45) 바로 앞:

```dart
  /// 테스트 전용 오버라이드 — 정상 경로로는 강제로 실패시킬 지점이 없어
  /// finding 3(리뷰 화면 로드 실패 처리) 회귀 테스트에 필요하다.
  @visibleForTesting
  static Future<List<Vocab>> Function()? allReviewableOverride;

  @visibleForTesting
  static void resetForTesting() {
    allReviewableOverride = null;
  }

  /// CSV + 커스텀 팩 + 책장 단어를 합친 복습 풀. 한국어 기준 중복 제거.
  static Future<List<Vocab>> allReviewable() async {
    final override = allReviewableOverride;
    if (override != null) {
      return override();
    }
    final out = <Vocab>[];
```

  (파일 상단에 `import 'package:flutter/foundation.dart' show visibleForTesting;`이 없으면 추가. 기존 `static Future<List<Vocab>> allReviewable() async { final out = <Vocab>[];` 줄은 `override` 분기 뒤로 그대로 이어진다 — 본문 나머지는 손대지 않는다.)

- [ ] **Step 2: 실패하는 테스트 작성:**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';

import 'package:ko_lernen_app/l10n/generated/app_localizations.dart';
import 'package:ko_lernen_app/screens/review_session_screen.dart';
import 'package:ko_lernen_app/services/review_deck_service.dart';
import 'package:ko_lernen_app/services/storage_service.dart';
import 'package:ko_lernen_app/theme.dart';
import 'package:ko_lernen_app/widgets/app_error.dart';

/// finding 3 — `_load()` 의 `catch (_) {}` 는 로그도 안 남기고 빈 덱으로
/// 떨어진다. 화면은 `_deck.isEmpty` 를 "오늘 복습할 게 없다"로만 해석해
/// 까치 축하 일러스트+celebration 아이콘을 보여준다 — 로드 실패와 정상
/// 완료가 화면에서 구분되지 않는다. sibling 패턴(grammar_screen.dart:
/// 695-706)과 같은 `AppError`+`onRetry` 로 고정한다.
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  setUp(() async {
    Storage.resetForTesting();
    SharedPreferences.setMockInitialValues({});
    await Storage.init();
    ReviewDeckService.resetForTesting();
  });

  tearDown(() => ReviewDeckService.resetForTesting());

  Widget buildScreen() => MaterialApp(
    theme: AppTheme.light,
    locale: const Locale('de'),
    supportedLocales: AppL10n.supportedLocales,
    localizationsDelegates: AppL10n.localizationsDelegates,
    home: const ReviewSessionScreen(),
  );

  testWidgets('로드 실패는 축하 빈 상태가 아니라 재시도 가능한 에러를 보여준다', (
    tester,
  ) async {
    ReviewDeckService.allReviewableOverride = () async {
      throw Exception('네트워크 없음');
    };

    await tester.pumpWidget(buildScreen());
    await tester.pump();
    await tester.pump(const Duration(milliseconds: 300));

    expect(
      find.byType(AppError),
      findsOneWidget,
      reason:
          '지금은 catch (_) {} 가 빈 덱으로 떨어져 magpie_celebrate 축하 '
          '빈 상태(SoriEmptyState)가 뜬다 — 로드 실패가 "오늘 복습 없음"과 '
          '구분이 안 된다',
    );
    expect(find.text(AppL10n.of(tester.element(find.byType(AppError)))
            .loadErrorTryAgain),
        findsOneWidget);
  });
}
```

- [ ] **Step 3: 실행해 실패 확인** — `flutter test test/review_session_load_error_test.dart`. 예상: `find.byType(AppError)` 못 찾음 — 지금은 `SoriEmptyState`(축하 일러스트)가 뜬다.
- [ ] **Step 4: 구현.** `lib/screens/review_session_screen.dart`에 `import '../widgets/app_error.dart';` 추가. `_ReviewSessionScreenState`의 상태 필드(~:75-76, `bool _loading = true;` 바로 뒤)에 추가:

```dart
  bool _loading = true;
  String? _error;
  List<Vocab> _deck = [];
```

  `_load()`(~:148-178)의 CSV 분기를 grammar_screen.dart:210-217 과 같은 패턴으로 교체:

```dart
  Future<void> _load() async {
    // M5: vorgegebener personalisierter Deck hat Vorrang.
    if (widget.deck != null) {
      setState(() {
        // §P2: ↓ 스킵이 덱 순서를 바꾸므로 호출부 리스트를 변형하지 않게 복사.
        _deck = List.of(widget.deck!);
        _flipped = false;
        _cardRevealed = false;
        _loading = false;
        _error = null;
      });
      return;
    }
    List<Vocab> deck = [];
    try {
      // A1: CSV + 나만의 단어장 + 책 한 컷 단어를 모두 포함 (한국어 기준 dedup).
      final vocab = await ReviewDeckService.allReviewable();
      deck = ReviewDeckService.todaySelectionForLevel(
        vocab,
        levelCode: Storage.userLevelCode,
      ).words;
    } catch (error) {
      // finding 3: 예전엔 여기서 로그 없이 빈 덱으로 떨어져, build() 가
      // "오늘 복습 없음" 축하 상태와 "로드 실패"를 구분 못 했다.
      if (!mounted) return;
      setState(() {
        _loading = false;
        _error = error.toString();
      });
      return;
    }
    if (!mounted) return;
    setState(() {
      _deck = deck;
      _flipped = false;
      _cardRevealed = false;
      _loading = false;
      _error = null;
    });
  }
```

  `build()`(~:329-336)에 에러 분기 추가:

```dart
      child: _loading
          ? const AppLoading()
          : _error != null
          ? AppError(
              message: AppL10n.of(context).loadErrorTryAgain,
              onRetry: _load,
            )
          : _deck.isEmpty
          ? _buildEmpty(t)
          : _done
          ? _buildDone(t, s)
          : _buildCard(t, s),
```

- [ ] **Step 5: 실행해 통과 확인** — `flutter test test/review_session_load_error_test.dart`.
- [ ] **Step 6: 회귀 확인** — `flutter test test/review_session_screen_speakable_test.dart`. `flutter analyze` 신규 이슈 0.
- [ ] **Step 7: 커밋** — `git commit -m "fix(review): 로드 실패를 축하 빈 상태와 구분 — AppError+재시도 (finding 3)"`

---

### Task 5: culture_notes 로드 실패가 루트 존까지 미청취로 올라감 (finding 4)

**Files:**
- Modify: `lib/screens/review_session_screen.dart:1`(import), `:141-145`(`initState()`)
- Test: `test/review_session_load_error_test.dart` (Task 4 파일에 추가)

**Interfaces:**
- Consumes: Task 4가 만든 `test/review_session_load_error_test.dart`. **이 태스크는 Task 4 다음에 실행한다** (같은 테스트 파일을 확장).
- Consumes(패턴): `lib/services/sori_stage_reward_receipt_service.dart:46`의 이미 검증된 관용구 — `unawaited(f.then<void>((_) {}, onError: (_) {}))`.

- [ ] **Step 1: 실패하는 테스트 추가** — `test/review_session_load_error_test.dart`에 두 번째 `testWidgets` 추가. `CultureNotesService.load()`를 직접 실패시킬 DI 훅은 없지만(자산 로드라 정상 실행 시 거의 항상 성공한다), 이 테스트는 **결함의 증상**(미청취 async 에러가 테스트 zone 으로 올라옴)이 아니라 **구조**(코드에 `onError`/`unawaited`가 있는지)를 확인하는 소스 패턴 테스트로 고정한다 — `tts_premium_only_test.dart`가 이미 쓰는 스타일과 동일:

```dart
import 'dart:io';

// (파일 상단 기존 import 아래에 추가)

void main() {
  // ...Task 4 의 기존 group/setUp 유지...

  test('CultureNotesService.load() 호출에 onError 가 있다 (finding 4)', () {
    final source = File(
      'lib/screens/review_session_screen.dart',
    ).readAsStringSync();
    final callSite = source.indexOf('CultureNotesService.load()');
    expect(callSite, greaterThanOrEqualTo(0));
    final snippet = source.substring(callSite, callSite + 200);

    expect(
      snippet,
      contains('onError'),
      reason:
          '지금은 CultureNotesService.load().then((_) {...}) 에 onError 가 '
          '없다 — load() 가 실패하면(에셋 파싱 등) 아무도 안 듣는 채로 '
          'Dart 가 루트 존에 미청취 비동기 에러를 보고한다 '
          '(sori_stage_reward_receipt_service.dart:46 의 '
          'unawaited(f.then(...,onError:...)) 관용구를 따를 것)',
    );
  });
}
```

- [ ] **Step 2: 실행해 실패 확인** — `flutter test test/review_session_load_error_test.dart`. 새 테스트만 FAIL.
- [ ] **Step 3: 구현.** `lib/screens/review_session_screen.dart` 맨 위에 `import 'dart:async';` 추가(현재 없음 — `unawaited` 사용에 필요). `initState()`(~:141-145):

```dart
    // K-Culture 노트 로드 후 카드 반영. load() 자체는 try/catch 없이
    // 실패할 수 있는 자산 파싱이라(finding 4), sori_stage_reward_receipt_
    // service.dart:46 과 같은 관용구로 청취해 둔다 — 실패해도 카드는
    // culture note 없이 그대로 보인다(noteFor 가 null 을 반환할 뿐).
    unawaited(
      CultureNotesService.load().then(
        (_) {
          if (mounted) {
            setState(() {});
          }
        },
        onError: (_) {},
      ),
    );
```

- [ ] **Step 4: 실행해 통과 확인** — `flutter test test/review_session_load_error_test.dart`.
- [ ] **Step 5: 회귀 확인** — `flutter test test/review_session_screen_speakable_test.dart`. `flutter analyze` 신규 이슈 0(특히 `unawaited` 미사용 경고가 없는지).
- [ ] **Step 6: 커밋** — `git commit -m "fix(review): CultureNotesService.load() 미청취 에러 방지 — 검증된 unawaited(onError:) 관용구 적용 (finding 4)"`

---

### Task 6: grammar_screen 체크포인트 시트 — 자기 자신 생명주기 무시 (finding 5)

**Files:**
- Modify: `lib/screens/grammar_screen.dart:476-497` (`submit()` 클로저, `_showCheckpoint()` 내부)
- Test: `test/grammar_checkpoint_sheet_lifetime_test.dart` (create)

**Interfaces:**
- Consumes: `BuildContext.mounted`(Flutter SDK 표준 게터 — 이 파일이 이미 바깥 `State.mounted`는 쓰고 있으니 같은 개념을 시트 자신의 `sheetContext`에도 적용).

- [ ] **Step 1: 실패하는 테스트 작성.** 실제 코스 체크포인트 링크가 필요하다 — `test/course_checkpoint_questions_test.dart`가 쓰는 것과 같은 실제 커리큘럼 카탈로그 픽스처를 재사용한다(가짜 데이터 대신 `CurriculumCatalog.load()`로 진짜 grammar 링크를 찾는다). 시트가 열린 채 응답 대기 중(`await CourseActivityReporter.recordContentAttempt(...)`) `Navigator.pop`으로 시트를 먼저 닫아버리면, 지금 코드는 그 뒤의 `setLocal(...)` 호출에서 던진다 — 그 예외 때문에 `_submittedAnswers[target.id] = answerId` 갱신이 실행되지 못하는 게 이 finding 의 핵심이다. 이 픽스처는 실제 `GrammarScreen` 위젯에서 체크포인트를 여는 정확한 탭 대상(문법 항목의 체크포인트 진입점 — `grammar_screen.dart:1023` 부근 `onTap: () => _showCheckpoint(g, assessmentLink!)`)을 실행 중 `flutter test` 위젯 트리(`debugDumpApp()` 또는 `tester.widgetList(find.byType(...))`로 실측)로 재확인해 채울 것 — 아래는 그 실측 이전 골격:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';

import 'package:ko_lernen_app/l10n/generated/app_localizations.dart';
import 'package:ko_lernen_app/models/course_practice_context.dart';
import 'package:ko_lernen_app/screens/grammar_screen.dart';
import 'package:ko_lernen_app/services/curriculum_catalog.dart';
import 'package:ko_lernen_app/services/data_loader.dart';
import 'package:ko_lernen_app/services/smalltalk_loader.dart';
import 'package:ko_lernen_app/services/storage_service.dart';
import 'package:ko_lernen_app/theme.dart';

/// finding 5 — 체크포인트 시트의 submit() 은 바깥 화면의 mounted 만 검사한다.
/// 서버 저장(recordContentAttempt)이 성공한 **뒤에도** 사용자가 시트를
/// 스와이프로 닫아버리면(dismissible), 시트 자신의 StatefulBuilder
/// setState(`setLocal`)가 던져 그 뒤의
/// `setState(() => _submittedAnswers[target.id] = answerId)` 가 실행되지
/// 못한다 — 다음에 같은 체크포인트를 다시 열면 서버엔 이미 기록된 답을
/// 로컬은 "안 푼 것"으로 보여줘 재제출을 허용한다.
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  setUp(() async {
    Storage.resetForTesting();
    SharedPreferences.setMockInitialValues({'kl_user_level': 'a1'});
    await Storage.init();
    DataLoader.reset();
    SmalltalkLoader.reset();
    CurriculumCatalog.reset();
  });

  testWidgets('제출 처리 중 시트가 닫혀도 로컬 제출 상태는 기록된다', (tester) async {
    final catalog = await CurriculumCatalog.load();
    final grammarLink = catalog.contentLinks.firstWhere(
      (link) => link.contentKind == CurriculumContentKind.grammar,
    );
    final courseContext = CoursePracticeContext.fromLink(grammarLink);

    await tester.pumpWidget(
      MaterialApp(
        theme: AppTheme.light,
        locale: const Locale('de'),
        supportedLocales: AppL10n.supportedLocales,
        localizationsDelegates: AppL10n.localizationsDelegates,
        home: GrammarScreen(courseContext: courseContext),
      ),
    );
    await tester.pumpAndSettle();

    // TODO(실행 중 확정): 위 grammarLink.contentId 카드로 이동 →
    // 체크포인트 진입점 탭(grammar_screen.dart:1023 부근) → 옵션 하나 탭
    // → 서버 응답이 아직 pending 인 한 프레임 안에서 시트를
    // Navigator.pop(context) 로 닫는다 → pumpAndSettle → 같은 체크포인트를
    // 다시 열어 "이미 제출됨" 상태(비활성화된 옵션들·정답 표시)로 뜨는지
    // 확인한다. 실측 위젯 트리는 `tester.allWidgets`/`debugDumpApp()` 로
    // 확정할 것 — grammar_choice_quiz_screen_test.dart 가 비슷한 화면
    // 진입 패턴을 참고할 수 있다.
  });
}
```

  이 스텝은 **실측 없이 컴파일만 통과하는 골격**이다 — 실행 전에 `tester.pumpAndSettle()` 뒤 `debugDumpApp()`으로 실제 체크포인트 진입 위젯(Key/타입/텍스트)을 확인하고 TODO 블록을 실제 `tester.tap`/`find` 호출로 채운 뒤에만 "실패하는 테스트"로 간주한다.

- [ ] **Step 2: 실측 후 테스트 완성 + 실행해 실패 확인** — 위 TODO를 실제 tap 시퀀스로 채우고 `flutter test test/grammar_checkpoint_sheet_lifetime_test.dart` 실행. 예상: 시트를 중간에 닫으면 `FlutterError`(`setState() called after dispose()`류)가 `tester.takeException()`에 잡히거나, 재오픈 시 "이미 제출됨" 상태가 아니라 다시 빈 상태로 뜬다.
- [ ] **Step 3: 구현.** `lib/screens/grammar_screen.dart` ~:476-497, `submit()`:

```dart
            Future<void> submit(String answerId) async {
              if (isComplete || isSaving) return;
              setLocal(() => isSaving = true);
              final update = await CourseActivityReporter.recordContentAttempt(
                CurriculumContentKind.grammar,
                target.id,
                question.isCorrect(answerId),
                courseContext: widget.courseContext,
                conceptId: assessmentLink.conceptIds.single,
                errorReason: question.isCorrect(answerId)
                    ? null
                    : MasteryErrorReason.unknown,
              );
              if (!mounted) return;
              // finding 5: sheetContext 는 이 시트 자신의 생명주기다 —
              // 바깥 화면(mounted)이 살아 있어도 사용자가 이 시트를
              // 스와이프로 닫아버리면 sheetContext 는 이미 unmount 됐고,
              // setLocal(StatefulBuilder 의 setState)을 계속 부르면 던진다.
              // 그 예외 때문에 아래 서버-확정 로컬 기록이 통째로
              // 스킵되면(예전 코드) 서버엔 저장된 답이 로컬엔 "안 풀림"으로
              // 남아 재제출을 허용한다 — 그러니 로컬 기록은 sheetContext
              // 생사와 무관하게 항상 실행하고, 시트 자신의 UI 갱신
              // (setLocal/에러 토스트)만 sheetContext.mounted 로 가드한다.
              final sheetStillOpen = sheetContext.mounted;
              if (sheetStillOpen) {
                setLocal(() => isSaving = false);
              }
              if (update != null) {
                setState(() => _submittedAnswers[target.id] = answerId);
                if (sheetStillOpen) {
                  setLocal(() => selectedAnswer = answerId);
                }
              } else if (sheetStillOpen) {
                _showCheckpointSaveError();
              }
            }
```

- [ ] **Step 4: 실행해 통과 확인** — `flutter test test/grammar_checkpoint_sheet_lifetime_test.dart`.
- [ ] **Step 5: 회귀 확인** — `flutter test test/course_checkpoint_questions_test.dart test/advanced_checkpoint_mastery_test.dart test/course_activity_reporter_test.dart`. `flutter analyze` 신규 이슈 0.
- [ ] **Step 6: 커밋** — `git commit -m "fix(grammar): 체크포인트 시트 자체 생명주기로 로컬 제출 기록 보호 (finding 5)"`

---

### Task 7: vocab_pack_screen 850ms 지연 타이머 미보관 (finding 6, Minor)

**Files:**
- Modify: `lib/screens/vocab_pack_screen.dart` (상태 필드 ~:207-211 부근, `dispose()` ~:213-218, `_selectChoice()` ~:713)
- Test: `test/vocab_pack_advance_timer_test.dart` (create)

**Interfaces:**
- Consumes: 없음(순수 내부 위생 수정). 참고 패턴: `lib/widgets/sori/content_feed.dart:125-126,142-144`의 `Timer? _tapReset; Timer? _burstHide;` + `dispose()`의 `?.cancel()`.

- [ ] **Step 1: 실패하는 테스트 작성** — `_advanceQuiz`가 이미 `mounted` 가드가 있어 크래시는 안 나지만, 850ms 안에 화면이 사라지면 `flutter_test`가 남은 `Timer`를 테스트 종료 시 잡아낸다(이 저장소의 `AudioPolicy`/video-lease Timer 들과 같은 방식으로 이미 여러 번 검출된 패턴):

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';

import 'package:ko_lernen_app/l10n/generated/app_localizations.dart';
import 'package:ko_lernen_app/models/vocab.dart';
import 'package:ko_lernen_app/models/vocab_pack.dart';
import 'package:ko_lernen_app/screens/vocab_pack_screen.dart';
import 'package:ko_lernen_app/services/custom_pack_service.dart';
import 'package:ko_lernen_app/services/storage_service.dart';
import 'package:ko_lernen_app/theme.dart';
import 'package:ko_lernen_app/widgets/flip_card.dart';

import 'helpers/deck_actions.dart';

/// finding 6 — `Future.delayed(850ms, _advanceQuiz)` 는 Timer 필드로
/// 저장되지 않아 dispose() 가 취소하지 못한다. `_advanceQuiz` 의 mounted
/// 가드 덕에 크래시는 안 나지만, 위생 문제다 — content_feed.dart 의
/// `_tapReset`/`_burstHide` 처럼 필드로 보관·취소해야 한다.
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  Vocab word(int n, String korean, {bool boss = false}) => Vocab(
    id: 'tim_v$n',
    korean: korean,
    romanization: 'r$n',
    german: 'GER-$n',
    english: 'EN-$n',
    level: 'A1',
    posDe: 'Nomen',
    exampleKorean: '',
    exampleGerman: '',
    topic: 'test',
    packId: 'a1_tim_1',
    packOrder: n,
    isReviewBoss: boss,
  );

  VocabPack pack() => VocabPack(
    id: 'a1_tim_1',
    level: 'A1',
    words: [word(1, '단어일'), word(2, '보스단어', boss: true)],
  );

  setUp(() async {
    Storage.resetForTesting();
    SharedPreferences.setMockInitialValues({});
    await Storage.init();
    CustomPackService.revision.value = 0;
    await Storage.setTutVocabPackSeen();
    await Storage.setTutPackQuizSeen();
    await Storage.setTutPackBossSeen();
    await Storage.setTutWordbookSeen();
  });

  Widget buildScreen() => MaterialApp(
    debugShowCheckedModeBanner: false,
    theme: AppTheme.light,
    locale: const Locale('de'),
    supportedLocales: AppL10n.supportedLocales,
    localizationsDelegates: AppL10n.localizationsDelegates,
    home: VocabPackScreen(
      packId: 'a1_tim_1',
      packLoader: (_) async => pack(),
      siblingPacksLoader: (_) async => [pack()],
    ),
  );

  testWidgets('선택 뒤 위젯이 즉시 사라져도 850ms 지연 타이머가 남지 않는다', (
    tester,
  ) async {
    await tester.pumpWidget(buildScreen());
    await tester.pump();
    await tester.pump(const Duration(milliseconds: 300));

    final t = await AppL10n.delegate.load(const Locale('de'));
    for (var i = 0; i < 2; i++) {
      tester.widget<FlipCard>(find.byType(FlipCard)).onTap!();
      await tester.pump();
      await tester.pump(const Duration(milliseconds: 400));
      tapDeckAction(tester, t.vocabPackGotIt);
      await tester.pump();
      await tester.pump(const Duration(milliseconds: 400));
    }

    // 퀴즈 단계 진입 — 정답을 골라 850ms Future.delayed(_advanceQuiz) 를
    // 예약시킨다.
    await tester.tap(find.text('GER-1'));
    await tester.pump();

    // 지연이 끝나기 전에 화면을 통째로 걷어낸다 — dispose() 가 타이머를
    // 취소하지 않으면 flutter_test 가 테스트 종료 시 "A Timer is still
    // pending" 으로 이 테스트를 실패시킨다.
    await tester.pumpWidget(const SizedBox());
    await tester.pump(const Duration(milliseconds: 900));

    expect(tester.takeException(), isNull);
  });
}
```

- [ ] **Step 2: 실행해 실패 확인** — `flutter test test/vocab_pack_advance_timer_test.dart`. 예상: `flutter_test`가 남은 `Timer`를 검출해 테스트가 FAIL한다(에러 메시지에 "Timer" 포함).
- [ ] **Step 3: 구현.** 상태 필드(~:207-211 부근, `late final QuestAbandonTracker _abandonTracker;` 근처)에 추가:

```dart
  late final QuestAbandonTracker _abandonTracker;
  Timer? _advanceQuizTimer;
```

  (`dart:async`의 `Timer`가 이미 이 파일에서 resolve 되지 않으면 `import 'dart:async';` 추가 — `Future.delayed` 자체는 이미 쓰고 있으므로 대개 이미 있다.)

  `dispose()`(~:213-218):

```dart
  @override
  void dispose() {
    _advanceQuizTimer?.cancel();
    _abandonTracker.dispose();
    _flipHintTrigger.dispose();
    super.dispose();
  }
```

  `_selectChoice()` 끝(~:713):

```dart
    // 짧은 피드백 후 다음 질문
    _advanceQuizTimer?.cancel();
    _advanceQuizTimer = Timer(const Duration(milliseconds: 850), _advanceQuiz);
```

- [ ] **Step 4: 실행해 통과 확인** — `flutter test test/vocab_pack_advance_timer_test.dart`.
- [ ] **Step 5: 회귀 확인** — `flutter test test/vocab_pack_quiz_save_test.dart`. `flutter analyze` 신규 이슈 0.
- [ ] **Step 6: 커밋** — `git commit -m "fix(vocab-pack): 퀴즈 진행 지연 타이머를 필드로 보관·dispose 시 취소 (finding 6)"`

---

### Task 8: SoriPressable — onLongPress 전용 위젯의 키보드 포커스 트랩 (finding 8, 감사 원문 재검증됨)

**Files:**
- Modify: `lib/widgets/sori/pressable.dart:148-158` (`Focus.onKeyEvent`)
- Test: `test/sori_pressable_keyboard_test.dart` (extend — 기존 파일)

**Interfaces:**
- Consumes: 없음(기존 위젯 내부 수정). `enabled`(빌드 로컬 변수, ~:140행 `final enabled = widget.onTap != null || widget.onLongPress != null;`)를 `onKeyEvent` 클로저에서도 재사용.

> **재검증 메모**: 원 감사가 "SoriPressable 은 순수 Focus+GestureDetector 라 Enter/Space 가 전혀 안 먹는다"고 적었지만, 현재 코드(`pressable.dart:146-158`)에는 이미 `Focus(onKeyEvent: ...)`로 Enter/Space→`_onTap()` 배선이 있고 `test/sori_pressable_keyboard_test.dart`가 이미 통과 중이다(`git log -p -- lib/widgets/sori/pressable.dart` 상 `260dc9d6`에서 landed). 이 태스크는 그 좁은 잔여 결함만 고친다: `onKeyEvent`가 `widget.onTap == null`이면 무조건 무시해서, **`onLongPress`만 있고 `onTap`이 없는 위젯**은 `canRequestFocus: enabled`(`enabled`는 `onLongPress`만 있어도 true) 때문에 Tab 으로 포커스는 잡히는데 Enter/Space 를 눌러도 아무 반응이 없는 "포커스 트랩"이 된다.

- [ ] **Step 1: 실패하는 테스트 추가** — `test/sori_pressable_keyboard_test.dart`에 세 번째 `testWidgets` 추가(파일은 이미 `flutter/services.dart`/`LogicalKeyboardKey`를 import 중):

```dart
  testWidgets(
    'onTap 없이 onLongPress 만 있어도 Enter/Space 가 활성화한다 (finding 8)',
    (tester) async {
      var longPressCount = 0;
      await tester.pumpWidget(
        MaterialApp(
          home: Scaffold(
            body: SoriPressable(
              haptic: null,
              onLongPress: () => longPressCount += 1,
              child: const SizedBox(
                width: 120,
                height: 48,
                child: Center(child: Text('Hold')),
              ),
            ),
          ),
        ),
      );

      await tester.sendKeyEvent(LogicalKeyboardKey.tab);
      await tester.pump();
      await tester.sendKeyEvent(LogicalKeyboardKey.enter);
      await tester.sendKeyEvent(LogicalKeyboardKey.space);

      expect(
        longPressCount,
        2,
        reason:
            '지금은 onKeyEvent 가 widget.onTap == null 이면 무조건 무시한다 '
            '— onLongPress 만 있는 위젯은 canRequestFocus: enabled 때문에 '
            'Tab 으로 포커스는 잡히지만 Enter/Space 를 눌러도 아무 일도 '
            '안 일어나는 포커스 트랩이 된다',
      );
    },
  );
```

- [ ] **Step 2: 실행해 실패 확인** — `flutter test test/sori_pressable_keyboard_test.dart`. 새 테스트: `longPressCount`가 0으로 남아 FAIL.
- [ ] **Step 3: 구현.** `lib/widgets/sori/pressable.dart` ~:146-158:

```dart
    return Focus(
      canRequestFocus: enabled,
      onKeyEvent: (_, event) {
        if (!enabled || event is! KeyDownEvent) {
          return KeyEventResult.ignored;
        }
        if (event.logicalKey == LogicalKeyboardKey.enter ||
            event.logicalKey == LogicalKeyboardKey.space) {
          // finding 8: onTap 이 없으면(= onLongPress 만 있는 위젯) 예전엔
          // 여기서 그냥 무시했다 — 그런데 canRequestFocus 는 enabled(둘 중
          // 하나만 있어도 true) 라서 그런 위젯도 Tab 포커스는 받는다.
          // 키보드엔 "누르고 있기"에 대응하는 제스처가 없으므로, 유일한
          // 액션인 onLongPress 를 Enter/Space 의 활성화 대상으로 쓴다.
          if (widget.onTap != null) {
            _onTap();
          } else if (widget.onLongPress != null) {
            _onLongPress();
          } else {
            return KeyEventResult.ignored;
          }
          return KeyEventResult.handled;
        }
        return KeyEventResult.ignored;
      },
```

- [ ] **Step 4: 실행해 통과 확인** — `flutter test test/sori_pressable_keyboard_test.dart` (기존 2개 + 신규 1개 전부 GREEN — 특히 기존 "SoriPressable activates with Enter and Space" 가 회귀 없이 그대로 통과하는지).
- [ ] **Step 5: 회귀 확인** — `flutter test test/widgets/`(pressable 을 쓰는 위젯 스냅샷류가 있으면). `flutter analyze` 신규 이슈 0.
- [ ] **Step 6: 커밋** — `git commit -m "fix(a11y): onLongPress 전용 SoriPressable 의 Enter/Space 포커스 트랩 해소 (finding 8 — 핵심 수정+테스트는 이미 존재, 잔여 케이스만)"`

---

### Task 9: smalltalk_screen 표현 넘기기가 제스처 전용 (finding 9)

**Files:**
- Modify: `lib/screens/smalltalk_screen.dart:645`, `:795` (`_PhraseCardState.build()` 반환문을 `Semantics` 로 감싸기)
- Modify: `lib/l10n/app_de.arb:513`, `lib/l10n/app_en.arb:513` (신규 키 2개, `smalltalkNextTurn` 바로 뒤)
- Test: `test/smalltalk_screen_ui_test.dart` (extend — 기존 파일)

**Interfaces:**
- Consumes: `t.smalltalkNextPhrase`/`t.smalltalkPreviousPhrase`(신규 ARB 키). 패턴 원본: `grammar_screen.dart:861-885`의 `Semantics(container: true, customSemanticsActions: {...}, child: SoriContentFeed(...))`.

> `knowLabel`/`hardLabel`(grammar_screen 이 쓰는 "이해함/어려움" 가시 버튼)은 **의도적으로 추가하지 않는다** — 이 화면의 `SoriContentFeed`는 `onHard`/`onSkip`을 배선하지 않는다(표현 열람이지 판정 덱이 아니다). "이해함/어려움" 라벨을 붙이면 없는 판정 기능을 있는 것처럼 알리게 된다. `customSemanticsActions`(스크린리더 전용, 화면엔 아무것도 안 그림)만 추가한다 — 이게 finding 9 의 WCAG 2.5.1 핵심(스와이프를 쓸 수 없는 사용자의 대체 수단)이다.

- [ ] **Step 1: arb 키 추가.** `lib/l10n/app_de.arb`와 `lib/l10n/app_en.arb` 양쪽 513행(`"smalltalkNextTurn": ...`) 바로 뒤에:

  `app_de.arb`:
```json
  "smalltalkNextTurn": "Nächster Gesprächsschritt",
  "smalltalkNextPhrase": "Nächster Ausdruck",
  "smalltalkPreviousPhrase": "Vorheriger Ausdruck",
```

  `app_en.arb`:
```json
  "smalltalkNextTurn": "Next turn",
  "smalltalkNextPhrase": "Next phrase",
  "smalltalkPreviousPhrase": "Previous phrase",
```

  `flutter gen-l10n` 실행.

- [ ] **Step 2: 실패하는 테스트 추가** — `test/smalltalk_screen_ui_test.dart`에 추가(파일은 이미 `SoriContentFeed`/`AppL10n`/`SmalltalkLoader.load()`를 import·초기화 중):

```dart
  testWidgets(
    '표현 넘기기 스와이프 대체수단이 스크린리더에 노출된다 (finding 9)',
    (tester) async {
      await _pumpSmalltalk(tester, child: const SmalltalkScreen());
      await tester.pumpAndSettle();

      final t = await AppL10n.delegate.load(const Locale('de'));
      final wrappers = tester
          .widgetList<Semantics>(
            find.ancestor(
              of: find.byType(SoriContentFeed),
              matching: find.byType(Semantics),
            ),
          )
          .where((w) => w.properties.customSemanticsActions != null)
          .toList();

      expect(
        wrappers,
        isNotEmpty,
        reason:
            '지금은 SoriContentFeed(onNext/onPrevious) 를 감싸는 '
            'customSemanticsActions 래퍼가 전혀 없다 — 표현 넘기기가 '
            '100% 제스처 전용이라 스와이프를 못 쓰는 스크린리더 사용자는 '
            '다음/이전 표현으로 갈 방법이 없다 (WCAG 2.5.1)',
      );
      final labels = wrappers.first.properties.customSemanticsActions!.keys
          .map((a) => a.label)
          .toList();
      expect(labels, contains(t.smalltalkNextPhrase));
    },
  );
```

  (정확한 `_pumpSmalltalk` 헬퍼 시그니처·`SmalltalkScreen` 생성자 인자는 이 파일의 기존 테스트들을 그대로 복사할 것 — 이미 이 파일 47-59행에서 쓰는 패턴.)

- [ ] **Step 3: 실행해 실패 확인** — `flutter test test/smalltalk_screen_ui_test.dart`. 새 테스트: `wrappers`가 비어 FAIL.
- [ ] **Step 4: 구현.** `lib/screens/smalltalk_screen.dart`, `_PhraseCardState.build()`의 반환문 시작(~:645)을:

```dart
    return Semantics(
      // finding 9: SoriContentFeed 의 onNext/onPrevious 는 세로 스와이프
      // 전용이다 — grammar_screen.dart:861-885 와 같은 패턴으로 스크린
      // 리더 전용 대체 액션을 얹는다. 화면엔 아무것도 그리지 않는다.
      container: true,
      customSemanticsActions: <CustomSemanticsAction, VoidCallback>{
        if (widget.onPrevious != null)
          CustomSemanticsAction(label: t.smalltalkPreviousPhrase):
              widget.onPrevious!,
        if (widget.onNext != null)
          CustomSemanticsAction(label: t.smalltalkNextPhrase): widget.onNext!,
      },
      child: SoriContentFeed(
```

  그리고 반환문 끝(~:795, 현재 `SoriContentFeed(`를 닫는 `    );`)을:

```dart
      ),
    );
```

  (즉 `SoriContentFeed(`를 닫는 괄호 한 줄을, `SoriContentFeed` 자신을 닫는 `),`와 새 `Semantics(`를 닫는 `);` 두 줄로 늘린다 — 사이의 본문 150여 줄은 손대지 않는다.)

- [ ] **Step 5: 실행해 통과 확인** — `flutter test test/smalltalk_screen_ui_test.dart`.
- [ ] **Step 6: 회귀 확인** — `flutter test test/course_checkpoint_questions_test.dart`(smalltalk 링크 사용), `flutter gen-l10n` 재확인(코드 생성물 diff 없음/일관). `flutter analyze` 신규 이슈 0.
- [ ] **Step 7: 커밋** — `git commit -m "feat(a11y): smalltalk 표현 넘기기 스크린리더 대체수단 추가 (finding 9)"`

---

### Task 10: SoriChromeRow — 44dp 조상이 문서화된 48dp 터치 영역을 잘라먹음 (finding 10)

**Files:**
- Modify: `lib/widgets/sori/chrome_row.dart` (`SoriChromeRow.build()` 전체, `_ChromeSlot.build()` 전체)
- Test: `test/chrome_row_test.dart` (create)

**Interfaces:**
- Produces: `SoriChromeRow`의 실제 레이아웃 높이가 44dp→**48dp**로 바뀐다(현재 호출부 0곳이라 파괴적 변경 없음). `SoriLayout.chromeRowHeight`(44)는 이제 아이콘의 시각 크기로만 쓰이고, 레이아웃 높이는 `SoriLayout.chromeRowTouchHeight`(48) 하나로 통일된다.

> **왜 OverflowBox 폭 확대가 아니라 조상 자체를 48dp로 바꾸는가**: Flutter 의 히트테스트는 `RenderBox.hitTest`가 **자기 자신의 size 안**인지부터 검사하고, 아니면 자식을 아예 시도하지 않는다. `_ChromeSlot`의 44×44 `SizedBox` 안에 48×48 `OverflowBox`를 넣는 것과 똑같이, `SoriChromeRow` 바깥의 `SizedBox(height: 44)` **자체가** 44dp 밖의 좌표를 자식(`Row`→`_ChromeSlot`)에 전달하기도 전에 끊는다 — `_ChromeSlot` 내부만 고쳐서는 세로 방향(위/아래 2dp)이 여전히 안 눌린다. 유일한 실제 해법은 `SoriChromeRow` 자신의 레이아웃 높이를 48dp로 올리는 것이다. 호출부가 없어 지금 하는 게 W5 도입 전 마지막 기회다(finding 10 원문 그대로).

- [ ] **Step 1: 실패하는 테스트 작성:**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ko_lernen_app/widgets/sori/chrome_row.dart';

/// finding 10 — `_ChromeSlot` 은 44dp `SizedBox` 안에 48dp `OverflowBox` 를
/// 넣는다. Flutter 는 히트테스트를 조상의 실제 크기(44dp)에서 끊으므로
/// 문서화된 48dp 터치 영역 중 조상 밖으로 튀어나온 구간은 "보이기만 하고
/// 눌리지 않는다" — speakable.dart:144-157(SoriSpeechIndicator)이 검수#13
/// 에서 고친 것과 같은 버그다. 현재 호출부가 0곳이라 잠복해 있다.
void main() {
  testWidgets('필터 아이콘의 48dp 터치 영역 전체가 눌린다', (tester) async {
    var tapped = false;
    await tester.pumpWidget(
      MaterialApp(
        home: Scaffold(
          body: SoriChromeRow(
            onFilterTap: () => tapped = true,
            filterSemanticLabel: 'filter',
          ),
        ),
      ),
    );

    final rowTopLeft = tester.getTopLeft(find.byType(SoriChromeRow));
    // 옛 버그에서는 SoriChromeRow 자신의 히트테스트가 44dp 조상
    // SizedBox 에서 끊겼다 — (2, 46) 은 44dp 안에는 없고 48dp 안에는
    // 있는 지점이다.
    await tester.tapAt(rowTopLeft + const Offset(2, 46));
    await tester.pump();

    expect(
      tapped,
      isTrue,
      reason:
          '문서화된 48dp 터치 영역 중 옛 44dp 경계 밖(y=46)을 찍었는데 '
          '반응이 없다 — 조상이 여전히 44dp 에서 히트테스트를 끊고 있다.',
    );
  });
}
```

- [ ] **Step 2: 실행해 실패 확인** — `flutter test test/chrome_row_test.dart`. 예상: `tapped`가 `false`로 남아 FAIL — `(2,46)`이 현재 44dp 외곽 `SizedBox`의 `size.contains()`를 통과 못 해 `Row`/`_ChromeSlot`까지 내려가지도 못한다.
- [ ] **Step 3: 구현.** `lib/widgets/sori/chrome_row.dart` 전체를 아래로 교체(클래스 독스트링·public API·import 는 그대로, `build()` 본문과 `_ChromeSlot`만 변경):

```dart
import 'package:flutter/material.dart';

import 'pressable.dart';
import 'tokens.dart';

/// **SoriChromeRow** — §17 앱바 아래 단 하나의 크롬 행.
///
/// leading 필터 아이콘 + center 진행 메타 + trailing 컨트롤 1개(보통
/// `TtsSpeedAction`)만 담는다. **본문 위에 이 행을 두 번 쌓지 않는다** —
/// `chrome_stack_guard_test.dart`가 화면당 Wrap+칩 중복 적층을 잡는다.
///
/// 레이아웃 높이는 [SoriLayout.chromeRowTouchHeight](48dp)로 고정한다 —
/// leading/trailing 아이콘의 실제 터치 영역과 같은 값이어야 한다.
/// Flutter 는 히트테스트를 조상의 실제 크기에서 끊으므로, 이 행 자신이
/// 44dp 라면 안쪽에서 48dp 를 아무리 그려도 위/아래 2dp 는 절대 눌리지
/// 않는다(검수#13 이 speakable.dart 에서 고친 것과 같은 버그, finding
/// 10). 아이콘의 시각 크기(44dp)는 `_ChromeSlot` 안에서 `Center` 로
/// 배치한다.
class SoriChromeRow extends StatelessWidget {
  const SoriChromeRow({
    super.key,
    this.onFilterTap,
    this.filterSemanticLabel,
    this.meta,
    this.trailing,
  });

  /// 탭하면 필터 시트를 여는 콜백(예: `showSoriFilterSheet`). null이면
  /// leading 아이콘 자체를 숨긴다.
  final VoidCallback? onFilterTap;
  final String? filterSemanticLabel;

  /// 가운데 진행 메타 — 보통 `Text('3 / 12', style: tt.meta)`.
  final Widget? meta;

  /// 오른쪽 단일 컨트롤 — 보통 `TtsSpeedAction`. 두 번째 컨트롤을 넣지 않는다.
  final Widget? trailing;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: SoriLayout.chromeRowTouchHeight,
      child: Row(
        children: [
          if (onFilterTap != null)
            _ChromeSlot(
              icon: Icons.tune_rounded,
              semanticLabel: filterSemanticLabel ?? '',
              onTap: onFilterTap!,
            )
          else
            const SizedBox(width: Spacing.sm),
          Expanded(child: Center(child: meta ?? const SizedBox.shrink())),
          if (trailing != null)
            trailing!
          else
            const SizedBox(width: Spacing.sm),
        ],
      ),
    );
  }
}

class _ChromeSlot extends StatelessWidget {
  const _ChromeSlot({
    required this.icon,
    required this.semanticLabel,
    required this.onTap,
  });

  final IconData icon;
  final String semanticLabel;
  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) {
    final s = SoriSurfaces.of(context);
    // SoriPressable 을 48dp SizedBox 로 직접 감싼다 — speakable.dart 의
    // SoriSpeechIndicator 와 같은 패턴(검수#13). OverflowBox 트릭을 쓰지
    // 않는다: 그 트릭은 조상이 44dp 인 채로 남아 있을 때만 필요했는데,
    // 이제 SoriChromeRow 자신이 48dp 이므로 불필요하고, 여전히 조상이
    // 44dp 라고 착각하게 만드는 코드 냄새다.
    return Semantics(
      button: true,
      label: semanticLabel,
      child: ExcludeSemantics(
        child: SoriPressable(
          onTap: onTap,
          child: SizedBox(
            width: SoriLayout.chromeRowTouchHeight,
            height: SoriLayout.chromeRowTouchHeight,
            child: Center(
              child: Icon(icon, size: 22, color: s.text),
            ),
          ),
        ),
      ),
    );
  }
}
```

- [ ] **Step 4: 실행해 통과 확인** — `flutter test test/chrome_row_test.dart`.
- [ ] **Step 5: 회귀 확인** — `grep -rn "SoriChromeRow(" lib/` 로 호출부가 정말 0곳인지 재확인(0곳이 아니면 그 화면의 골든/레이아웃 테스트를 함께 돌릴 것). `flutter analyze` 신규 이슈 0. `flutter test test/uiux_bible_closeout_inventory_test.dart`(이 태스크는 파일을 추가/삭제하지 않으므로 Sori 파일 수 133 불변 — 그린이어야 한다).
- [ ] **Step 6: 커밋** — `git commit -m "fix(a11y): SoriChromeRow 를 48dp 로 통일 — 44dp 조상이 끊던 히트테스트 해소 (finding 10)"`

---

### Task 11: speakable.dart ⚠️ 주석의 원인 설명 정정 (fold-in, doc-only)

**Files:**
- Modify: `lib/widgets/sori/speakable.dart:79-87` (주석만)

**Interfaces:**
- 없음 — 동작하는 코드는 손대지 않는다(`_startSpeak`/`prefetch`의 "중간 변수 + 블록 바디" 구현 자체는 이미 옳다).

- [ ] **Step 1: 현재 주석 재확인.** `lib/widgets/sori/speakable.dart:79-87`은 지금 "원인은 특정되지 않았으나(원인 미상), speak()↔prefetch() 교차 합류에서만 재현, 단독 호출로는 재현 안 됨"이라 적혀 있다. 실제 메커니즘은 특정 가능하다: `_inFlight`의 값 타입이 `Future<bool>`이라, 화살표 바디 `.whenComplete(() => _inFlight.remove(key))`는 `_inFlight.remove(key)`의 반환값 — 바로 그 키에 막 저장된, **지금 만들고 있는 이 future 자신** — 을 콜백의 반환값으로 암묵 반환한다. `whenComplete`의 콜백이 Future 를 반환하면 바깥 future 는 그 반환된 Future 가 끝날 때까지 완료를 미루는데, 여기선 그 반환된 Future 가 바깥 future 자기 자신이라 완료가 영원히 미뤄진다(self-await). "교차 합류에서만 재현"은 착시였다 — 자기대기는 호출 방식과 무관하게 항상 걸리고, 단독 호출 테스트는 그 Future 의 완료를 끝까지 기다리지 않아 증상이 안 보였을 뿐이다. 블록 바디(`() { _inFlight.remove(key); }`)는 반환값이 없어(void) 이 문제가 원천적으로 없다.
- [ ] **Step 2: 주석 교체.** ~:79-87 을 아래로 교체(코드는 그대로):

```dart
  // ⚠️ speakImpl(...)/prefetchImpl(...) 뒤의 .then/.whenComplete 는 반드시
  // (1) 중간 변수로 한 번 끊고 (2) 콜백을 블록 바디로 쓸 것.
  // 진짜 원인(정정, 2026-08-27 사후 재분석): `_inFlight` 의 값 타입이
  // `Future<bool>` 이라, 화살표 바디 `.whenComplete(() =>
  // _inFlight.remove(key))` 는 `_inFlight.remove(key)` 의 반환값 — 바로
  // 그 키에 막 저장된, **지금 만들고 있는 이 future 자신** — 을 콜백의
  // 반환값으로 암묵 반환한다. `whenComplete` 콜백이 Future 를 반환하면
  // 바깥 future 는 그 반환된 Future 가 끝날 때까지 완료를 미루는데,
  // 여기선 그 반환된 Future 가 바깥 future 자기 자신이라 완료가 영원히
  // 미뤄진다(self-await). 예전 주석은 "원인 미상 + speak()↔prefetch()
  // 교차 합류에서만 재현"이라 적었지만 이건 착시였다 — 자기대기는 호출
  // 방식과 무관하게 항상 걸리고, 단독 호출 테스트는 그 Future 의 완료를
  // 끝까지 기다리지 않아 증상이 안 보였을 뿐이다. 블록 바디(`() {
  // _inFlight.remove(key); }`)는 반환값이 없어(void) 이 문제가 원천적으로
  // 없다 — 인라인 체인 방식으로 되돌릴 땐 이 메커니즘을 반드시 재확인할 것.
```

- [ ] **Step 3: 검증** — 코드 diff 가 주석 줄만인지 `git diff`로 확인(로직 변경 0줄). `flutter analyze` 신규 이슈 0(주석 변경이라 사실상 항상 통과하지만 계약대로 확인). `flutter test test/speakable_semantics_test.dart` 그린 유지(회귀 없음 확인용 — 이 태스크가 건드리지 않은 동작이 여전히 통과하는지).
- [ ] **Step 4: 커밋** — `git commit -m "docs(speakable): whenComplete 자기대기 원인 정정 — 화살표 바디가 Map.remove 의 Future 를 반환 (동작 코드 불변)"`

---

## Self-Review 결과

- **스펙 커버리지**: 감사 원문 10개 finding + fold-in 1개 = 11개 전부 태스크 1개씩 대응(1↔Task2, 2↔Task3, 3↔Task4, 4↔Task5, 5↔Task6, 6↔Task7, 7↔Task1, 8↔Task8, 9↔Task9, 10↔Task10, fold-in↔Task11). 빠진 항목 없음.
- **재검증 정정 2건**: finding 7(전역 훅 — 실제론 이미 있었고 설치 시점만 문제)과 finding 8(키보드 활성화 — 핵심 fix+테스트 이미 존재, `onLongPress`-only 케이스만 잔존)을 원문 그대로 옮기지 않고 코드 재확인 결과로 재작성했다 — 상단 "⚠️ 감사 결과 재검증" 섹션에 이유를 남겼다. 나머지 8개(1,2,3,4,5,6,9,10)는 재확인 결과 원문과 현재 코드가 (라인 번호 소폭 차이 외) 일치했다.
- **플레이스홀더 스캔**: "TBD"/"나중에"/"적절히 처리" 류 문구 없음. Task 6만 예외적으로 "실측 후 채울 것" TODO 블록을 포함하는데 — 이유를 명시했다: `_showCheckpoint`를 여는 정확한 UI 진입 시퀀스(카드 위치·assessmentLink 조건)는 실행 중인 위젯 트리 실측이 필요해, 코드만 읽고 좌표/파인더를 확정하면 오히려 틀린 걸 그대로 베끼는 실패 모드가 된다 — 이 한 곳만 "그 자리에서 실측 후 채우기"를 명시적 스텝(Step 2)으로 분리했고, 나머지 10개 태스크의 코드/테스트는 전부 실제 파일을 읽고 확정한 구체 코드다.
- **시그니처 일관성**: `PackProgressService.recordBossAttempt`의 반환 레코드 타입(`({PackProgress progress, bool justCleared, VocabPack? nextUnlocked})`)을 Task 3에서 그대로 사용. `DiagnosticsService.logBreadcrumb(String)` 시그니처를 Task 3에서 실제 정의(diagnostics_service.dart:136)와 일치시킴. `ReviewDeckService.allReviewableOverride`(Task 4가 만듦)는 다른 태스크가 소비하지 않음(Task 5는 별도 소스 패턴 검증이라 이 훅에 의존하지 않음) — 확인 완료.
- **래칫/계약 재확인**: Task 3·7(vocab_pack_screen.dart)과 Task 4·5(review_session_screen.dart)는 같은 파일이지만 함수/행 범위가 겹치지 않는다(각 태스크 diff 를 직접 대조 확인) — 순서대로(3→7, 4→5) 실행하면 충돌 없음. 신규 raw `TextStyle(`/`BorderRadius.circular(`/`InkWell(`/그리드 밖 간격 리터럴을 도입한 태스크는 없음(Task 10이 `SizedBox`/`Center`만 씀 — 전부 기존 허용 패턴). Task 9의 arb 신규 키 2개는 de/en 동시 추가로 계약 충족.
- **실행 순서**: Task 1은 다른 모든 태스크와 파일이 겹치지 않아 아무 때나 먼저 해도 되지만, "나머지를 관측 가능하게 만든다"는 취지상 최우선 배치. Task 4→5, Task 3→7 은 순서 의존(파일 겹침, 후자가 전자의 산출물인 테스트 파일을 확장). 그 외 모든 태스크 쌍은 파일 교집합 0 — 상호 독립적으로 아무 순서로나 실행 가능.
