# W7 PR1 — TTS·오디오 (T1.1–T1.8) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SoriSpeech/TtsService에 idle/resolving/speaking 3단 재생 상태를 배선하고, 남은 무음 표면(3화면)·프리페치 누락(vocab_pack)·햅틱/levelUp 게이팅을 채우고, TTS 로컬 캐시에 80MB LRU 상한을 걸고, 첫 문장 126개(유니크 125개)를 앱 번들로 승격하며, 콘텐츠 변경 시 Firebase Storage 완결성을 CI에서 검증하는 게이트를 추가한다.

**Architecture:** `TtsPlaybackEngine`(엔진, tts_service.dart)이 재생이 실제로 시작되는 유일한 지점에서 `onPlaybackStarted` 콜백을 쏘고, `TtsService.phase`가 이를 받아 idle→speaking을 알린다. `SoriSpeech`(파사드, speakable.dart)는 자기 자신의 3단 `phase`를 갖되, 새 발화 시작 시 즉시 `resolving`으로 세팅하고 `TtsService.phase`를 구독해 실제 재생 시작 순간에만 `speaking`으로 승격한다 — "해석 시작 전에 이미 재생 중으로 보인다"는 기존 결함을 두 레이어 모두에서 고친다. 나머지 6태스크는 이 파사드 위에 기존 화면 배선 패턴(prefetch/SoriSpeakable/SoriSpeechIndicator)을 반복 적용하고, 캐시·번들·CI 게이트는 기존 strict/best-effort 이분법과 스코프 선택기 패턴을 복제한다.

**Tech Stack:** Flutter/Dart(flutter_test), Python 3.12(`tool/generate_tts.py`, `unittest`), GitHub Actions(YAML), gcloud CLI(Storage), Firebase Storage/`google-github-actions/auth`.

**Spec:** `C:\Users\vjinn\.claude\plans\c-dev-hangulsori-ko-lernen-app-docs-han-zany-pancake.md` §8.1(T1.1–T1.8)·§8.8(브리프 형식)·§9(룰링 1,4,5)·§4(실행 프로토콜). 이 플랜: `docs/superpowers/plans/2026-09-03-w7-pr1-tts.md`.

## Global Constraints

- 워크트리 전용: `C:\dev\hangulsori\ko_lernen_app_worktrees\w7-pr1-tts-20260903` 밖의 파일 편집 금지.
- 래칫(가드 상한·개수 계약)은 **하향 전용** — 상향은 브리프 결함(R3).
- 기존 테스트 단언 약화·삭제·skip 금지(R2). 단 Task 2의 speaking→phase 전환은 스펙이 명시한 **계약 변경**이라 예외이며 커밋 메시지에 그 사실을 적는다.
- 하드코딩 UI 문자열 금지 — arb 키만. `if`/`else`는 항상 중괄호. 볼륨 숫자 리터럴 신규 삽입 금지(`AudioPolicy.volumeFor`만). arb 신규 키는 **de/en 동시**(R6).
- 셸은 PowerShell 문법(`&&` 금지). Python은 `.venv\Scripts\python.exe`.
- 골든은 Linux CI 정본 — 로컬 검증 불가(이 PR은 골든 영향 없음).
- 커밋: 이 워크트리의 `claude/w7-pr1-tts-20260903` 브랜치에 **태스크별 로컬 커밋 허용**(컨트롤러 룰링 2026-09-03 — 리뷰 패키지·복구 원장의 전제). 푸시·PR·병합은 Jin이 명시 요청할 때만.
- TTS 캐시 키 3중 고정(Dart `tts_cache_key_test.dart`/Python `generate_tts.py`/functions/tts) — `storagePath='tts/v3/{voice}/{hash}.mp3'`, `localFileName='tts_v3_{voice}_{hash}[_r1].mp3'` 형식 변경 금지.

---

## Task 1: `TtsPlaybackEngine.onPlaybackStarted` — 재생 시작 콜백

**Files:**
- Modify: `lib/services/tts_service.dart` — enum 삽입(`class TtsPlaybackEngine` 선언 직전, 현재 L257 부근) / 생성자·필드(L259-278) / 삽입 지점(L383-386 사이) / static `phase` 필드 + 엔진 배선(L519-545) / `TtsService.speak()`의 `whenComplete`(L606-612)
- Test: `test/tts_premium_only_test.dart`

**Interfaces:**
Produces: `enum TtsSpeechPhase { idle, resolving, speaking }`(top-level, `tts_service.dart` — Task 2가 import해 그대로 쓰는 앱 전체 공유 enum) Produces: `TtsPlaybackEngine({..., this.onPlaybackStarted})` / `final VoidCallback? onPlaybackStarted;` Produces: `static final ValueNotifier<TtsSpeechPhase> phase` on `TtsService`(idle 기본, 실제 재생 시작 순간에만 speaking)

**Frozen contracts:**
`test/tts_premium_only_test.dart` 기존 9개 테스트(L99-117·L119-135·L137-158·L160-192·L194-221 포함) 무수정 GREEN. `test/content_audio_policy_guard_test.dart`의 `int\s+_generation`/`TtsService.stop()`/`didPushNext` — 이 태스크는 speakable.dart를 건드리지 않으므로 자동 안전, 인접 가드로 재확인. `onResolutionFailed`/`errorReporter` 시그니처·호출 시점 불변(post-review 계약, L330-333).

- [ ] **Step 1: 실패하는 테스트 작성**

`test/tts_premium_only_test.dart`의 `'프리미엄이 없으면...'` 그룹(L98-222) 뒤에 추가:
```dart
group('재생 시작 콜백(onPlaybackStarted) — post-review T1.1', () {
  test('해석 성공 + startAudio 성공에서만 정확히 1회 불린다', () async {
    final throwingCalls = <void>[];
    final throwingEngine = TtsPlaybackEngine(
      resolveAudio: (text, voice) async => const TtsAudio.path('/tmp/ok.mp3'),
      platform: const _ThrowingStartPlatform(),
      onPlaybackStarted: () => throwingCalls.add(null),
    );
    expect(
      await throwingEngine.speak(text: 'x', voice: 'female', baseRate: 0.42),
      isFalse,
    );
    expect(throwingCalls, isEmpty, reason: '재생-기전 실패에서는 콜백이 불리면 안 된다');
    final startedCalls = <void>[];
    final startingEngine = TtsPlaybackEngine(
      resolveAudio: (text, voice) async => const TtsAudio.path('/tmp/ok.mp3'),
      platform: const _StartingPlatform(),
      onPlaybackStarted: () => startedCalls.add(null),
    );
    expect(
      await startingEngine.speak(text: 'x', voice: 'female', baseRate: 0.42),
      isTrue,
    );
    expect(startedCalls, hasLength(1));
    final missingCalls = <void>[];
    final missingEngine = TtsPlaybackEngine(
      resolveAudio: (text, voice) async => null,
      platform: const _StartingPlatform(),
      onPlaybackStarted: () => missingCalls.add(null),
    );
    expect(
      await missingEngine.speak(text: 'x', voice: 'female', baseRate: 0.42),
      isFalse,
    );
    expect(missingCalls, isEmpty);
  });
});
```
`_ThrowingStartPlatform`(L251-261) 뒤에 새 가짜를 추가(`_RecordingPlatform`은 항상 null 세션을 돌려줘 `onPlaybackStarted` 조건을 못 만든다):
```dart
class _StartingPlatform implements TtsPlaybackPlatform {
  const _StartingPlatform();
  @override
  Future<TtsPlaybackSession?> startAudio(TtsAudio audio, double rate) async {
    return TtsPlaybackSession(Future<bool>.value(true));
  }
  @override
  Future<void> stop() async {}
}
```
- [ ] **Step 2: 실패 확인**

Run: `flutter test --no-pub test/tts_premium_only_test.dart`
Expected: FAIL(컴파일) — `The named parameter 'onPlaybackStarted' isn't defined.`(2곳)

- [ ] **Step 3: 최소 구현**

`class TtsPlaybackEngine {` 바로 위에 enum 추가:
```dart
/// SoriSpeech(파사드)와 TtsService(엔진 배선)가 공유하는 재생 3단계.
enum TtsSpeechPhase { idle, resolving, speaking }
```
생성자/필드(L259-278)에 콜백 추가:
```dart
class TtsPlaybackEngine {
  TtsPlaybackEngine({
    required this.resolveAudio,
    required this.platform,
    this.completionTimeout = const Duration(seconds: 30),
    this.errorReporter,
    this.onResolutionFailed,
    this.onPlaybackStarted,
  });
  final TtsAudioResolver resolveAudio;
  final TtsPlaybackPlatform platform;
  final Duration completionTimeout;
  final TtsErrorReporter? errorReporter;
  // ...(기존 post-review 주석 그대로 유지)...
  final TtsErrorReporter? onResolutionFailed;
  /// 해석 성공 + startAudio 성공(세션 획득)이 둘 다 확정된 직후 정확히
  /// 1회 불린다. 해석 실패·재생-기전 실패에서는 절대 불리지 않는다.
  final VoidCallback? onPlaybackStarted;
  Future<void> _platformTail = Future<void>.value();
  Completer<void>? _cancellation;
  int _generation = 0;
  bool _disposed = false;
```
호출 지점 — 기존 L383-386(`if (session == null || ...) { return false; } bool completed;`)을 교체:
```dart
    if (session == null || _disposed || generation != _generation) {
      return false;
    }
    onPlaybackStarted?.call();
    bool completed;
```
`_playbackEngine`(L523) 바로 위에 static `phase` 추가, 엔진 생성자에 배선:
```dart
  /// [speaking](레거시 bool)과 별개인 3단 재생 상태. `SoriSpeech.phase`가
  /// 이 리스너를 구독해 resolving→speaking 승격 신호로 쓴다(Task 2).
  static final ValueNotifier<TtsSpeechPhase> phase =
      ValueNotifier<TtsSpeechPhase>(TtsSpeechPhase.idle);
  static final TtsPlaybackEngine _playbackEngine = TtsPlaybackEngine(
    resolveAudio: _resolveAudio,
    platform: const _ServicePlaybackPlatform(),
    completionTimeout: _playTimeout,
    errorReporter: (message) => lastError = message,
    onResolutionFailed: (_) {
      if (unavailable.value == null) {
        _reportUnavailable(TtsUnavailableReason.audioUnavailable);
      }
    },
    onPlaybackStarted: () => phase.value = TtsSpeechPhase.speaking,
  );
```
`TtsService.speak()`의 `whenComplete`(L606-612)에 idle 복귀 추가:
```dart
    result.whenComplete(() {
      if (token == _speakToken) {
        speaking.value = false;
        phase.value = TtsSpeechPhase.idle;
        AudioPolicy.instance.noteSpeechEnded();
      }
    });
```
- [ ] **Step 4: 통과 확인 + 인접 가드 + 정적 분석**

Run: `flutter test --no-pub test/tts_premium_only_test.dart`
Expected: PASS — 10개(기존 9 + 신규 1).
Run: `flutter test --no-pub test/content_audio_policy_guard_test.dart` → PASS(무수정)
Run: `flutter analyze --no-pub` → `No issues found!`

- [ ] **Step 5: 커밋(Jin 요청 시에만)**
```powershell
git add lib/services/tts_service.dart test/tts_premium_only_test.dart
git commit -m "feat(tts): 재생 시작 콜백(onPlaybackStarted)과 3단 phase 엔진 배선 추가 (지시서 4.3/4.5 / 스윕 tts-02)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
**Done criteria:** `tts_premium_only_test.dart` 10/10 GREEN · `content_audio_policy_guard_test.dart` 무수정 GREEN · analyze 0 · diff ≤ 60줄(테스트 제외).

**Report format:** ①diffstat ②RED 로그 ③GREEN 로그 ④analyze 0 ⑤인접 가드 ⑥예상 밖 가드 실패(없으면 "없음") ⑦미해결 의문 ≤3줄.

**Open questions:** `TtsService.phase`가 `resolving`을 스스로 쓰지 않는다(엔진은 idle/speaking 2단만 오간다) — 그래도 T1.1/T1.2가 같은 3단 enum 하나를 공유하도록 스펙이 지시해 이 설계를 그대로 따랐다.

---

## Task 2: `SoriSpeech.phase` 승격 — 인디케이터 3단

**Files:**
- Modify: `lib/widgets/sori/speakable.dart` — `speaking`(L50) / `_publishSpeak`(L149-212) / `stop()`(L295-308) / `resetForTesting()`(L80-92) / `SoriSpeechIndicator`(L356-420)
- Modify: `lib/l10n/app_de.arb:1094`(뒤), `app_en.arb:1087`(뒤) — 신규 키 `speechIndicatorResolving`
- Test: `test/speakable_semantics_test.dart`, `test/speakable_screen_lifecycle_test.dart`, `test/sori_speech_dedupe_test.dart`

**Interfaces:**
Consumes: `TtsSpeechPhase`, `TtsService.phase`(Task 1) Produces: `static final ValueNotifier<TtsSpeechPhase> phase` on `SoriSpeech`(신규) — `speaking`(bool)은 남기되 `phase`에서 파생(직접 대입 금지, `phase.value`만 대입) Produces: `AppL10n.speechIndicatorResolving`(de "Wird geladen" / en "Loading")

**Frozen contracts:**
`content_audio_policy_guard_test.dart` 전체 무수정 GREEN — `int\s+_generation`(`ContentSpeechController._generation`, 안 건드림), `'didPushNext'`/`'TtsService.stop()'`(docstring L426-437, **문구 리라이트 금지**), `RegExp(r'Map<String,\s*Future')`. `SoriSpeakable`(L313-342)은 `phase`를 구독하지 않는다(§4 계약 — 플립 카드 전용). **계약 변경(정당화됨, R2 예외):** `speakable_semantics_test.dart` L55·L94, `speakable_screen_lifecycle_test.dart` L125-139(탭 직후 아이콘 기대값), `sori_speech_dedupe_test.dart` L49-53 — bool `speaking` 직접 대입/단언을 enum `phase` 기준으로 바꾼다.

- [ ] **Step 1: 실패하는 테스트 작성**

`speakable_semantics_test.dart` 최상단에 `import 'dart:async';`와 `import 'package:ko_lernen_app/services/tts_service.dart';` 추가. L55와 L94의 `SoriSpeech.speaking.value = true;`를 각각 `SoriSpeech.phase.value = TtsSpeechPhase.speaking;`로 교체. 세 번째 테스트 뒤에 추가:
```dart
  testWidgets('해석 중(resolving)에는 로딩 값을 읽어주고 아이콘이 hourglass다', (
    tester,
  ) async {
    final semantics = tester.ensureSemantics();
    final completion = Completer<bool>();
    SoriSpeech.speakImpl = (text, voice) => completion.future;
    await tester.pumpWidget(host());
    await tester.tap(find.byType(SoriSpeechIndicator));
    await tester.pump();
    final t = await AppL10n.delegate.load(const Locale('de'));
    final node = tester.getSemantics(
      find.bySemanticsLabel(t.speechIndicatorLabel),
    );
    expect(node.getSemanticsData().value, t.speechIndicatorResolving);
    expect(find.byIcon(Icons.hourglass_top_rounded), findsOneWidget);
    completion.complete(true);
    await tester.pump();
    semantics.dispose();
  });
```
`speakable_screen_lifecycle_test.dart` 최상단에 `import 'package:ko_lernen_app/services/tts_service.dart';` 추가. `'화면 dispose 뒤 발화 완료는...'`(L125-139)를 교체:
```dart
  testWidgets('화면 dispose 뒤 발화 완료는 UI 예외를 만들지 않는다', (tester) async {
    final completion = Completer<bool>();
    SoriSpeech.speakImpl = (text, voice) => completion.future;
    await tester.pumpWidget(host());
    await tester.tap(find.byType(SoriSpeechIndicator));
    await tester.pump();
    expect(
      find.byIcon(Icons.hourglass_top_rounded),
      findsOneWidget,
      reason: '해석이 끝나기 전까지는 resolving 아이콘이어야 한다',
    );
    TtsService.phase.value = TtsSpeechPhase.speaking;
    await tester.pump();
    expect(find.byIcon(Icons.graphic_eq_rounded), findsOneWidget);
    await tester.pumpWidget(const SizedBox.shrink());
    completion.complete(true);
    await tester.pump();
    expect(tester.takeException(), isNull);
  });
```
같은 파일의 `'발화 실패는...'`(L141-156)에서 탭 직후 단언(L148) `find.byIcon(Icons.graphic_eq_rounded)`만 `find.byIcon(Icons.hourglass_top_rounded)`로 교체(실패는 resolving 단계에서 바로 idle로 강등되므로 speaking을 거치지 않는다).

`sori_speech_dedupe_test.dart` 최상단에 `import 'package:ko_lernen_app/services/tts_service.dart';` 추가. L49-53을 교체:
```dart
    expect(
      SoriSpeech.phase.value,
      TtsSpeechPhase.idle,
      reason: 'prefetch 완료 전에는 speaking으로 승격되면 안 된다',
    );
```
- [ ] **Step 2: 실패 확인**

Run: `flutter test --no-pub test/speakable_semantics_test.dart test/speakable_screen_lifecycle_test.dart test/sori_speech_dedupe_test.dart`
Expected: FAIL — `The getter 'phase' isn't defined for the type 'SoriSpeech'.` / `speechIndicatorResolving` 미정의.

- [ ] **Step 3: arb 키 추가**

`app_de.arb` L1094(`"speechIndicatorIdle": "Nicht aktiv",`) 뒤:
```json
  "speechIndicatorResolving": "Wird geladen",
```
`app_en.arb` L1087(`"speechIndicatorIdle": "Not playing",`) 뒤:
```json
  "speechIndicatorResolving": "Loading",
```
- [ ] **Step 4: `SoriSpeech.phase` 도입**

L50을 교체:
```dart
  /// 화면이 구독하는 발화 상태(레거시 호환) — `phase`에서 파생된다.
  static final ValueNotifier<bool> speaking = ValueNotifier<bool>(false);
  /// idle/resolving/speaking 3단. 신규 발화는 즉시 resolving으로 보이고,
  /// 실제 재생이 시작된 순간(TtsService.phase 승격 신호)에만 speaking이
  /// 된다 — 해석 시작 전에 이미 재생 중으로 보이던 기존 결함의 근본 수정.
  static final ValueNotifier<TtsSpeechPhase> phase =
      ValueNotifier<TtsSpeechPhase>(TtsSpeechPhase.idle)
        ..addListener(_syncSpeakingFromPhase);
  static bool _engineListenerBound = false;
  static void _syncSpeakingFromPhase() {
    speaking.value = phase.value == TtsSpeechPhase.speaking;
  }
  /// TtsService.phase(엔진 레이어)가 실제 재생 시작을 알릴 때만 우리 phase를
  /// speaking으로 승격한다. 늦게 도착한 신호가 이미 취소/완료된 발화를
  /// 오염시키지 않도록 활성 키가 있을 때만 반응한다.
  static void _onEnginePhaseChanged() {
    if (TtsService.phase.value != TtsSpeechPhase.speaking) return;
    if (_activeSpeechKey == null) return;
    phase.value = TtsSpeechPhase.speaking;
  }
  static void _bindEngineListenerOnce() {
    if (_engineListenerBound) return;
    _engineListenerBound = true;
    TtsService.phase.addListener(_onEnginePhaseChanged);
  }
```
`resetForTesting()`(L80-92): `speaking.value = false;`를 제거하고 `phase.value = TtsSpeechPhase.idle;`로 교체(리스너가 `speaking`도 자동 복귀시킨다) — 나머지 4줄(`speakImpl`/`speakSlowImpl`/`prefetchImpl`/`stopImpl` 재대입)은 그대로 둔다.

`_publishSpeak`(L149-212) 전체를 교체(변경 지점: 시작부 phase 세팅 하나로 통합, pending 승격 경로의 `speaking.value = true;` 제거, whenComplete의 idle 복귀):
```dart
  static Future<bool> _publishSpeak(
    String key,
    String text,
    String voice,
    Future<bool> Function(String text, String voice) resolver, {
    Future<bool>? pending,
  }) {
    _bindEngineListenerOnce();
    final previousKey = _activeSpeechKey;
    if (previousKey != null && previousKey != key) {
      _cancelFlightAndRestorePrefetch(previousKey);
    }
    final generation = ++_speechGeneration;
    _activeSpeechKey = key;
    // 신규 speak(pending 없음)는 즉시 resolving. pending 승격(이미 진행 중인
    // prefetch에 올라타는 경우)은 그 prefetch가 끝날 때까지 idle로 남는다
    // (기존 bool 계약과 동일). 실제 speaking 전환은 두 경우 모두
    // TtsService.phase 리스너의 승격에서만 일어난다.
    phase.value = pending == null
        ? TtsSpeechPhase.resolving
        : TtsSpeechPhase.idle;
    Future<bool> resolve() async {
      if (previousKey != null && previousKey != key) {
        try {
          await stopImpl();
        } catch (_) {
          // 정지는 best-effort다.
        }
      }
      if (generation != _speechGeneration) return false;
      try {
        if (pending != null) {
          final alreadyPlayed = await pending;
          if (generation != _speechGeneration) return false;
          if (alreadyPlayed) return true;
        }
        return await resolver(text, voice);
      } catch (_) {
        return false;
      }
    }
    final resolved = resolve();
    late final Future<bool> future;
    future = resolved.whenComplete(() {
      _playbackFlights.remove(future);
      _promotionPrefetches.remove(future);
      if (identical(_inFlight[key], future)) {
        _inFlight.remove(key);
      }
      if (generation == _speechGeneration && _activeSpeechKey == key) {
        _activeSpeechKey = null;
        phase.value = TtsSpeechPhase.idle;
      }
    });
    _playbackFlights.add(future);
    if (pending != null) {
      _promotionPrefetches[future] = pending;
    }
    _inFlight[key] = future;
    return future;
  }
```
`stop()`(L295-308)의 `speaking.value = false;`(L302)를 `phase.value = TtsSpeechPhase.idle;`로 교체(나머지 로직 불변).

- [ ] **Step 5: `SoriSpeechIndicator` 3단 렌더**

`SoriSpeechIndicator.build`(L362-419) 전체를 교체:
```dart
  @override
  Widget build(BuildContext context) {
    final s = SoriSurfaces.of(context);
    final t = AppL10n.of(context);
    return ValueListenableBuilder<TtsSpeechPhase>(
      valueListenable: SoriSpeech.phase,
      builder: (context, phase, _) {
        void handleTap() {
          if (phase != TtsSpeechPhase.idle) {
            SoriSpeech.stop();
          } else {
            SoriSpeech.speak(text, voice: voice);
          }
        }
        final semanticsValue = switch (phase) {
          TtsSpeechPhase.idle => t.speechIndicatorIdle,
          TtsSpeechPhase.resolving => t.speechIndicatorResolving,
          TtsSpeechPhase.speaking => t.speechIndicatorSpeaking,
        };
        final icon = switch (phase) {
          TtsSpeechPhase.idle => Icons.volume_up_rounded,
          TtsSpeechPhase.resolving => Icons.hourglass_top_rounded,
          TtsSpeechPhase.speaking => Icons.graphic_eq_rounded,
        };
        final iconAlpha = phase == TtsSpeechPhase.resolving ? 0.6 : 1.0;
        return Semantics(
          button: true,
          label: t.speechIndicatorLabel,
          value: semanticsValue,
          onTap: handleTap,
          // 48x48 터치 타깃 안 44x44 원형 배지(s.surface 0.85) + 18px 아이콘 —
          // 기존 SizedBox/Center/DecoratedBox 구조는 그대로, icon/alpha만 phase값을 따른다.
          child: ExcludeSemantics(
            child: SoriPressable(
              onTap: handleTap,
              child: SizedBox(
                width: SoriLayout.chromeRowTouchHeight,
                height: SoriLayout.chromeRowTouchHeight,
                child: Center(
                  child: SizedBox(
                    width: 44,
                    height: 44,
                    child: DecoratedBox(
                      decoration: BoxDecoration(color: s.surface.withValues(alpha: 0.85), shape: BoxShape.circle),
                      child: Icon(icon, size: 18, color: SoriColors.contentCta.withValues(alpha: iconAlpha)),
                    ),
                  ),
                ),
              ),
            ),
          ),
        );
      },
    );
  }
```
- [ ] **Step 6: 통과 확인 + 인접 가드 + 정적 분석**

Run: `flutter test --no-pub test/speakable_semantics_test.dart test/speakable_screen_lifecycle_test.dart test/sori_speech_dedupe_test.dart`
Expected: PASS 전부.
Run: `flutter test --no-pub test/content_audio_policy_guard_test.dart test/review_session_screen_speakable_test.dart` → PASS(무수정)
Run: `flutter analyze --no-pub` → `No issues found!`

- [ ] **Step 7: 커밋(Jin 요청 시에만)**
```powershell
git add lib/widgets/sori/speakable.dart lib/l10n/app_de.arb lib/l10n/app_en.arb test/speakable_semantics_test.dart test/speakable_screen_lifecycle_test.dart test/sori_speech_dedupe_test.dart
git commit -m "feat(sori): SoriSpeech에 idle/resolving/speaking 3단 phase 승격 (지시서 4.3/4.5 / 스윕 tts-02)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
**Done criteria:** 위 3개 테스트 파일 GREEN(정확한 카운트는 Step 6 로그로 보고) · `content_audio_policy_guard_test.dart`·`review_session_screen_speakable_test.dart` 무수정 GREEN · analyze 0 · arb 키 de/en 쌍.

**Report format:** ①diffstat ②RED 로그 ③GREEN 로그(파일별 카운트) ④analyze 0 ⑤인접 가드 2개 ⑥예상 밖 가드 실패 ⑦미해결 의문 ≤3줄.

**Open questions:** 인디케이터 아이콘 알파(0.6)를 배지가 아닌 아이콘 자체에 건 선택이 "hourglass_top_rounded(반투명 0.6)" 지시와 정확히 일치하는지 설계자 확인 필요.

---

## Task 3: `vocab_pack_screen` 다음 카드/문항 프리페치

**Files:**
- Modify: `lib/screens/vocab_pack_screen.dart` — `_advanceLearn()`(L421-439), `_advanceQuiz()`(L773-803), `_enterQuiz()`(L566-602), `_enterBoss()`(L604-640)
- Test: `test/vocab_pack_screen_prefetch_test.dart`(신규)

**Interfaces:**
- Consumes: `SoriSpeech.prefetchImpl`(`@visibleForTesting`), `LearnSessionQueue<Vocab>.peekNext`(`Vocab?`), `VocabPackScreen({packId, packLoader, siblingPacksLoader, finishOperations})`(하네스는 `test/vocab_pack_advance_timer_test.dart`의 `_pumpPack`/`Vocab` 빌더 패턴 재사용), `tapDeckAction(WidgetTester, String label)`(`test/helpers/deck_actions.dart`)

**Frozen contracts:**
`content_audio_policy_guard_test.dart`의 targetScreens(L12-22)에 이미 `vocab_pack_screen.dart`가 있고 `'TtsService'` 0건이 강제된다 — `SoriSpeech.prefetch`만 호출하므로 자동 안전. 프리페치는 반드시 `voice` 생략(=auto)으로 호출해 `_speakCurrent()`(L686-693)의 실제 speak 키와 dedupe되게 한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`test/vocab_pack_screen_prefetch_test.dart` 신규(하네스는 `vocab_pack_advance_timer_test.dart`의 `_pumpPack`/`Vocab` 필드 그대로):
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:ko_lernen_app/l10n/generated/app_localizations.dart';
import 'package:ko_lernen_app/models/vocab.dart';
import 'package:ko_lernen_app/models/vocab_pack.dart';
import 'package:ko_lernen_app/screens/vocab_pack_screen.dart';
import 'package:ko_lernen_app/services/storage_service.dart';
import 'package:ko_lernen_app/services/vocab_pack_finish_coordinator.dart';
import 'package:ko_lernen_app/theme.dart';
import 'package:ko_lernen_app/widgets/flip_card.dart';
import 'package:ko_lernen_app/widgets/sori/quiz_choice.dart';
import 'package:ko_lernen_app/widgets/sori/speakable.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'helpers/deck_actions.dart';
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  setUp(() async {
    Storage.resetForTesting();
    SharedPreferences.setMockInitialValues(<String, Object>{});
    await Storage.init();
    await Storage.setTutVocabPackSeen();
    await Storage.setTutPackQuizSeen();
    await Storage.setTutPackBossSeen();
    SoriSpeech.resetForTesting();
  });
  tearDown(SoriSpeech.resetForTesting);
  testWidgets('Learn 카드 전진 시 다음 단어를 정확히 1회 프리페치한다', (tester) async {
    final prefetched = <String>[];
    SoriSpeech.prefetchImpl = (text, voice) async => prefetched.add(text);
    final t = await _pumpPack(tester, _pack(count: 3));
    tester.widget<FlipCard>(find.byType(FlipCard)).onTap!();
    await tester.pump();
    await tester.pump(const Duration(milliseconds: 400));
    tapDeckAction(tester, t.vocabPackGotIt);
    await tester.pump();
    expect(prefetched, ['단어1']);
  });
  testWidgets('마지막 Learn 카드에서는 다음 단어가 없어 프리페치하지 않는다', (tester) async {
    final prefetched = <String>[];
    SoriSpeech.prefetchImpl = (text, voice) async => prefetched.add(text);
    final t = await _pumpPack(tester, _pack(count: 1));
    tester.widget<FlipCard>(find.byType(FlipCard)).onTap!();
    await tester.pump();
    await tester.pump(const Duration(milliseconds: 400));
    tapDeckAction(tester, t.vocabPackGotIt);
    await tester.pump();
    expect(prefetched, isEmpty);
  });
  testWidgets('퀴즈 문항 전진 시 다음 문항을 프리페치한다', (tester) async {
    final prefetched = <String>[];
    SoriSpeech.prefetchImpl = (text, voice) async => prefetched.add(text);
    final t = await _pumpPack(tester, _pack(count: 2));
    for (var i = 0; i < 2; i++) {
      tester.widget<FlipCard>(find.byType(FlipCard)).onTap!();
      await tester.pump(const Duration(milliseconds: 400));
      tapDeckAction(tester, t.vocabPackGotIt);
      await tester.pump(const Duration(milliseconds: 400));
    }
    prefetched.clear();
    tester
        .widgetList<QuizChoice>(find.byType(QuizChoice))
        .firstWhere((choice) => choice.isCorrect)
        .onSelected!();
    await tester.pump(const Duration(milliseconds: 900));
    expect(prefetched, contains('단어2'));
  });
}
Future<AppL10n> _pumpPack(WidgetTester tester, VocabPack pack) async {
  await tester.pumpWidget(
    MaterialApp(
      theme: AppTheme.light,
      locale: const Locale('de'),
      supportedLocales: AppL10n.supportedLocales,
      localizationsDelegates: AppL10n.localizationsDelegates,
      routes: <String, WidgetBuilder>{
        '/vocab/result': (_) => const Scaffold(body: Text('pack-result')),
      },
      home: VocabPackScreen(
        packId: pack.id,
        packLoader: (_) async => pack,
        siblingPacksLoader: (_) async => <VocabPack>[pack],
        finishOperations: _NoopFinishOperations(),
      ),
    ),
  );
  await tester.pump();
  await tester.pump(const Duration(milliseconds: 300));
  return AppL10n.delegate.load(const Locale('de'));
}
VocabPack _pack({required int count}) => VocabPack(
  id: 'a1_prefetch_$count',
  level: 'A1',
  words: <Vocab>[
    for (var index = 0; index < count; index++)
      Vocab(
        id: 'pf-$count-$index',
        korean: '단어${index + 1}',
        romanization: 'daneo${index + 1}',
        german: 'Wort-${index + 1}',
        english: 'word-${index + 1}',
        level: 'A1',
        posDe: 'Nomen',
        exampleKorean: '',
        exampleGerman: '',
        topic: 'test',
        packId: 'a1_prefetch_$count',
        packOrder: index + 1,
      ),
  ],
);
class _NoopFinishOperations implements VocabPackFinishOperations {
  @override
  Future<VocabPackFinishOutcome> recordBossAttempt(
    VocabPackFinishRequest request,
  ) async => const VocabPackFinishOutcome(
    justCleared: false,
    nextUnlockedPackId: null,
  );
  @override
  Future<void> recordCourseAttempt(VocabPackFinishRequest request) async {}
  @override
  Future<void> awardXp(VocabPackFinishRequest request) async {}
  @override
  Future<void> recordCompletionStamp(
    VocabPackFinishRequest request,
    VocabPackFinishOutcome outcome,
  ) async {}
  @override
  Future<void> persistPendingState(
    VocabPackFinishRequest request,
    VocabPackFinishOutcome outcome,
  ) async {}
}
```
- [ ] **Step 2: 실패 확인**

Run: `flutter test --no-pub test/vocab_pack_screen_prefetch_test.dart`
Expected: FAIL — 1번·3번 테스트 RED(`prefetched`가 빈 리스트).

- [ ] **Step 3: 최소 구현**

`_advanceLearn()`(L421-439)의 `setState` 블록 직후:
```dart
    final next = _learnQueue?.peekNext;
    if (next != null) {
      // ignore: discarded_futures
      SoriSpeech.prefetch(next.korean);
    }
```
`_advanceQuiz()`(L773-803)의 `_prepareNextQuestion();`(L800) 뒤:
```dart
    final list = isQuiz ? _quizQuestions : _bossQuestions;
    if (_qIdx + 1 < list.length) {
      // ignore: discarded_futures
      SoriSpeech.prefetch(list[_qIdx + 1].korean);
    }
```
`_enterQuiz()`(L566-602)의 `_prepareNextQuestion();`(L581) 뒤, `_speakCurrent();`(L583) 앞:
```dart
    if (_quizQuestions.length > 1) {
      // ignore: discarded_futures
      SoriSpeech.prefetch(_quizQuestions[1].korean);
    }
```
`_enterBoss()`(L604-640)의 `_prepareNextQuestion();`(L619) 뒤, `_speakCurrent();`(L621) 앞: 동일 패턴으로 `_bossQuestions`.

- [ ] **Step 4: 통과 확인 + 인접 가드 + 정적 분석**

Run: `flutter test --no-pub test/vocab_pack_screen_prefetch_test.dart` → PASS 3/3.
Run: `flutter test --no-pub test/vocab_pack_advance_timer_test.dart test/content_audio_policy_guard_test.dart` → PASS(무수정)
Run: `flutter analyze --no-pub` → `No issues found!`

- [ ] **Step 5: 커밋(Jin 요청 시에만)**
```powershell
git add lib/screens/vocab_pack_screen.dart test/vocab_pack_screen_prefetch_test.dart
git commit -m "feat(vocab-pack): Learn/Quiz/Boss 다음 카드·문항 오디오 프리페치 배선 (지시서 4.5 / 스윕 tts-03)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
**Done criteria:** `vocab_pack_screen_prefetch_test.dart` 3/3 GREEN · 인접 2개 무수정 GREEN · analyze 0.

**Report format:** ①diffstat ②RED 로그 ③GREEN 로그 ④analyze 0 ⑤인접 가드 ⑥예상 밖 가드 실패 ⑦미해결 의문 ≤3줄.

---

## Task 4: 무음 표면 3곳 — vocab_notebook_result/studio, chosung_quiz

**Files:**
- Modify: `lib/screens/vocab_notebook_result_screen.dart`(import + L205), `lib/screens/vocab_notebook_studio_screen.dart`(import + L221), `lib/screens/chosung_quiz_screen.dart`(import + `_QuizCard.build` L1062-1097)
- Modify: `test/content_audio_policy_guard_test.dart:12-22`(targetScreens에 3파일)
- Test: `test/vocab_notebook_audio_test.dart`(신규), `test/chosung_quiz_audio_test.dart`(신규)

**Interfaces:** Consumes `SoriSpeakable({required text, required child, voice})`, `SoriSpeech.speakImpl`.

**Frozen contracts:**
`SoriSpeakable`은 플립 카드 금지(§4 계약) — 이 3화면의 단어 행/카드는 플립 카드가 아니므로 위반 아님. `chosung_quiz_screen.dart`의 `_State.waiting`(L1089-1097)은 정답 단어를 아직 노출 안 함 — **waiting 분기에 오디오 절대 추가 금지**(정답 유출 방지). correct/wrong(L1062-1087)에만 추가. targetScreens에 화면을 추가하기 **전에** 그 화면 소스에 `'TtsService'` 0건을 먼저 확인(순서 뒤바뀌면 가드 즉시 RED) — 세 화면 모두 원래 미import이므로 안전.

- [ ] **Step 1: 실패하는 테스트 작성**

`test/chosung_quiz_audio_test.dart` 신규:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:ko_lernen_app/l10n/generated/app_localizations.dart';
import 'package:ko_lernen_app/models/vocab.dart';
import 'package:ko_lernen_app/screens/chosung_quiz_screen.dart';
import 'package:ko_lernen_app/theme.dart';
import 'package:ko_lernen_app/widgets/sori/speakable.dart';
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  setUp(SoriSpeech.resetForTesting);
  tearDown(SoriSpeech.resetForTesting);
  Widget host() => MaterialApp(
    theme: AppTheme.light,
    locale: const Locale('de'),
    supportedLocales: AppL10n.supportedLocales,
    localizationsDelegates: AppL10n.localizationsDelegates,
    home: ChosungQuizScreen(
      deck: <Vocab>[
        Vocab(
          id: 'cq-1',
          korean: '사과',
          romanization: 'sagwa',
          german: 'Apfel',
          english: 'apple',
          level: 'A1',
          posDe: 'Nomen',
          exampleKorean: '',
          exampleGerman: '',
          topic: 'test',
        ),
      ],
    ),
  );
  testWidgets('정답 전(waiting)에는 정답 단어를 재생할 수단이 없다', (tester) async {
    await tester.pumpWidget(host());
    await tester.pump();
    expect(find.byType(SoriSpeakable), findsNothing);
  });
  testWidgets('정답을 맞히면 카드 탭으로 발음을 재생할 수 있다', (tester) async {
    final spoken = <String>[];
    SoriSpeech.speakImpl = (text, voice) async {
      spoken.add(text);
      return true;
    };
    await tester.pumpWidget(host());
    await tester.pump();
    await tester.enterText(find.byType(TextField), '사과');
    await tester.testTextInput.receiveAction(TextInputAction.done);
    await tester.pump();
    expect(find.byType(SoriSpeakable), findsOneWidget);
    await tester.tap(find.byType(SoriSpeakable));
    await tester.pump();
    expect(spoken, ['사과']);
  });
}
```
`test/vocab_notebook_audio_test.dart`는 기존 `test/vocab_notebook_result_screen_test.dart`·`test/vocab_notebook_studio_screen_test.dart`의 `pumpWidget` 호출부(정확한 위젯 생성자 인자)를 그대로 복사해 두 화면을 띄운 뒤, `SoriSpeech.speakImpl` 훅으로 카드 탭 시 `spoken`이 채워지는지 검증한다(단어 카드 탭 → `spoken == ['학교']`류). 정확한 생성자 인자는 구현자가 두 기존 테스트 파일을 열어 그대로 옮긴다(아래 "Open questions" 참고).

- [ ] **Step 2: 실패 확인**

Run: `flutter test --no-pub test/chosung_quiz_audio_test.dart`
Expected: FAIL — 두 번째 테스트에서 `find.byType(SoriSpeakable)`가 0개.

- [ ] **Step 3: 최소 구현**

`vocab_notebook_result_screen.dart` import에 `import '../widgets/sori/speakable.dart';` 추가. L205을 교체:
```dart
                  SoriSpeakable(
                    text: pair.korean,
                    child: Text(
                      pair.korean,
                      style: SoriTextTheme.of(context).h3,
                    ),
                  ),
```
`vocab_notebook_studio_screen.dart` 동일 import 추가. L221을 교체:
```dart
                        SoriSpeakable(
                          text: word.korean,
                          child: Text(
                            word.korean,
                            style: SoriTextTheme.of(context).h3,
                          ),
                        ),
```
`chosung_quiz_screen.dart` import에 `import '../widgets/sori/speakable.dart';` 추가. `_QuizCard.build`의 `switch (state)`(L1062-1098)에서 correct/wrong만 교체:
```dart
            switch (state) {
              _State.correct => SoriSpeakable(
                text: word,
                child: Text(
                  word,
                  style: const TextStyle(
                    fontSize: 22,
                    fontWeight: FontWeight.w800,
                    color: SoriColors.success,
                  ),
                ),
              ),
              _State.wrong => SoriSpeakable(
                text: word,
                child: Column(
                  children: [
                    Text(
                      t.chosungAnswerLabel(word),
                      style: const TextStyle(
                        fontSize: 19,
                        fontWeight: FontWeight.w700,
                        color: SoriColors.danger,
                      ),
                    ),
                    const SizedBox(height: 4),
                    Text(
                      translation,
                      style: TextStyle(fontSize: 14, color: s.textMuted),
                    ),
                  ],
                ),
              ),
              // waiting에는 정답 단어가 아직 화면에 없으므로 SoriSpeakable을
              // 절대 붙이지 않는다(정답 유출 방지).
              _State.waiting => Text(
                translation,
                textAlign: TextAlign.center,
                style: TextStyle(
                  fontSize: 20,
                  color: s.text,
                  fontWeight: FontWeight.w700,
                ),
              ),
            },
```
`content_audio_policy_guard_test.dart`의 `targetScreens`(L12-22)에 3줄 추가:
```dart
    'lib/screens/vocab_notebook_result_screen.dart',
    'lib/screens/vocab_notebook_studio_screen.dart',
    'lib/screens/chosung_quiz_screen.dart',
```
- [ ] **Step 4: 통과 확인 + 인접 가드 + 정적 분석**

Run: `flutter test --no-pub test/vocab_notebook_audio_test.dart test/chosung_quiz_audio_test.dart test/content_audio_policy_guard_test.dart` → PASS 전부.
Run: `flutter test --no-pub test/vocab_notebook_result_screen_test.dart test/vocab_notebook_studio_screen_test.dart` → PASS(무수정, `SoriSpeakable` 래핑이 기존 `find.text(...)` 탐색을 안 깸)
Run: `flutter analyze --no-pub` → `No issues found!`

- [ ] **Step 5: 커밋(Jin 요청 시에만)**
```powershell
git add lib/screens/vocab_notebook_result_screen.dart lib/screens/vocab_notebook_studio_screen.dart lib/screens/chosung_quiz_screen.dart test/content_audio_policy_guard_test.dart test/vocab_notebook_audio_test.dart test/chosung_quiz_audio_test.dart
git commit -m "feat(audio): notebook 결과·스튜디오·초성퀴즈 정답 카드에 탭 재생 배선 (지시서 1.1 / 스윕 tts-04)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
**Done criteria:** 신규 2파일 + guard 테스트 GREEN · 인접 2개 무수정 GREEN · analyze 0.

**Report format:** ①diffstat ②RED 로그 ③GREEN 로그 ④analyze 0 ⑤인접 가드 2개 ⑥예상 밖 가드 실패 ⑦미해결 의문 ≤3줄.

**Open questions:** `vocab_notebook_audio_test.dart`의 정확한 `pumpWidget` 인자는 기존 두 테스트 파일에서 그대로 복사해야 하는데, 이 플랜 작성 세션에서 그 두 파일을 직접 열어보지 못했다 — 구현자가 Step 1에서 확인 후 채운다.

---

## Task 5: 커스텀팩 타이핑 햅틱 + 마일스톤 levelUp SFX

**Files:**
- Modify: `lib/screens/custom_pack_typing_screen.dart`(import + `_submit()` L141-170)
- Modify: `lib/widgets/sori/milestone_celebration.dart`(import + `showMilestoneCelebration` L19-30)
- Modify: `lib/services/sound_service.dart`(테스트 시임 `playImpl` 추가)
- Test: `test/custom_pack_typing_haptic_test.dart`(신규), `test/milestone_feedback_widget_test.dart`(확장)

**Interfaces:** Produces `SoundService.playImpl`(`@visibleForTesting static void Function(String asset)?`) + `SoundService.resetForTesting()` — 기존 5개 공개 메서드 시그니처 불변.

**Frozen contracts:**
`custom_pack_quiz_screen.dart:156-165`의 "정답: lightImpact→correct(), 오답: mediumImpact→wrong()" 순서를 복제(형제 화면 일관성). `MilestoneType`은 `streak/level/vocab` 3종 — **level에만** levelUp SFX. `AudioPolicy`/`SoundService._play`의 볼륨 게이팅 불변 — 새 볼륨 리터럴 금지.

- [ ] **Step 1: 실패하는 테스트 작성**

`test/custom_pack_typing_haptic_test.dart` 신규(팩 시딩은 `test/custom_pack_flipgate_test.dart`의 `kl_custom_packs_v1` SharedPreferences 패턴 재사용):
```dart
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:ko_lernen_app/l10n/generated/app_localizations.dart';
import 'package:ko_lernen_app/screens/custom_pack_typing_screen.dart';
import 'package:ko_lernen_app/services/storage_service.dart';
import 'package:ko_lernen_app/theme.dart';
import 'package:shared_preferences/shared_preferences.dart';
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  const packId = 'cp_typing_haptic';
  final packJson = <String, Object?>{
    packId: {
      'name': 'Typing Haptic Test',
      'sourcePageId': 'page_test',
      'createdAt': '2026-01-01T00:00:00.000Z',
      'words': [
        {
          'korean': '학교',
          'romanization': 'hakgyo',
          'pos_de': 'N.',
          'translation_de': 'Schule',
          'translation_en': 'School',
          'example_korean': '',
          'example_de': '',
          'definition_ko': '',
          'image_path': '',
          'saved_to_pack_id': null,
        },
      ],
    },
  };
  setUp(() async {
    SharedPreferences.setMockInitialValues({
      'kl_user_level': 'a1',
      'kl_custom_packs_v1': packJson,
    });
    await Storage.init();
  });
  Widget host() => MaterialApp(
    theme: AppTheme.light,
    locale: const Locale('de'),
    supportedLocales: AppL10n.supportedLocales,
    localizationsDelegates: AppL10n.localizationsDelegates,
    home: const CustomPackTypingScreen(packId: packId, speaker: null),
  );
  Future<List<String>> _tapAndCollectHaptics(
    WidgetTester tester,
    String input,
  ) async {
    final types = <String>[];
    tester.binding.defaultBinaryMessenger.setMockMethodCallHandler(
      SystemChannels.platform,
      (call) async {
        if (call.method == 'HapticFeedback.vibrate') {
          types.add('${call.arguments}');
        }
        return null;
      },
    );
    addTearDown(
      () => tester.binding.defaultBinaryMessenger.setMockMethodCallHandler(
        SystemChannels.platform,
        null,
      ),
    );
    await tester.pumpWidget(host());
    await tester.pump();
    await tester.enterText(find.byType(TextField), input);
    await tester.testTextInput.receiveAction(TextInputAction.done);
    await tester.pump();
    return types;
  }
  testWidgets('정답 제출은 lightImpact 햅틱을 낸다', (tester) async {
    final types = await _tapAndCollectHaptics(tester, '학교');
    expect(types, contains('HapticFeedbackType.lightImpact'));
  });
  testWidgets('오답 제출은 mediumImpact 햅틱을 낸다', (tester) async {
    final types = await _tapAndCollectHaptics(tester, '틀림');
    expect(types, contains('HapticFeedbackType.mediumImpact'));
  });
}
```
`test/milestone_feedback_widget_test.dart` 상단에 `import 'package:ko_lernen_app/services/sound_service.dart';` 추가. 파일 끝(마지막 `testWidgets` 뒤, `main()` 닫는 `}` 앞)에 추가:
```dart
  Future<void> openMilestone(
    WidgetTester tester,
    Milestone milestone,
    Key key,
  ) async {
    final completion = FeedbackCompletion.milestone(
      createId: () => 'w-${milestone.id}',
      milestoneId: milestone.id,
      milestoneType: milestone.type.name,
      value: milestone.value,
    );
    await tester.pumpWidget(
      _feedbackHost(
        Builder(
          builder: (context) => Scaffold(
            body: Center(
              child: ElevatedButton(
                key: key,
                onPressed: () => showMilestoneCelebration(
                  context,
                  milestone,
                  feedbackContext: completion.context,
                ),
                child: const Text('open'),
              ),
            ),
          ),
        ),
      ),
    );
    await tester.tap(find.byKey(key));
    await tester.pump();
  }
  testWidgets('SoundService.levelUp은 level 마일스톤에서만 불린다', (tester) async {
    final played = <String>[];
    SoundService.playImpl = played.add;
    addTearDown(SoundService.resetForTesting);
    await openMilestone(
      tester,
      const Milestone(MilestoneType.level, 5),
      const Key('open-level-celebration'),
    );
    expect(played, ['sfx/levelup.wav']);
    played.clear();
    await openMilestone(
      tester,
      const Milestone(MilestoneType.streak, 7),
      const Key('open-streak-celebration'),
    );
    expect(played, isEmpty, reason: 'streak 마일스톤은 levelUp SFX를 내면 안 된다');
  });
```
- [ ] **Step 2: 실패 확인**

Run: `flutter test --no-pub test/custom_pack_typing_haptic_test.dart test/milestone_feedback_widget_test.dart`
Expected: FAIL(컴파일) — `SoundService.playImpl`/`resetForTesting` 미정의; 햅틱 리스트 비어 있음.

- [ ] **Step 3: 최소 구현**

`lib/services/sound_service.dart`의 클래스 본문 상단(`static bool get enabled ...` 바로 뒤)에 시임을 추가 — 공개 5메서드·볼륨 게이팅 로직·나머지 구조는 손대지 않는다:
```dart
  /// 테스트 시임 — 설정되면 실제 AudioPlayer/에셋 없이 재생 요청을 기록만
  /// 한다(`SoriSpeech.speakImpl`과 같은 패턴). 프로덕션은 null.
  @visibleForTesting
  static void Function(String asset)? playImpl;
  @visibleForTesting
  static void resetForTesting() {
    playImpl = null;
  }
```
`_play(String asset)` 본문 맨 앞(`if (kIsWeb) { return; }` 앞)에 시임 분기를 삽입:
```dart
  static Future<void> _play(String asset) async {
    final hook = playImpl;
    if (hook != null) {
      hook(asset);
      return;
    }
    if (kIsWeb) {
      return;
    }
    // ...(나머지 AudioPlayer 재생 로직은 그대로)...
```
`import 'package:flutter/foundation.dart';`(`@visibleForTesting`용)가 이미 최상단에 있으므로 import 추가는 불필요.

`custom_pack_typing_screen.dart` import에 `import 'package:flutter/services.dart';` 추가. `_submit()`(L141-170)의 `if (ok) { SoundService.correct(); } else { SoundService.wrong(); }`을 교체:
```dart
    if (ok) {
      HapticFeedback.lightImpact();
      SoundService.correct();
    } else {
      HapticFeedback.mediumImpact();
      SoundService.wrong();
    }
```
`milestone_celebration.dart` import에 `import '../../services/sound_service.dart';` 추가. `showMilestoneCelebration`의 `SoriCelebration.burst(context);` 다음 줄:
```dart
  if (milestone.type == MilestoneType.level) {
    SoundService.levelUp();
  }
```
- [ ] **Step 4: 통과 확인 + 인접 가드 + 정적 분석**

Run: `flutter test --no-pub test/custom_pack_typing_haptic_test.dart test/milestone_feedback_widget_test.dart` → PASS 전부.
Run: `flutter test --no-pub test/audio_policy_guard_test.dart` → PASS(무수정, 볼륨 리터럴 래칫 확인)
Run: `flutter analyze --no-pub` → `No issues found!`

- [ ] **Step 5: 커밋(Jin 요청 시에만)**
```powershell
git add lib/screens/custom_pack_typing_screen.dart lib/widgets/sori/milestone_celebration.dart lib/services/sound_service.dart test/custom_pack_typing_haptic_test.dart test/milestone_feedback_widget_test.dart
git commit -m "feat(feedback): 타이핑 게임 정답/오답 햅틱 + level 마일스톤 levelUp SFX 배선 (지시서 2.x / 스윕 tts-05)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
**Done criteria:** 두 테스트 파일 GREEN · `SoundService` 공개 API 시그니처 불변 · analyze 0.

**Report format:** ①diffstat ②RED 로그 ③GREEN 로그 ④analyze 0 ⑤인접 가드 ⑥예상 밖 가드 실패 ⑦미해결 의문 ≤3줄.

---

## Task 6: TTS 로컬 캐시 80MB LRU 상한

**Files:**
- Modify: `lib/services/tts_service.dart` — `_maxBytes` 옆 새 상수(L515 부근), `_cacheAndWrap`(L904-918), 새 prune 함수(`clearCacheStrict` 뒤, L1059 부근)
- Test: `test/tts_cache_prune_test.dart`(신규)

**Interfaces:**
Produces: `static const int _maxCacheTotalBytes = 80 * 1024 * 1024;`(§9 룰링 4) Produces: `static Future<int> pruneCacheStrict({Directory? directory, int? maxBytes})` — mtime 오름차순 삭제, `.part` 절대 보존, `.mp3`로 끝나는 모든 캐시 파일(`_r1` 포함) 대상, 반환=삭제 바이트, 디렉터리 조회 실패는 전파. Produces: `static Future<void> pruneCacheBestEffort({Directory? directory, int? maxBytes})` — 모든 예외를 삼킨다(`clearCache`/`clearCacheStrict` 이분법 복제).

**Frozen contracts:**
`tts_cache_key_test.dart`의 `storagePath`/`localFileName` 정확한 문자열 불변 — prune은 이 포맷만 신뢰(재정의 금지). `account_cleanup_test.dart`의 `clearCacheStrict`/`clearCache` 4개 테스트 무수정 GREEN — strict throws / ordinary swallows 계약을 prune도 복제. `_maxBytes`(L515, 5MB)는 Storage 단일 오브젝트 상한 — 총 캐시 예산과 혼동 금지(별도 상수). `_cacheDir`(정적 캐싱 필드)에 새 prune 함수가 의존 금지 — `directory` 매개변수만.

- [ ] **Step 1: 실패하는 테스트 작성**

`test/tts_cache_prune_test.dart` 신규(`Directory.systemTemp.createTemp` 실파일 샌드박스 — `test/crop_recovery_test.dart` 관례 재사용):
```dart
import 'dart:io';
import 'package:flutter_test/flutter_test.dart';
import 'package:ko_lernen_app/services/tts_service.dart';
void main() {
  late Directory sandbox;
  setUp(() async {
    sandbox = await Directory.systemTemp.createTemp('tts_cache_prune_');
  });
  tearDown(() async {
    await sandbox.delete(recursive: true);
  });
  Future<void> writeCacheFile(String name, int bytes, DateTime modified) async {
    final file = File('${sandbox.path}${Platform.pathSeparator}$name');
    await file.writeAsBytes(List<int>.filled(bytes, 0));
    await file.setLastModified(modified);
  }
  bool exists(String name) =>
      File('${sandbox.path}${Platform.pathSeparator}$name').existsSync();
  test('예산 초과 시 오래된 mp3부터 지우고 .part는 절대 지우지 않는다', () async {
    final now = DateTime.now();
    await writeCacheFile(
      'tts_v3_female_aaaa.mp3',
      500,
      now.subtract(const Duration(minutes: 10)),
    );
    await writeCacheFile(
      'tts_v3_male_bbbb_r1.mp3',
      500,
      now.subtract(const Duration(minutes: 5)),
    );
    await writeCacheFile('tts_v3_female_cccc.mp3', 500, now);
    await writeCacheFile(
      'tts_v3_female_dddd.mp3.part',
      500,
      now.subtract(const Duration(minutes: 20)),
    );
    final freed = await TtsService.pruneCacheStrict(
      directory: sandbox,
      maxBytes: 700,
    );
    expect(freed, 1000, reason: '700 예산을 맞추려면 가장 오래된 2개(1000바이트)를 지워야 한다');
    expect(exists('tts_v3_female_aaaa.mp3'), isFalse);
    expect(
      exists('tts_v3_male_bbbb_r1.mp3'),
      isFalse,
      reason: '_r1 복구 접미사 파일도 캐시 엔트리로 인식돼야 한다',
    );
    expect(exists('tts_v3_female_cccc.mp3'), isTrue, reason: '가장 최신 파일은 남아야 한다');
    expect(
      exists('tts_v3_female_dddd.mp3.part'),
      isTrue,
      reason: '진행 중인 원자적 쓰기를 prune이 지우면 안 된다',
    );
  });
  test('총량이 예산 이하면 아무것도 지우지 않는다', () async {
    await writeCacheFile('tts_v3_female_aaaa.mp3', 100, DateTime.now());
    final freed = await TtsService.pruneCacheStrict(
      directory: sandbox,
      maxBytes: 1000,
    );
    expect(freed, 0);
  });
  test('strict 버전은 디렉터리 조회 실패를 전파하고 best-effort는 삼킨다', () async {
    final missing = Directory(
      '${sandbox.path}${Platform.pathSeparator}missing_subdir',
    );
    await expectLater(
      TtsService.pruneCacheStrict(directory: missing, maxBytes: 0),
      throwsA(isA<FileSystemException>()),
    );
    await TtsService.pruneCacheBestEffort(directory: missing, maxBytes: 0);
  });
}
```
- [ ] **Step 2: 실패 확인**

Run: `flutter test --no-pub test/tts_cache_prune_test.dart`
Expected: FAIL(컴파일) — `The method 'pruneCacheStrict' isn't defined for the type 'TtsService'.`

- [ ] **Step 3: 최소 구현**

`_maxBytes`(L515) 아래에 상수 + 스로틀 카운터 추가:
```dart
  static const int _maxBytes = 5 * 1024 * 1024; // 5MB/파일 상한
  /// 로컬 TTS mp3 캐시 디렉터리 총량 상한(§9 룰링 4 — 확정 80MB). [_maxBytes]
  /// 와 혼동 금지 — 그건 단일 오브젝트 다운로드 상한이다.
  static const int _maxCacheTotalBytes = 80 * 1024 * 1024;
  static int _cacheWritesSincePrune = 0;
  static DateTime? _lastPruneAt;
```
`_cacheAndWrap`(L904-918)의 `await _writeAtomically(file, data);` 뒤에 스로틀 훅 추가:
```dart
    await _writeAtomically(file, data);
    _maybePruneCache(file.parent);
    return TtsAudio.path(file.path);
  }
  /// 매 쓰기마다 전체 스캔하면 캐시가 커질수록 재생 지연에 영향을 준다 —
  /// 쓰기 16회 또는 마지막 prune 후 5분 중 먼저 오는 조건에서만 스캔한다
  /// (§9 룰링 4). best-effort — prune 실패가 재생 경로를 막지 않는다.
  static void _maybePruneCache(Directory directory) {
    _cacheWritesSincePrune++;
    final last = _lastPruneAt;
    final dueByCount = _cacheWritesSincePrune >= 16;
    final dueByTime =
        last == null ||
        DateTime.now().difference(last) >= const Duration(minutes: 5);
    if (!dueByCount && !dueByTime) {
      return;
    }
    _cacheWritesSincePrune = 0;
    _lastPruneAt = DateTime.now();
    unawaited(pruneCacheBestEffort(directory: directory));
  }
```
`clearCacheStrict`(L1048-1059) 뒤에 prune strict/best-effort 쌍 + 헬퍼 클래스 추가:
```dart
  /// 디렉터리 총 바이트가 [maxBytes](기본 [_maxCacheTotalBytes])를 넘으면
  /// mtime이 가장 오래된 mp3부터 지워 예산 이하로 맞춘다. `.part`는 삭제
  /// 대상에서 제외한다. 반환값 = 삭제한 바이트. 디렉터리 조회 실패는 전파.
  @visibleForTesting
  static Future<int> pruneCacheStrict({
    Directory? directory,
    int? maxBytes,
  }) async {
    final dir = directory ?? await _strictCacheDirectory();
    final budget = maxBytes ?? _maxCacheTotalBytes;
    final entries = <_TtsCacheFileStat>[];
    var total = 0;
    await for (final entity in dir.list()) {
      if (entity is! File || !entity.path.endsWith('.mp3')) {
        continue;
      }
      final stat = await entity.stat();
      total += stat.size;
      entries.add(_TtsCacheFileStat(entity, stat.size, stat.modified));
    }
    if (total <= budget) {
      return 0;
    }
    entries.sort((a, b) => a.modified.compareTo(b.modified));
    var freed = 0;
    for (final entry in entries) {
      if (total - freed <= budget) {
        break;
      }
      await entry.file.delete();
      freed += entry.bytes;
    }
    return freed;
  }
  /// [pruneCacheStrict]의 best-effort 래퍼(`clearCache`와 같은 이분법).
  static Future<void> pruneCacheBestEffort({
    Directory? directory,
    int? maxBytes,
  }) async {
    try {
      await pruneCacheStrict(directory: directory, maxBytes: maxBytes);
    } catch (_) {
      // best effort
    }
  }
```
파일 하단(`_TtsResolution` 근처)에 값 타입 추가:
```dart
class _TtsCacheFileStat {
  const _TtsCacheFileStat(this.file, this.bytes, this.modified);
  final File file;
  final int bytes;
  final DateTime modified;
}
```
- [ ] **Step 4: 통과 확인 + 인접 가드 + 정적 분석**

Run: `flutter test --no-pub test/tts_cache_prune_test.dart` → PASS 3/3.
Run: `flutter test --no-pub test/account_cleanup_test.dart test/tts_cache_key_test.dart` → PASS(무수정)
Run: `flutter analyze --no-pub` → `No issues found!`

- [ ] **Step 5: 커밋(Jin 요청 시에만)**
```powershell
git add lib/services/tts_service.dart test/tts_cache_prune_test.dart
git commit -m "feat(tts-cache): 로컬 캐시 80MB LRU 상한(mtime 기준, 스로틀 prune) 추가 (지시서 4.6 / 스윕 tts-06)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
**Done criteria:** `tts_cache_prune_test.dart` 3/3 GREEN · 인접 2개 무수정 GREEN · analyze 0.

**Report format:** ①diffstat ②RED 로그 ③GREEN 로그 ④analyze 0 ⑤인접 가드 2개 ⑥예상 밖 가드 실패 ⑦미해결 의문 ≤3줄.

---

## Task 7: 첫 문장 126개(유니크 125개) 앱 번들 승격

**Files:**
- Modify: `tool/generate_tts.py` — `_pubspec_declares_tts_assets`(L229-236), 신규 `download_first_line_bundle`/CLI 모드(L1164·L1280 부근)
- Modify: `pubspec.yaml:139`(assets 3줄), `assets/data/tts_first_line_manifest.json`(스크립트 산출물, 손편집 금지)
- Modify: `test/content_audio_policy_guard_test.dart:110-137`, `test/asset_orphan_guard_test.dart:25-39`, `test/tts_bundled_manifest_test.dart:17-18`(테스트명만)
- Test: `tool/test_generate_tts.py`(신규 1건)

**Interfaces:**
Produces(Python): `def download_first_line_bundle(manifest_items, project_root=ROOT) -> list[str]` — 같은 `storagePath`를 공유하는 항목은 1회만 다운로드(126개 중 유니크 125개). Produces(Python): CLI 모드 `--download-first-line-bundle`(다른 모드와 상호배타).

**Frozen contracts:**
`tts_bundled_manifest_test.dart:195-209`(`_resolveAudio` 티어 순서 문자열 위치) 불변. `tts_bundled_manifest.dart:106-121`(`bundled:false`면 두 필드 null, 위반 시 FormatException) — 매니페스트는 항상 스크립트 산출물이어야 유지된다. `tool/test_generate_tts.py`의 `test_v3_storage_key_matches_flutter_and_function_contract`(L147-152) 무수정 GREEN.

> **이 태스크는 네트워크에 닿을 수 없는 세션에서 실행된다.** gcloud 다운로드는 Jin 머신 전용(아래 Operator steps). 구현자는 스크립트·pubspec·가드를 "125개 다운로드 완료 후"의 최종 상태로 작성하되, 실제 검증은 **가짜 mp3 2개 픽스처**로만 한다. 그 결과 `content_audio_policy_guard_test.dart`의 `bundledCount==125` 단언은 Jin의 Operator steps 전까지 로컬 RED로 남는다 — 이것은 **예상된 인터림 상태**이며 "예상 밖 가드 실패"가 아니다. 보고서에 명시할 것.

- [ ] **Step 1: 실패하는 테스트 작성**

`tool/test_generate_tts.py`에 신규 추가(업로드 테스트 근처):
```python
    def test_download_first_line_bundle_dedupes_shared_storage_paths(self):
        items = [
            {"storagePath": "tts/v3/female/aaa.mp3", "voice": "female", "cacheHashSha1": "aaa"},
            {
                # 두 시나리오가 같은 (voice, text) 첫 문장을 공유(126개 중 125
                # 유니크). 같은 storagePath는 한 번만 다운로드해야 한다.
                "storagePath": "tts/v3/female/aaa.mp3",
                "voice": "female",
                "cacheHashSha1": "aaa",
            },
            {"storagePath": "tts/v3/male/bbb.mp3", "voice": "male", "cacheHashSha1": "bbb"},
        ]
        calls = []
        def fake_run(argv, check=True):
            calls.append(argv)
            target = argv[4]
            with open(target, "wb") as handle:
                handle.write(b"ID3" + bytes(40))
            return subprocess.CompletedProcess(argv, 0)
        with tempfile.TemporaryDirectory() as tmp:
            with (
                patch.object(generate_tts.shutil, "which", return_value="gcloud"),
                patch.object(generate_tts.subprocess, "run", side_effect=fake_run),
            ):
                downloaded = generate_tts.download_first_line_bundle(
                    items, project_root=tmp
                )
        self.assertEqual(len(calls), 2, "중복 storagePath는 한 번만 다운로드해야 한다")
        self.assertEqual(len(downloaded), 2)
```
(`tempfile`이 파일 상단에 없으면 import 추가.)

- [ ] **Step 2: 실패 확인**

Run: `.venv\Scripts\python.exe -m unittest tool.test_generate_tts.TestGenerateTts.test_download_first_line_bundle_dedupes_shared_storage_paths -v`
Expected: FAIL — `AttributeError: module 'generate_tts' has no attribute 'download_first_line_bundle'`

- [ ] **Step 3: 최소 구현(Python)**

`_pubspec_declares_tts_assets`(L229-236)를 교체(female·male 폴더 둘 다 선언돼야 True):
```python
def _pubspec_declares_tts_assets(project_root):
    path = os.path.join(project_root, "pubspec.yaml")
    try:
        with open(path, encoding="utf-8") as handle:
            source = handle.read()
    except OSError:
        return False
    female = re.search(r"(?m)^\s*-\s+assets/tts/v3/female/\s*$", source)
    male = re.search(r"(?m)^\s*-\s+assets/tts/v3/male/\s*$", source)
    return bool(female and male)
```
`remote_cache_paths()`(L534-540) 뒤에 다운로드 함수 추가:
```python
def download_first_line_bundle(manifest_items, project_root=ROOT):
    """Download every unique first-line storagePath into assets/tts/<rev>/<voice>/."""
    seen_paths = set()
    downloaded = []
    for item in manifest_items:
        storage_path = item["storagePath"]
        if storage_path in seen_paths:
            continue
        seen_paths.add(storage_path)
        voice = item["voice"]
        digest = item["cacheHashSha1"]
        target = os.path.join(
            project_root, "assets", "tts", TTS_CACHE_REVISION, voice, f"{digest}.mp3"
        )
        os.makedirs(os.path.dirname(target), exist_ok=True)
        tmp = target + ".part"
        subprocess.run(
            gcloud_argv(
                "storage", "cp", f"gs://{BUCKET}/{storage_path}", tmp,
                "--project", PROJECT,
            ),
            check=True,
        )
        with open(tmp, "rb") as handle:
            data = handle.read()
        if len(data) < MIN_REMOTE_MP3_BYTES or not _usable_manifest_mp3(data):
            os.remove(tmp)
            raise ValueError(f"downloaded object is not a usable MP3: {storage_path}")
        os.replace(tmp, target)
        downloaded.append(target)
    return downloaded
```
CLI 모드 그룹(`_parse_args`)에 새 상호배타 옵션 추가:
```python
    modes.add_argument(
        "--download-first-line-bundle",
        action="store_true",
        help="Download every unique first-line-manifest storagePath into assets/tts/ (network; Jin's machine only).",
    )
```
`main()`의 `first_line_path` 분기 뒤에 처리 블록 추가:
```python
    if args.download_first_line_bundle:
        if shutil.which("gcloud") is None and shutil.which("gcloud.cmd") is None:
            raise SystemExit(
                "TTS 실행 중단: gcloud가 PATH에 없습니다. Google Cloud SDK를 "
                "설치하고 Storage 인증을 마친 뒤 다시 실행하세요."
            )
        manifest = build_first_line_manifest(ROOT)
        downloaded = download_first_line_bundle(manifest["items"], ROOT)
        unique_paths = {item["storagePath"] for item in manifest["items"]}
        print(
            f"다운로드 {len(downloaded)}개 (유니크 storagePath {len(unique_paths)}개) "
            f"-> assets/tts/{TTS_CACHE_REVISION}/"
        )
        return 0
```
`pubspec.yaml`의 `- assets/data/`(L139) 뒤에 2줄(빈 `- assets/tts/` 항목은 넣지 않는다 — 컨트롤러 룰링):
```yaml
    - assets/tts/v3/female/
    - assets/tts/v3/male/
```
`content_audio_policy_guard_test.dart`의 `'first-line bundle tier...'`(L110-137)를 교체:
```dart
  test(
    'first-line bundle tier bundles the immutable first-dialog corpus',
    () {
      final manifestFile = File('assets/data/tts_first_line_manifest.json');
      final loaderSource = File(
        'lib/services/tts_bundled_manifest.dart',
      ).readAsStringSync();
      final pubspec = File('pubspec.yaml').readAsStringSync();
      final manifest =
          jsonDecode(manifestFile.readAsStringSync()) as Map<String, dynamic>;
      expect(manifestFile.existsSync(), isTrue);
      expect(manifest['scenarioCount'], 126);
      expect(
        manifest['bundledCount'],
        125,
        reason: '126개 시나리오 중 2개가 (voice,text) 동일한 첫 대사를 공유해 유니크는 125개',
      );
      expect(pubspec, contains('- assets/data/'));
      expect(pubspec, contains('- assets/tts/v3/female/'));
      expect(pubspec, contains('- assets/tts/v3/male/'));
      expect(
        loaderSource,
        contains('assets/data/tts_first_line_manifest.json'),
      );
      expect(loaderSource, isNot(contains('Directory(')));
      expect(loaderSource, isNot(contains('.listSync(')));
    },
  );
```
`asset_orphan_guard_test.dart`의 `dynamicDirs`(L25-39)에 추가:
```dart
    // sha1 파일명이라 lib/ 소스에 리터럴로 안 나온다 — tts_bundled_manifest.dart
    // 가 'assets/tts/' 접두사만 검사(런타임 검증)해 조립한다.
    'assets/tts/v3/female/': 'assets/tts/',
    'assets/tts/v3/male/': 'assets/tts/',
```
`tts_bundled_manifest_test.dart`의 테스트명(L17-18)만 갱신(내용 불변 — `'안녕하세요'`/`'male'`은 126개 첫 문장 중 하나가 아니므로 계속 null):
```dart
  test(
    'checked baseline manifest is valid, cache-hit-aware, and single-flight',
    () async {
```
- [ ] **Step 4: 다운로드 함수 픽스처 검증(네트워크 없이)**

Run: `.venv\Scripts\python.exe -m unittest tool.test_generate_tts.TestGenerateTts.test_download_first_line_bundle_dedupes_shared_storage_paths -v` → PASS.

`assets/tts/v3/{voice}/`에 실제 첫 문장 2개의 `(voice, cacheHashSha1)`로 32바이트 이상 `ID3`로 시작하는 더미 mp3를 만들고 `--write-first-line-manifest`를 재실행해 그 2개 항목만 `bundled: true`가 되는지 확인한 뒤, 픽스처 파일을 삭제한다(전체 125개는 Jin의 Operator steps 이후에만 재현).

- [ ] **Step 5: 정적 분석 + 인접 가드**

Run: `flutter analyze --no-pub` → `No issues found!`(이 태스크는 Dart를 건드리지 않아 회귀만 확인)
Run: `.venv\Scripts\python.exe -m unittest tool.test_generate_tts -v` → 전부 PASS(신규 1건 포함).

- [ ] **Step 6: 커밋(Jin 요청 시에만) — 2회로 분리**

코드/가드(구현자, Operator steps 전에도 가능):
```powershell
git add tool/generate_tts.py tool/test_generate_tts.py pubspec.yaml test/content_audio_policy_guard_test.dart test/asset_orphan_guard_test.dart test/tts_bundled_manifest_test.dart
git commit -m "feat(tts): 첫 문장 126개(유니크 125개) 다운로드 모드 + 앱 번들 승격 (지시서 4.4 / 스윕 tts-07)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
산출물(Jin, Operator steps 완료 후 — 스크립트 산출물이므로 손편집 금지):
```powershell
git add assets/data/tts_first_line_manifest.json assets/tts/v3/
git commit -m "chore(tts): 첫 문장 mp3 125개 + 매니페스트 재생성 (Jin 실행, 지시서 4.4)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
### Operator steps (Jin 머신 — 네트워크·gcloud 인증 필요)
```powershell
gcloud auth login vjinny2@gmail.com
gcloud config set project ko-lernen-app
.venv\Scripts\python.exe tool\generate_tts.py --download-first-line-bundle
.venv\Scripts\python.exe tool\generate_tts.py --write-first-line-manifest assets\data\tts_first_line_manifest.json
.venv\Scripts\python.exe tool\generate_tts.py --check-first-line-manifest assets\data\tts_first_line_manifest.json
flutter test --no-pub test/content_audio_policy_guard_test.dart test/asset_orphan_guard_test.dart test/tts_bundled_manifest_test.dart
Get-ChildItem assets\tts\v3 -Recurse -File | Measure-Object -Property Length -Sum
```
(순서대로: 다운로드 → 매니페스트 재생성 → 드리프트 확인 → Dart 가드 재검증 → 총 용량 기록(예상 ≤6MB, 8MB 초과 시 Fable에 보고).)

**Done criteria(구현자 몫):** `test_generate_tts.py` 신규 테스트 GREEN · analyze 0 · 기존 Python/Dart 가드 전부 GREEN(픽스처 mp3는 삭제된 상태) · `pubspec.yaml` 3줄 추가. `bundledCount==125` 단언은 Operator steps 전까지 로컬 RED가 정상(보고서에 명시).

**Report format:** ①diffstat ②RED 로그 ③GREEN 로그(신규 테스트+픽스처 검증) ④analyze 0 ⑤인접 가드(Python 전체) ⑥"예상된 인터림 RED"로 bundledCount 단언 명시 ⑦미해결 의문 ≤3줄.

**Open questions:** 125개 mp3 총 용량이 8MB를 넘는지는 Jin의 Operator steps 후에만 확인 가능. CI에도 다운로드 모드를 태울지는 설계자 판단 밖 — 이 플랜은 로컬 1회 실행 후 커밋으로 가정했다.

---

## Task 8: CI 콘텐츠 TTS 완결성 게이트 + stale 삭제 커맨드

**Files:**
- Modify: `.github/scripts/ci_scope.py`(`SCOPES`/`TASK_SCOPE` L17-30, `scopes_for_paths` 신규 분기 L119 뒤)
- Modify: `.github/scripts/test_ci_scope.py`(신규 2건)
- Modify: `.github/workflows/ci.yml`(`changes` outputs L87-93에 `content`, `tts-functions-security`(L596-619) 뒤에 신규 잡)
- Modify: `tool/generate_tts.py`(`--delete-stale`/`--confirm-delete`, `delete_remote_objects()`, verify-storage 분기에 `STALE\t{path}`)
- Test: `.github/scripts/test_ci_scope.py`, `tool/test_generate_tts.py`(신규 2건)

**Interfaces:**
Produces(Python): `SCOPES`에 `"content"` 추가 → `_write_outputs`가 `for scope in SCOPES:`로 순회하므로 GH Actions 출력 `content=true/false`가 자동 생성(별도 배선 불필요). Produces(Python): `def delete_remote_objects(paths) -> None`. Produces(YAML): `secrets.GCS_TTS_VERIFY_SA_JSON`(Jin이 등록) — 이 태스크는 비어 있을 때 명시적으로 실패하는 스텝만 만든다.

**Frozen contracts:**
`test_ci_scope.py`의 `test_isolated_product_areas_select_only_their_gate`(`functions/tts/` → `tts`만) 무수정 GREEN — `content` 신설이 기존 분류를 오염시키면 안 됨. `test_verify_storage_mode_reaches_its_own_read_only_branch`(L721-746)·`test_bare_invocation_is_rejected`(L546-554) 무수정 GREEN — `--delete-stale`은 `modes` 그룹 **밖**의 부가 옵션. exit 정책: **missing>0만 1**, stale은 exit code 무영향(§9 룰링 5). 시크릿 부재 시 조용히 스킵/거짓 통과 금지 — 명시적 실패(§9 룰링 5).

- [ ] **Step 1: 실패하는 테스트 작성**

`.github/scripts/test_ci_scope.py`의 `test_isolated_product_areas_select_only_their_gate` 뒤:
```python
    def test_content_shard_change_selects_only_content_gate(self):
        self.assert_enabled(["assets/data/scenarios_a1.json"], "content")
        self.assert_enabled(["assets/data/cloze.json"], "content")
        self.assert_enabled(["assets/data/korean_vocab.csv"], "content")
    def test_tts_function_change_is_not_conflated_with_content_scope(self):
        self.assert_enabled(["functions/tts/index.js"], "tts")
```
`tool/test_generate_tts.py`에 2건 추가(`test_verify_storage_mode_reaches_its_own_read_only_branch` 뒤):
```python
    def test_delete_stale_requires_verify_storage(self):
        with patch("builtins.print"), patch("sys.stderr"):
            with self.assertRaises(SystemExit) as ctx:
                generate_tts.main(["--delete-stale"])
        self.assertNotEqual(ctx.exception.code, 0)
    def test_verify_storage_lists_stale_paths_without_deleting(self):
        pairs = [("female", "안녕하세요")]
        remote_path = generate_tts.cache_relative_path("female", "안녕하세요")
        stale_path = "tts/v3/female/deadbeef00000000000000000000000000000000.mp3"
        with (
            patch.object(generate_tts, "collect", return_value=pairs),
            patch.object(generate_tts.shutil, "which", return_value="gcloud"),
            patch.object(
                generate_tts, "remote_cache_objects",
                return_value={remote_path: 4096, stale_path: 4096},
            ),
            patch.object(generate_tts, "delete_remote_objects") as delete,
            patch.object(generate_tts.subprocess, "run") as run,
            patch("builtins.print") as printed,
        ):
            result = generate_tts.main(["--verify-storage", "--delete-stale"])
        self.assertEqual(result, 0)
        delete.assert_not_called()
        run.assert_not_called()
        stale_lines = [
            call.args[0]
            for call in printed.call_args_list
            if call.args and str(call.args[0]).startswith("STALE\t")
        ]
        self.assertEqual(stale_lines, [f"STALE\t{stale_path}"])
```
- [ ] **Step 2: 실패 확인**

Run: `.venv\Scripts\python.exe -m unittest test_ci_scope -v`(작업 디렉터리 `.github/scripts`)
Expected: FAIL — `content` 스코프가 없어 기존 폴백(`app`)이 대신 켜짐.

Run: `.venv\Scripts\python.exe -m unittest tool.test_generate_tts.TestGenerateTts.test_delete_stale_requires_verify_storage tool.test_generate_tts.TestGenerateTts.test_verify_storage_lists_stale_paths_without_deleting -v`
Expected: FAIL — `unrecognized arguments: --delete-stale` / `AttributeError: ... delete_remote_objects`.

- [ ] **Step 3: 최소 구현(ci_scope.py)**

`SCOPES`/`TASK_SCOPE`(L17-30) 교체:
```python
SCOPES = (
    "app", "website", "book", "gye", "pronunciation", "tts", "auth_cleanup",
    "content",
)
TASK_SCOPE = {
    "full": SCOPES,
    "flutter": ("app",),
    "website": ("website",),
    "book": ("book",),
    "gye": ("gye",),
    "pronunciation": ("pronunciation",),
    "tts": ("tts",),
    "auth-cleanup": ("auth_cleanup",),
    "content": ("content",),
    "release-internal": ("app",),
    "release-website": ("website",),
}
```
`functions/tts/` 분기(L119-121) 뒤에 콘텐츠 샤드 분기 추가:
```python
        if path.startswith("assets/data/scenarios_") and path.endswith(".json"):
            result["content"] = True
            continue
        if path in {
            "assets/data/cloze.json",
            "assets/data/satz_sentences.json",
            "assets/data/smalltalk.json",
            "assets/data/silben_puzzles.json",
            "assets/data/korean_vocab.csv",
            "assets/data/grammar.csv",
        }:
            result["content"] = True
            continue
```
- [ ] **Step 4: 최소 구현(generate_tts.py stale 삭제)**

`remote_cache_paths()` 뒤에 삭제 함수 추가:
```python
def delete_remote_objects(paths):
    """Permanently remove the given immutable v3 objects from Storage."""
    if not paths:
        return
    subprocess.run(
        gcloud_argv(
            "storage", "rm",
            *(f"gs://{BUCKET}/{path}" for path in sorted(paths)),
            "--project", PROJECT,
        ),
        check=True,
    )
```
`_parse_args`에 `modes` 그룹 밖 부가 옵션 2개(`--verification-output` 옆) + 조합 검증:
```python
    parser.add_argument(
        "--delete-stale",
        action="store_true",
        help="With --verify-storage, list Storage objects the corpus no longer references.",
    )
    parser.add_argument(
        "--confirm-delete",
        action="store_true",
        help="With --delete-stale, actually delete the listed objects (default: dry-run).",
    )
    # ... (기존 args = parser.parse_args(argv) 이전 검증 블록 안에 추가)
    if args.delete_stale and not args.verify_storage:
        parser.error("--delete-stale requires --verify-storage")
    if args.confirm_delete and not args.delete_stale:
        parser.error("--confirm-delete requires --delete-stale")
```
`main()`의 verify-storage 분기(missing 개별 출력 루프 뒤)에 stale 출력 + 삭제 추가:
```python
        for path in missing:
            print(f"MISSING\t{path}")
        for path in unexpected:
            print(f"STALE\t{path}")
        if args.delete_stale and args.confirm_delete and unexpected:
            delete_remote_objects(set(unexpected))
            print(f"deleted {len(unexpected)} stale object(s)")
        return 1 if missing else 0
```
- [ ] **Step 5: 최소 구현(ci.yml)**

`changes` job `outputs`(L87-93)에 한 줄:
```yaml
      content: ${{ steps.scope.outputs.content }}
```
`tts-functions-security`(L596-619) 뒤에 신규 잡:
```yaml
  tts-storage-verify:
    name: TTS Storage completeness (content)
    needs: changes
    if: needs.changes.outputs.content == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Fail with a clear message when the GCS secret is missing
        run: |
          if [ -z "${{ secrets.GCS_TTS_VERIFY_SA_JSON }}" ]; then
            echo "::error::GCS_TTS_VERIFY_SA_JSON is not set — ask Jin to register a storage.objectViewer service account key (W9-A) before this gate can run."
            exit 1
          fi
      - name: Authenticate to Google Cloud (read-only)
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCS_TTS_VERIFY_SA_JSON }}
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Verify TTS Storage completeness
        run: python -X utf8 tool/generate_tts.py --verify-storage
```
- [ ] **Step 6: 통과 확인**

Run: `.venv\Scripts\python.exe -m unittest test_ci_scope -v`(`.github/scripts`에서) → PASS 전부(기존+신규 2).
Run: `.venv\Scripts\python.exe -m unittest tool.test_generate_tts -v` → PASS 전부(기존+신규 2).

- [ ] **Step 7: 정적 분석 등가물 + 회귀**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8')); print('yaml ok')"` → `yaml ok`(PyYAML 없으면 `pip install pyyaml` 후 재시도 — 문법 조기 확인용).
Run: `flutter analyze --no-pub` → `No issues found!`(이 태스크는 Dart 무변경, 회귀만 확인).

- [ ] **Step 8: 커밋(Jin 요청 시에만)**
```powershell
git add .github/scripts/ci_scope.py .github/scripts/test_ci_scope.py .github/workflows/ci.yml tool/generate_tts.py tool/test_generate_tts.py
git commit -m "feat(ci): 콘텐츠 변경 시 TTS Storage 완결성 게이트 + stale 목록/삭제 커맨드 추가 (지시서 4.4 / 스윕 tts-08)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
**Done criteria:** `test_ci_scope.py`·`test_generate_tts.py` 전체 GREEN(신규 4건 포함) · analyze 0 · YAML 파싱 성공 · 시크릿 미등록 상태에서 `tts-storage-verify`가 명시적 에러로 실패함(코드 리뷰로 확인).

**Report format:** ①diffstat ②RED 로그(Python 2세트) ③GREEN 로그(Python 2세트) ④analyze 0 ⑤인접 가드(각 파일 기존 케이스) ⑥예상 밖 가드 실패 ⑦미해결 의문 ≤3줄.

**Open questions:** `GCS_TTS_VERIFY_SA_JSON` 최소 권한 범위·등록 시점은 Jin 몫(§9 룰링 5, W9-A). `google-github-actions/setup-gcloud@v2`가 이 저장소에서 처음 쓰이므로 실제 유효성은 첫 PR CI 실행으로만 확인 가능.

---

## Self-review

**1) Spec coverage**

T1.1→Task1 · T1.2→Task2 · T1.3→Task3 · T1.4→Task4 · T1.5→Task5 · T1.6→Task6 · T1.7→Task7 · T1.8→Task8 — §8.1의 8개 태스크가 정확히 8개 Task로 1:1 매핑, 갭 없음. §4 브리프 템플릿 9항목은 각 태스크의 Files/Interfaces/Frozen contracts/Done criteria/Report format + Global Constraints에 반영. §9 룰링 4(LRU 80MB·mtime·스로틀)는 Task 6, 룰링 5(CI 게이트 PR+push·missing만 실패·읽기전용 SA·시크릿 부재 명시 실패)는 Task 8에 반영. 룰링 1(자동재생 범위)은 이 PR 범위 밖(PR2 T2.1·§8.2 소관) — 코드 변경 없이 참고만 했다.

**2) Placeholder scan**

"TBD"/"TODO: 구현"/"적절히 처리"/"Task N과 유사" 패턴 검색: Task 4의 `vocab_notebook_audio_test.dart` 설명에 정확한 `pumpWidget` 인자를 기존 테스트 파일에서 복사하라는 지시가 있고, 이 사실을 해당 태스크의 "Open questions"에 명시했다 — 구현 로직 자체를 생략한 TODO가 아니라 "다음 이터레이션이 실제 파일을 열어 채워야 하는 미해결 지점"의 정직한 표시다. 다른 7개 태스크·모든 코드 블록은 전부 실행 가능한 완성 코드이며 TBD/TODO/"유사하게" 문구가 없다.

**3) Type consistency**

`TtsSpeechPhase`(Task 1 정의)를 Task 2가 값 3개 그대로 재사용(재정의 없음). `TtsService.phase`(Task 1)와 `SoriSpeech.phase`(Task 2)는 필드명은 같지만 서로 다른 인스턴스(레이어 분리)임을 두 태스크 모두 명시하고, `_onEnginePhaseChanged`가 전자를 구독한다는 서술이 양쪽 Interfaces에서 일관됨. `SoriSpeech.speaking`(레거시 bool)은 Task 2 이후 어디서도 직접 대입되지 않는다(전부 `phase.value =`). `prefetchImpl`/`speakImpl`/`stopImpl`은 Task 2·3·4에서 동일 시그니처. `SoundService.playImpl`(Task 5 신설)은 타입·사용처 일치. `pruneCacheStrict`/`pruneCacheBestEffort`(Task 6)는 `clearCacheStrict`/`clearCache`와 같은 시그니처·이름 패턴. `download_first_line_bundle`(Task 7)과 `delete_remote_objects`(Task 8)는 둘 다 기존 `gcloud_argv()`/`BUCKET`/`PROJECT` 전역만 재사용. `SCOPES`에 `content` 추가(Task 8)가 `_write_outputs` 순회에 자동 편입됨을 Interfaces에 명시(배선 누락 없음).

불일치 없음.
