# 시나리오 씬 에셋 참조 감사 리포트

`python tool/audit_scene_assets.py` 로 생성 — 직접 편집 금지, 스크립트 재실행으로 갱신한다.

시나리오 `id`/`backdrop` 을 `lib/services/scene_asset_resolver.dart`(`SceneAssetResolver`) 의 파일명 규약대로 포스터(PNG)/루프(MP4) 경로로 재구성해 실제 `assets/` 파일과 대조한다. 포스터는 카테고리 폴백을 **존재 확인 없이** 반환하는 리졸버 로직 그대로라, `backdrop` 오타나 누락 파일은 실제로 앱에서 깨진 이미지 경로가 된다 — 이게 이 리포트가 잡는 핵심 버그 클래스다. 루프는 폴백이 존재를 확인하고 없으면 null 을 돌려주는 안전장치가 있어 오타가 있어도 깨지지 않는다(포스터로 대체) — 그래도 커버리지 통계는 보여준다. 수정은 이 스크립트 범위 밖(W4) — 여기선 검출·리포트만 한다.

## 오타·누락 포스터 (버그)

0건 — 스캔한 시나리오 413개 전부 포스터가 전용 파일이거나 실제로 존재하는 카테고리 파일로 해석됨.

## 오타·누락 루프

0건 — 루프 폴백은 리졸버가 존재를 확인 후 반환하므로 이 카테고리는 구조적으로 항상 0건이어야 한다(존재하면 리졸버 로직 자체가 깨진 것이니 우선 점검할 것).

## 고아 포스터 파일

0건 — `assets/illustrations/scenes` 의 14개 파일 전부 어떤 시나리오의 id(전용) 또는 backdrop(카테고리) 로 참조됨.

## 고아 루프 파일

0건 — `assets/video/loops` 의 `scene_*.mp4` 파일 5개 전부 어떤 시나리오의 id 또는 backdrop 으로 참조됨(8개는 `scene_` 접두사가 아니라 이 규약 밖이라 대상에서 제외).

## 샤드별 시나리오 수

- scenarios_a1.json: 87개
- scenarios_a2.json: 82개
- scenarios_b1.json: 75개
- scenarios_b2.json: 73개
- scenarios_c1.json: 50개
- scenarios_c2.json: 46개

## 리소스 커버리지 통계

- 포스터 디렉터리 파일 수: 14
- 루프 디렉터리: `scene_*.mp4` 5개 + 규약 밖 파일 8개

### 포스터 해석 상태별 시나리오 수

- 전용 포스터: 0개
- 카테고리 포스터(정상 폴백): 413개
- 카테고리 포스터 없음(버그): 0개
- 포스터 없음(backdrop 비어있음, 정상 — 마스코트 대체): 0개

### 루프 해석 상태별 시나리오 수

- 전용 루프: 0개
- 카테고리 루프: 87개
- 카테고리 루프 없음(리졸버 로직상 불가능해야 함): 0개
- 루프 없음, 포스터만 사용(정상 폴백): 326개
- 루프 없음(backdrop 비어있음, 정상): 0개

## 요약

- 스캔한 시나리오: **413개** (샤드 6개)
- 오타·누락 포스터: **0건**
- 오타·누락 루프: **0건**
- 고아 포스터 파일: **0건**
- 고아 루프 파일: **0건**
- 시나리오 id 중복: **0건**

