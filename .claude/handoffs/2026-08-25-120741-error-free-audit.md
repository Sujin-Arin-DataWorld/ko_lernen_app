# 2026-08-25 에러프리 감사 세션 인수인계

> Jin 의 명시 요청으로 작성 (평시 규칙: 수기 문서 금지 — git log/PR 본문이 정본).
> 작업 브랜치: `session/error-free-audit-2026-08-25` (워크트리 `~/Developer/ko_lernen_app_worktrees/error-free-audit`)

## 세션 목표
① 맥 용량 확보 → ② 워크트리들의 미병합 작업 회수 → ③ 피드백 submit 에러 해결
→ ④ 코드 전수 검사·방어 → ⑤ main 커밋·푸시.

---

## 발견한 것

### 피드백 submit 에러 — 원인 4겹 (전부 소스로 확인)
1. **서버 레벨 화이트리스트**: `functions/gye/tester_feedback_runtime.js` 의
   `LEVELS = {A1,A2,B1,B2}` 뿐 — `scenarios_c1/c2.json` 콘텐츠에서 온 피드백은
   전부 `invalid-argument` 거부. 클라이언트는 C1/C2 허용이라 시트는 멀쩡히 닫히고
   서버에서만 죽는 구조.
2. **시트 3자 불일치**: `content_feedback_sheet.dart` 의 `_canSubmit` 이 bug
   카테고리에서 구조화 필드 전부를 요구 — 모델(`draft.validate()`)·서버가 처음부터
   허용하던 **메시지-온리 버그 신고가 UI에서 차단**돼 있었다.
3. **App Check 전멸 경로**: `initializeAppCheck()` 가 던지면
   `coordinator.start()` 전체가 중단 → 익명 로그인·아웃박스·프리미엄·푸시까지 사망.
   피드백은 네트워크 호출도 못 하고 실패.
4. **인프라 미완(코드 밖)**: Play Integrity(App Check) 등록이
   `docs/release-readiness.md:109` 에 미체크로 남아 있고 08-12 핸드오프들에 실기기
   App Check 차단 이력 다수. `submitTesterFeedback` 은 `enforceAppCheck:true`.

### 미병합 작업 판정
- `feat/p7-share-scroll`: 앞 8커밋은 PR #83 스쿼시로 이미 main에 있고 content-ui
  부분은 PR 194–203 재건으로 대체됨. **살릴 것은 share-scroll 에셋 커밋 하나**
  (948d53fc) — 단, 그 작업 자체가 **테스트 6개 red 인 미완 상태**로 방치돼 있었다.
- `chaekgado-scroll-overflow` 워크트리: 코드는 main 재설계로 대체, **회귀 테스트만 유효**.
- `ko_lernen_app-uiux`: main에 전부 반영 — 회수할 것 없음.

### share-scroll red 의 원인 3개 (실측으로 판명)
- `_paperFrac*` 4값이 재생성 **전** PNG 실측 — 현행 785×1330 PNG 에선 상단이 짙은 축 위.
- 테스트가 실제 폰트 미로드 → 전 글자가 등폭 사각형(꼬리말이 마진 침범).
- 잉크 임계 0.45가 절차형 상아 종이(선형 0.75+) 기준 — 실제 황갈 에셋 종이는
  **선형 휘도 바닥 0.30** 이라 종이 그레인이 잉크로 오검출.

### main 자체의 고장
- `docs/UIUX_Audit_DesignBible_Idea.md` **0바이트 파손** (원문 2,152줄은
  `origin/backup/main-local-wip-20260820` 에만 존재). 한옥 감사 보고서·판정 CSV·
  로드맵 CSV·딥리서치 문서도 그 브랜치에만 있던 유일 사본.
- `tool/test_style_lock.py` 가 4가족 시절 고정 → F-D-ildoo(measured null)·
  F-D-share(스칼라 실측)에서 사전 고장 (2 failures + 1 error).
- `lib/widgets/sori/module_card.dart` 배지 `fontSize: 11 * comfortScale` —
  SoriTypeScale(main.dart:532 전역 배율)과 **이중 배율**.
- l10n 생성본 커밋 누락 (`listeningLevelDrawer` 키가 arb에만 있음).
- 시트가 bug 카테고리에서 빈 `TextEditingController.text('')` 를
  expectedOutcome/actualOutcome 으로 와이어에 실음 — 서버 `optionalString(minLength 1)`
  이 페이로드 전체 거부 (현재는 _canSubmit 완화로 실제 경로가 됐고, 방어 완료).

### 적대 리뷰(3렌즈+반박 검증)가 잡은 내 수정의 결함 2건 — 즉시 재수정
- 시트 _canSubmit 미완화 상태에선 빈-outcome 방어가 데드 코드 (→ 완화로 해소).
- App Check 격리 후 `unauthenticated` 가 non-retryable 이라 **아웃박스가 유효
  피드백을 영구 폐기** (→ retryable 재분류로 큐 보존).

---

## 고친 것 (세션 브랜치 커밋 순)
| 커밋 | 내용 |
|---|---|
| `dd22c58b`/`948d53fc` | 타 워크트리 미커밋 작업을 각자 브랜치에 커밋 보존 |
| merge ×2 | chaekgado 회귀 테스트 병합(코드는 main 채택) + share 에셋 cherry-pick (pubspec·STYLE_LOCK 수동 해소) |
| `4384aca5` | l10n 생성본 정합화 |
| `f276a8f6` | STYLE_LOCK 테스트를 6가족 체제로 (measured null/스칼라/placeholders 승격 전 dirs 허용) |
| `e53a139a` | 파손·유일사본 문서 5개 복구 |
| `c8f5b67c` | ModuleCard 배지 이중 배율 제거 (+테스트) |
| `bf3eafae` | 피드백 3중 수정: toWire 공백 생략 + 서버 LEVELS C1/C2 + App Check 격리 (+테스트) |
| `2c1938c5` | share-scroll 완성: 프랙션 재실측(top 0.1594 등)·FontLoader·임계 0.25 재보정·mergeGap — 10/10 green |
| `c92bc4fa` | graphify 재동기화 |
| `fb52cf72` | 리뷰 반영: _canSubmit 완화(메시지-온리 버그 허용) + unauthenticated→retryable (+테스트 2) |

**용량**: ~/.gradle 7.7G·DerivedData 3.9G·npm 2.8G·build/ 합계 ~4G 등 재생성물 삭제
→ 여유 4.0GB→22GB (검증 빌드로 일부 재사용 중).

## 검증 상태 (최종)
- `flutter analyze`: **0 issues**.
- functions 테스트: **340/340** (C1/C2 수용 테스트 포함).
- 전체 Flutter 스위트 최종 게이트(fb52cf72 후): **4,627 통과/14 스킵(플랫폼 전용)/0 실패**.
- 적대 리뷰 확정 결함 3건 → 전부 수정 반영.

---

## 후속 작업 (중요도 순)

1. **functions 배포** — LEVELS C1/C2 수정은 배포 전까지 실효 없음. C1/C2 피드백은
   계속 죽는다. Jin 승인 후:
   `firebase deploy --only functions:gye-firebase-functions:submitTesterFeedback`
2. **App Check Play Integrity 콘솔 등록** (`docs/release-readiness.md:109` 미체크) —
   Play App Signing SHA-256 등록 + Play 프로젝트 연결. 테스터 기기 근본 해결책.
   등록 전까지는 retryable 재분류 덕에 피드백이 큐에 보존됐다가 재시도된다.
3. **머지 마무리** — 최종 스위트 green 확인 후 push → PR → squash 머지(저장소 관례).
   세션이 이어서 처리 예정; 중단됐다면 브랜치는 워크트리에 그대로 있다.
4. **Jin 결정 필요한 브랜치 2개** (조사 완료, 병합 보류):
   - `origin/session/mascot-decoration-assets-2026-08-20`: 신규 캐릭터 아트 6종 +
     한옥 장식 시스템 — **08-06 tiger_sitting2 단일 포즈 정본과 충돌**, 재확인 필요.
   - `origin/session/hanok-level-proof-2026-08-20`: Jin 승인 능력 계약 문서(517줄) —
     외부 학습 증빙·보상 회수 안전장치가 main에 없음. 단 estate 참조를 일두고택
     정본 기준으로 재작성해야 함.
5. **워크트리 정리(선택)** — 회수 완료된 `ko_lernen_app-p7-scroll`·
   `.claude/worktrees/chaekgado-scroll-overflow`·`ko_lernen_app-uiux` 는 머지 확인 후
   제거 가능. 세션 워크트리는
   `git worktree remove ~/Developer/ko_lernen_app_worktrees/error-free-audit`.
6. **share_scroll.png 재생성 시**: `_paperFrac*`·`_scrollAspect` 재실측 필수
   (share_slip.dart 주석에 측정법 기록. 주의: Flutter `computeLuminance()` 는
   **선형** 휘도 — 감마 공간에서 재면 어긋난다).
7. 잔여 브랜치 청소(선택): `cursor/content-ui-bible-f7a6`·`feat/content-uiux-finish`·
   `fix/android-audio-level-balance-2026-08-19`·`origin/claude/p7-scroll-share-slip`·
   `origin/codex/mascot-sit`·hanok-level-phase0 은 전부 흡수/대체 확인됨 — 삭제 가능.
   `origin/backup/main-local-wip-20260820` 은 문서 회수 완료 후에도 백업으로 유지 권장.
