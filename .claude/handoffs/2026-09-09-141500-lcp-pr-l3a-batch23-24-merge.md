# 인수인계 — 레벨 정본화 PR-L3a(Batch 23·24) 병합, 다음은 L3b 2026-09-09

## 상태
- PR #288(브랜치 `claude/level-normalization-l2a-l2b-deploy-dbu8y6`, 워크트리 `ko_lernen_app_worktrees/lcp-pr-l3a`, base main 43769596) 커밋 3개: 0c67d19(Batch 23 1차)·eea1c60(Batch 23 2차)·4699d76(Batch 24) + 이 인수인계 커밋. draft 범위 CI는 셋 다 녹색. Jin 지시로 squash 병합(병합 SHA·main run 결과는 이 파일 맨 아래 "추가" 절에 적는다).
- 룰링 원장: `docs/data/level_bible/F10_review_lessons.md` 2026-09-08·09 절(Batch 23 1차·2차, Batch 24). Jin 표본 패킷: `docs/data/review_packets/batch_23_jin_sample.md`(Batch 23 4건 + Batch 24 6건, 아직 미판정).
- 결과 수치: 어휘 2499·cloze 1895·satz 2416·팩 224 · 유닛별 로더 커버리지 목표 충족(`audit_game_loader_coverage --check` rc 0; main 43769596은 rc 1이었음) · 1급 결손 341→321, 2급 725→703 · `audit_content_naturalness` 897→73 · 8단어 미만 팩 16→1(`a1_colors` 6, 색 이름이 더 없음).
- 검증(컨테이너, Flutter 3.44.0 = CI 핀): `flutter analyze` 0, 전체 `flutter test` 6,065 통과/7 skipped/0 실패, `validate_content` ok, can-do 정합성 0, canonical TTS manifest verified, `audit_batch_live_promotion` 5960/6331(기준선 동일), 래칫 OK, content_factory 스위트 0 실패, `tool/` 스위트는 scipy·Pillow 환경 차이 5건만(CI 통과).

## 이번 PR이 바꾼 것(요지)
- 교체 대기 L3분 4건 문안 교체(값→가격 포함), `a1_particles_in_use_1` 3→11, 신규 팩 `a1_first_class_1`(신규 9 + 하향 흡수 전공·사귀다·졸업하다), `a1_body`→a1_10 cloze 8·satz 4, 하향 흡수 누적 6.
- Batch 24: 8단어 미만 팩 16개에 62단어(A1 14·A2 24·B1 10·B2 14) + cloze/satz 62·62 + can-do 상속 행 124; cloze 배분어 위생 834항목(조사·받침 규칙, 노출 금지) — `cloze_content_guard_test` allowlist 0.
- 도구: `compile_pattern_regex` 문장부호 오탐 수정, `audit_vocab_levels` sino3_low NIKL 우선, 기준선 테스트 7건 정리(T2.9a), `audit_content_text`에 `tts_canonical_manifest.json` 분류. T3.0 출처 고지 시트(공공누리 1유형 카드 4 + KOGL 고지 + 구간 표제 + `_NoticeBox`).
- 승격 스크립트(재사용): `tools/content_factory/promote_batch23_l3a_units.py`(a1_10·a1_15 채우기, 신규 팩 등록 순서의 실물), `promote_batch24_supplement.py`(기존 팩에 단어+cloze+satz+상속 행 추가, 문장 판정기 게이트·배분어 자동 선택), `fix_cloze_distractor_hygiene.py`(배분어 규칙 교체). 데이터는 스크립트 안에 인라인. 드라이런(`check`/인자 없음) 뒤 `apply`.

## 병합 직후 Jin이 할 것(순서)
1. **TTS 합성** — 로컬(Windows 랩탑, GCP 자격 증명): `python -X utf8 tool/generate_tts.py --missing-from-storage --workers 8` → `python -X utf8 tool/generate_tts.py --verify-storage` missing 0. main 대비 새 발화 키 133건. 이걸 끝내기 전에는 main의 `TTS Storage completeness (content)` 잡이 빨간불이고, 앱에서 새 단어·예문 133건은 음성이 없다(OS 폴백 없음).
2. 표본 패킷 10건 판정(F8 D-4). 반려 항목은 해당 ID만 재작업.
3. Android 2단 범프 배포(런북 동일: 내부 +1 커밋 → 비공개 +1 커밋, `PLAY_INTERNAL_RELEASE_ENABLED` 종료 후 false 원복). iOS는 CI macOS 러너.
4. `graphify update .` 로컬 실행(컨테이너는 0.9.56이라 저장소 캐시 0.9.48과 달라 갱신본을 넣지 않았다).
5. 승인 후 L3b에서 `batch_23_manifest.json`·`batch_24_manifest.json` 등록(`provenance.approval.authority=Jin`), review 원장은 `review/c3_batch23_*`·`c2_batch23_*`·`*_batch24_*`.
6. (이전 보고서 잔여) kornorms API 키 재발급 — 채팅에 노출된 키.

## 다음 단계 — PR-L3b(새 브랜치, main에서 분기)
1. **A1 신규 팩 집필(1급 결손 321)**: `docs/data/level_bible/F2_vocab_coverage.md` 1급 결손 목록에서 주제별로 11~12단어씩. 후보 주제(결손어가 8개 이상 모이는 것): 나라·국적(미국·일본·중국·영국·프랑스·러시아·베트남·태국·캐나다·몽골·인도네시아·말레이시아), 달·날짜(일월~십이월·그저께·모레·지난주·지난달·작년·일주일), 음식·과일(갈비·갈비탕·김밥·김치찌개·된장찌개·떡볶이·삼계탕·잡채·귤·감·참외·포도·바나나·오렌지·수박?), 장소·시설(공항·극장·백화점·서점·기숙사·사무실·슈퍼마켓·영화관·목욕탕·수영장·운동장·커피숍·대사관·터미널·역·건물), 운동·취미(축구·야구·농구·테니스·탁구·수영·스키·등산·산책·태권도·노래·춤·게임·그림·피아노), 학교·일(학생증·방학·아르바이트·회사원·주부·종업원·계획·시작·연습). 미채택 하향 후보(뉴스·배우·박물관·시청·우체국·초대하다·선물하다·문화·생각·이유·설명하다·준비하다·특히·친절하다·마음에 들다·약속하다·기간)는 받아 줄 새 팩 주제가 생기면 relevel로 흡수(id 불변). 규칙: 표제어 kiiq 1급, 앱 내 중복 0, 예문 판정기 A1·미지 토큰 0·3어절 이상, cloze 정답 2음절 이상(1음절 표제어는 서술어 활용형), 배분어 조사·받침 정합·문장 미노출.
2. **A2 결손 703**: 같은 방식으로 2급 팩(a2_* 8단어 미만은 이번에 채웠으니 신규 주제: 집·가구, 날씨·계절, 몸·병원 심화, 교통 심화, 직장 기초 등).
3. **사람 판단 필요 품질 항목**: `formality_mix` 33(시나리오)·`josa_dup` 28, 레벨 초과 의심(vocab over2 159·cloze 35·satz 26·smalltalk 19 — `tool/content_level_suspects.csv`, `tool/vocab_level_suspects.csv`), `cloze_b2_0040`(전공이 뭐예요? — B2 direct 참조 잔류), `b2_honorifics_1` 번들 하향(L4), `replacement_backlog.json` 15건(L4), "＿＿＿가 있어요"류 열린 틀의 배분어 그럴듯함.
4. **도구 드리프트**: `tools/content_factory/build_can_do_segments.py --check`가 `unknown segment route 'a2_cafe_study'`로 실패(PR-L2a 팩 개명 미반영; can-do 파일은 relevel 도구·수동 편집으로만 유지 중) → 생성기 스펙 갱신 또는 폐기 결정. `validate_promoted_batch`는 batch_09_4x 외 매니페스트 대부분이 main에서도 실패(상태·승인·은퇴 시나리오) — 추적 게이트는 `audit_batch_live_promotion --check`뿐.
5. C1/C2는 20팩·240단어씩, NIKL 5·6급 보유율 7%/5% — 별도 트랙.

## 주의
- 새 팩 등록 체크리스트(F10 2026-09-09 2차 절): CSV → `curriculum_manifest.json`(vocabPackUnitMap·clozeTopicUnitMap) → `can_do_segments.json`(cluster ref+seed, revision) → `can_do_content_authorities.json`(vocabPack ref·seed·상속 행·카운트, `tool/refresh_can_do_vocab_fingerprints.py`) → Dart `vocab_pack_service`·`cloze_topic_groups`·`dancheong_stamp` → 카운트 핀(`test/data_loader_test.dart`, `test/cloze_topic_groups_test.dart`, `tool/test_cefr_lexicon.py`, `tools/content_factory/test_level_content_4x.py`) → `content_audit_manifest.json` → canonical TTS manifest → 리포트 재생성(`audit_content_levels`·`audit_vocab_levels`·`build_level_bible_tables`; F6은 `git checkout`으로 복원, F9 손 편집 행 재적용).
- cloze 게임 계약(Dart): 정답 2음절 이상, 배분어 3개·정답 아님·`sentenceKo` 부분 문자열 아님. satz: 3어절 이상, 배분어 2개.
- copy-revision 원장(`review/promoted_copy_revisions_20260822.json`)의 afterSha는 `validate_promoted_batch`의 `_relevel_normalized_live` 정규화로 계산해야 한다(원본 live 지문으로 갱신하면 relevel 이동 항목이 stale).
- 파이썬 스위트(batch_11/13/14/15 빌더 테스트)가 review CSV 6개를 LF로 덮어쓴다 — 커밋 전 `git checkout --`로 복원.
- Jin 로컬은 Windows 랩탑. "맥"은 CI macOS 러너(iOS 잡)만 뜻한다.
